---
phase: 01-trace-contract-brownfield-reset
plan: 04
type: execute
wave: 2
depends_on:
  - 01-03
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - README.md
  - CLAUDE.md
  - AGENTS.md
  - docs/preserved-capabilities.md
autonomous: true
requirements:
  - FOUND-01
user_setup: []
must_haves:
  truths:
    - "All planning documents use agent-tracing-dashboard branding"
    - "README.md and AGENTS.md describe the project as multi-source tracing dashboard"
    - "OVAO branding only appears in historical/archival context"
    - "No component names, route paths, or directory structures are renamed (per D-09)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Project positioning and core value statement"
      contains: "agent-tracing-dashboard"
    - path: "README.md"
      provides: "User-facing project description"
      contains: "agent-tracing-dashboard"
    - path: "CLAUDE.md"
      provides: "AI agent project instructions"
      contains: "agent-tracing-dashboard"
  key_links:
    - from: "README.md"
      to: "project positioning"
      via: "branding update"
      pattern: "agent-tracing-dashboard appears in title and first paragraph"
    - from: ".planning/STATE.md"
      to: "project context"
      via: "current focus update"
      pattern: "Current Focus: Phase 1 — Trace Contract & Brownfield Reset"
---

<objective>
Update project documentation and visible labels to reposition from OVAO (single-source OpenClaw dashboard) to agent-tracing-dashboard (multi-source tracing tool).

Purpose: Align documentation with the new product scope (OpenClaw + Claude Code + Codex) while preserving the cyberpunk HUD design language and existing code structure.

Output: All .planning/ docs, README.md, CLAUDE.md, AGENTS.md updated to use agent-tracing-dashboard branding. OVAO only appears in historical context.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-trace-contract-brownfield-reset/01-CONTEXT.md
@.planning/phases/01-trace-contract-brownfield-reset/01-RESEARCH.md
@.planning/REQUIREMENTS.md
@/Users/ebbi/Work/ai-dashboard-projects/agents-tracing-dashboard/README.md
@/Users/ebbi/Work/ai-dashboard-projects/agents-tracing-dashboard/CLAUDE.md
@/Users/ebbi/Work/ai-dashboard-projects/agents-tracing-dashboard/AGENTS.md
</context>

<interfaces>
<!-- Key project positioning strings that must be updated -->

From README.md (current OVAO branding):
```markdown
# OVAO — OpenClaw Visual Agents Office
```

This must become:
```markdown
# agent-tracing-dashboard

**Multi-source AI agent session tracing dashboard for OpenClaw, Claude Code, and Codex**
```

From CLAUDE.md (current description):
```
赛博朋克 HUD 风格的 Agent 可视化管理界面。通过 WebSocket 连接 OpenClaw Gateway，实时展示多个 AI Agent 的状态、日志、Office 布局和单 Agent 工作区。
```

This must be updated to:
```
赛博朋克 HUD 风格的 multi-source AI agent session tracing dashboard。支持 OpenClaw、Claude Code、Codex 三个数据来源，提供 session browsing、turn replay 和 activity 追踪。
```

Key constraint per D-09: DO NOT rename component names, route paths, or directory structures. Only change documentation and visible labels.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update .planning/ core documents</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    1. .planning/PROJECT.md (project positioning)
    2. .planning/REQUIREMENTS.md (requirement descriptions)
    3. .planning/ROADMAP.md (vision and scope)
    4. .planning/STATE.md (current position and focus)
    5. .planning/phases/01-trace-contract-brownfield-reset/01-CONTEXT.md (D-09 decision on rename scope)
  </read_first>
  <action>
1. **Update .planning/PROJECT.md**:
   - Replace "OVAO — OpenClaw Visual Agents Office" with "agent-tracing-dashboard" in the title
   - Update project description from single-source (OpenClaw) to multi-source (OpenClaw + Claude Code + Codex)
   - Update core value statement to emphasize "session tracing" and "turn replay" rather than "visual office"
   - Keep references to cyberpunk HUD design language (per D-11)
   - Add note: "Formerly known as OVAO during initial development" for historical context

2. **Update .planning/REQUIREMENTS.md**:
   - Change project description in header from OVAO to agent-tracing-dashboard
   - Ensure all requirement descriptions use "agent-tracing-dashboard" when referring to the project
   - Verify FOUND-01 requirement states: "项目文档、导航文案和默认入口使用 agent-tracing-dashboard 语义"

3. **Update .planning/ROADMAP.md**:
   - Change vision statement from "把 OVAO 从 OpenClaw visual office/dashboard 改造" to "Build agent-tracing-dashboard as a multi-source session tracing tool"
   - Update scope section to list "OpenClaw / Claude Code / Codex 三个 source dashboard" (not single-source)
   - Ensure milestone descriptions reference agent-tracing-dashboard

4. **Update .planning/STATE.md**:
   - Change "Project: OVAO" to "Project: agent-tracing-dashboard"
   - Update core value statement to match PROJECT.md
   - Ensure "Current Focus" reflects Phase 1 brownfield reset context
   - Keep git status and session continuity sections (these are accurate metadata)

5. **Verification**:
   - grep -r "OVAO" .planning/*.md — should return 0 results (except historical references)
   - grep -r "agent-tracing-dashboard" .planning/*.md — should return >= 10 results
   - DO NOT change component names, route paths, or directory names (per D-09)

Scope per D-09: Only change documentation text and visible labels. NO changes to:
- Component file names (e.g., AgentInfoCard.tsx)
- Route paths (e.g., /dashboard, /sessions)
- Directory names (e.g., app/(shell)/dashboard/)
- Type names imported from gateway/
  </action>
  <verify>
    <automated>grep -c "agent-tracing-dashboard" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/PROJECT.md title and first paragraph mention agent-tracing-dashboard
    - .planning/REQUIREMENTS.md header describes agent-tracing-dashboard project
    - .planning/ROADMAP.md vision references agent-tracing-dashboard
    - .planning/STATE.md project name is agent-tracing-dashboard
    - grep -c "OVAO" .planning/*.md (excluding grep output itself) returns <= 2 (only historical references allowed)
    - grep -c "agent-tracing-dashboard" .planning/*.md returns >= 10
    - No component names, route paths, or directory names were changed
  </acceptance_criteria>
  <done>
    All .planning/ core documents use agent-tracing-dashboard branding. OVAO only appears in historical context (e.g., "formerly known as OVAO").
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update user-facing documentation</name>
  <files>README.md, CLAUDE.md, AGENTS.md, docs/preserved-capabilities.md</files>
  <read_first>
    1. README.md (main project readme)
    2. CLAUDE.md (AI agent project instructions)
    3. AGENTS.md (additional project instructions)
    4. docs/preserved-capabilities.md (created in Plan 03, may need branding updates)
    5. .planning/phases/01-trace-contract-brownfield-reset/01-CONTEXT.md (D-11 decision on HUD design preservation)
  </read_first>
  <action>
1. **Update README.md**:
   - Change title from "# OVAO — OpenClaw Visual Agents Office" to "# agent-tracing-dashboard"
   - Add subtitle: "**Multi-source AI agent session tracing dashboard**"
   - Update first paragraph to describe support for OpenClaw, Claude Code, and Codex
   - Replace "OpenClaw visual office/dashboard" with "session tracing and turn replay"
   - Keep cyberpunk HUD design references (per D-11)
   - Add note: "This project was formerly known as OVAO during initial development."

2. **Update CLAUDE.md**:
   - Change title from "# OVAO — OpenClaw Visual Agents Office" to "# agent-tracing-dashboard"
   - Update description from "赛博朋克 HUD 风格的 Agent 可视化管理界面" to "赛博朋克 HUD 风格的 multi-source AI agent session tracing dashboard"
   - Add "支持 OpenClaw、Claude Code、Codex 三个数据来源"
   - Replace "实时展示多个 AI Agent 的状态、日志、Office 布局和单 Agent 工作区" with "提供 session browsing、turn replay 和 activity 追踪"
   - Keep all tech stack, architecture, conventions, and gotchas sections (these are still accurate)
   - Keep Gateway connection instructions (still relevant for OpenClaw source)

3. **Update AGENTS.md** (if it exists and contains OVAO references):
   - Replace any "OVAO" with "agent-tracing-dashboard"
   - Update project descriptions to reflect multi-source scope
   - Keep technical content intact

4. **Update docs/preserved-capabilities.md** (created in Plan 03):
   - Ensure document header references agent-tracing-dashboard
   - Add note that this is a Phase 1 brownfield reset artifact
   - Keep all capability inventory content (technical accuracy)

5. **Verification**:
   - grep "OVAO" README.md CLAUDE.md AGENTS.md — should return 0 (except historical "formerly known as" note)
   - grep "agent-tracing-dashboard" README.md CLAUDE.md — should return >= 5
   - DO NOT change code examples, component names, or route names in documentation

Scope per D-09: Only change user-visible text. DO NOT change:
- Code snippets with component names (e.g., AgentInfoCard)
- Route paths (e.g., /dashboard, /workspace)
- Directory structures in architecture diagrams
  </action>
  <verify>
    <automated>grep -c "agent-tracing-dashboard" README.md CLAUDE.md AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md title is "agent-tracing-dashboard" (not OVAO)
    - README.md mentions OpenClaw, Claude Code, and Codex as supported sources
    - CLAUDE.md title and description use agent-tracing-dashboard
    - CLAUDE.md description lists three sources (OpenClaw, Claude Code, Codex)
    - grep -c "OVAO" README.md CLAUDE.md AGENTS.md returns <= 3 (only "formerly known as" references allowed)
    - grep -c "agent-tracing-dashboard" README.md CLAUDE.md AGENTS.md returns >= 8
    - Cyberpunk HUD design language references are preserved (grep "HUD\|cyberpunk" returns >= 1)
  </acceptance_criteria>
  <done>
    User-facing documentation (README.md, CLAUDE.md, AGENTS.md) uses agent-tracing-dashboard branding and describes multi-source scope. Cyberpunk HUD design language is preserved. OVAO only appears in historical "formerly known as" notes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All project documentation (.planning/ core docs, README.md, CLAUDE.md, AGENTS.md) has been updated from OVAO to agent-tracing-dashboard branding. The documentation now describes a multi-source tracing dashboard (OpenClaw + Claude Code + Codex) rather than a single-source OpenClaw visual office.
  </what-built>
  <how-to-verify>
    1. **Check .planning/ documents**:
       - Open .planning/PROJECT.md — title should be "agent-tracing-dashboard"
       - Open .planning/ROADMAP.md — vision should mention multi-source scope
       - Run `grep -r "OVAO" .planning/*.md | grep -v "formerly known"` — should return 0

    2. **Check user-facing docs**:
       - Open README.md — title should be "agent-tracing-dashboard"
       - Open CLAUDE.md — description should list OpenClaw, Claude Code, Codex
       - Run `grep "OVAO" README.md CLAUDE.md AGENTS.md | grep -v "formerly known"` — should return 0

    3. **Verify preservation**:
       - Run `grep -c "cyberpunk\|HUD" README.md CLAUDE.md` — should return >= 1 (design language preserved)
       - Check that component names (AgentInfoCard, SessionList, etc.) are NOT renamed in docs

    4. **Manual review**:
       - Read README.md first paragraph — does it describe multi-source tracing?
       - Read CLAUDE.md header — does it mention three sources?
  </how-to-verify>
  <resume-signal>
    Type "documentation updated" if all OVAO references are replaced (except historical), cyberpunk HUD design language is preserved, and multi-source scope is clear. Or describe issues found.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation only — no runtime data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | N/A |

**Note**: Plan 04 is pure documentation work. No security threats apply.
</threat_model>

<verification>
## Overall Phase Checks

### Branding Consistency
- [ ] All .planning/ documents use "agent-tracing-dashboard"
- [ ] README.md, CLAUDE.md, AGENTS.md use "agent-tracing-dashboard"
- [ ] OVAO only appears in "formerly known as" context

### Scope Accuracy
- [ ] Documentation mentions OpenClaw, Claude Code, and Codex as supported sources
- [ ] "Multi-source" and "session tracing" terminology used
- [ ] "Visual office" terminology replaced with "tracing dashboard"

### Preservation
- [ ] Cyberpunk HUD design language references are preserved
- [ ] No component names, route paths, or directory names were changed
- [ ] Technical content (tech stack, architecture, conventions) remains accurate
</verification>

<success_criteria>
## Measurable Completion

1. **Planning Docs Updated**: .planning/PROJECT.md, REQUIREMENTS.md, ROADMAP.md, STATE.md all use agent-tracing-dashboard
2. **User Docs Updated**: README.md, CLAUDE.md, AGENTS.md all use agent-tracing-dashboard
3. **OVAO Removed**: grep "OVAO" returns <= 3 total (only historical "formerly known as" references)
4. **Multi-Scope Clear**: README and CLAUDE mention OpenClaw, Claude Code, Codex
5. **Design Preserved**: Cyberpunk HUD design language references remain (grep "HUD\|cyberpunk" >= 1)
6. **No Structural Changes**: Component names, route paths, directory names unchanged (per D-09)
7. **Documentation Accurate**: Technical content (tech stack, commands, architecture) is still correct
</success_criteria>

<output>
After completion, create `.planning/phases/01-trace-contract-brownfield-reset/01-04-SUMMARY.md` with:
- List of files updated
- Count of OVAO → agent-tracing-dashboard replacements made
- Any instances where OVAO was intentionally kept (historical context)
- Verification that cyberpunk HUD design language is preserved
- Confirmation that no component/route/directory names were changed
- Next steps for Phase 1 completion and Phase 2 preparation
</output>
