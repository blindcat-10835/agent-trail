---
phase: 04-agent-dashboard
plan: 04-02-PLAN
title: Agent Detail Right Panel
depends_on: [04-01-PLAN]
requirements: [WORK-01, WORK-02]
wave: 1
---

# Agent Detail Right Panel

## Goal

实现右侧 300px 面板的 Agent 详情视图，展示 Agent 基本信息、实时日志流和能力信息。当用户点击 Agent 卡片时，右侧面板展示该 Agent 的详细状态；未选中时显示空状态。日志流采用终端风格，自动滚动到底部，彩色编码区分事件类型（lifecycle/tool/assistant/error）。

## Files to Create

- `components/dashboard/agent-detail-panel.tsx` — Agent 详情面板容器，处理选中/未选中/加载/错误状态
- `components/dashboard/agent-basic-info.tsx` — Agent 基本信息区（头像/名称/状态/工具/会话时长）
- `components/dashboard/agent-log-stream.tsx` — 终端风格日志流组件（自动滚动 + 彩色编码）
- `components/dashboard/agent-capabilities.tsx` — Agent 能力信息区（模型/工具列表）

## Files to Modify

- `app/(shell)/dashboard/page.tsx` — 替换右侧占位面板为 `AgentDetailPanel` 组件

## Implementation Steps

### Step 1: Create AgentBasicInfo component

**File:** `components/dashboard/agent-basic-info.tsx`

**What:** 显示 Agent 基本信息区，包括头像、名称、状态徽章、当前工具和会话时长

**Detail:**
```tsx
'use client'

import { Badge } from '@/components/ui/badge'
import { Clock } from 'lucide-react'
import { cn } from '@/lib/utils'
import type { AgentInfo } from '@/stores/gateway/gateway-store'

interface AgentBasicInfoProps {
  agent: AgentInfo
  className?: string
}

// Agent status colors (reuse from Plan 4.1)
const statusColors: Record<AgentInfo['status'], string> = {
  idle: 'oklch(0.55 0.008 160)',        // gray-500
  working: 'oklch(0.62 0.17 65)',       // blue-500 (accent)
  tool_calling: 'oklch(0.8 0.17 75)',   // yellow-500 (accent)
  speaking: 'oklch(0.65 0.15 145)',     // green-500
  error: 'oklch(0.577 0.245 27.325)',   // red-500 (destructive)
}

export function AgentBasicInfo({ agent, className }: AgentBasicInfoProps) {
  const statusColor = statusColors[agent.status]

  // Calculate session duration
  const sessionDuration = agent.sessionStartedAt
    ? Math.floor((Date.now() - agent.sessionStartedAt) / 1000)
    : null

  const formatDuration = (seconds: number) => {
    const mins = Math.floor(seconds / 60)
    const secs = seconds % 60
    return `${mins}:${secs.toString().padStart(2, '0')}`
  }

  return (
    <div className={cn('p-4 border-b border-border space-y-3', className)}>
      {/* Avatar + Name */}
      <div className="flex items-center gap-3">
        {agent.avatarUrl ? (
          <img
            src={agent.avatarUrl}
            alt={agent.name}
            className="w-12 h-12 rounded bg-muted"
          />
        ) : (
          <div className="w-12 h-12 rounded bg-muted flex items-center justify-center text-lg font-semibold">
            {agent.name.charAt(0).toUpperCase()}
          </div>
        )}
        <div className="flex-1 min-w-0">
          <div className="text-sm font-bold text-foreground truncate">{agent.name}</div>
          <div className="flex items-center gap-2 mt-1">
            <div
              className="w-2 h-2 rounded-full"
              style={{ backgroundColor: statusColor }}
            />
            <span className="text-xs text-muted-foreground capitalize">{agent.status}</span>
          </div>
        </div>
      </div>

      {/* Current tool (if any) */}
      {agent.currentTool && (
        <div className="flex items-center gap-2">
          <Badge variant="secondary" className="text-xs">
            {agent.currentTool}
          </Badge>
        </div>
      )}

      {/* Session duration */}
      {sessionDuration !== null && (
        <div className="flex items-center gap-2 text-xs text-muted-foreground">
          <Clock className="h-3 w-3" />
          <span>Session: {formatDuration(sessionDuration)}</span>
        </div>
      )}

      {/* Agent ID */}
      <div className="text-xs text-muted-foreground font-mono">
        ID: {agent.id}
      </div>
    </div>
  )
}
```

---

### Step 2: Create AgentLogStream component

**File:** `components/dashboard/agent-log-stream.tsx`

**What:** 终端风格日志流组件，黑色背景 + 等宽字体 + 彩色编码 + 自动滚动

**Detail:**
```tsx
'use client'

import { useEffect, useRef } from 'react'
import { LogEntry } from '@/types/log'
import { cn } from '@/lib/utils'

interface AgentLogStreamProps {
  logs: LogEntry[]
  className?: string
}

// Log type colors (terminal style, optimized for dark background)
const logTypeColors: Record<LogEntry['type'], string> = {
  lifecycle: 'text-white',
  tool: 'text-yellow-400',
  assistant: 'text-green-400',
  error: 'text-red-400',
}

export function AgentLogStream({ logs, className }: AgentLogStreamProps) {
  const logEndRef = useRef<HTMLDivElement>(null)

  // Auto-scroll to bottom when logs update
  useEffect(() => {
    logEndRef.current?.scrollIntoView({ behavior: 'smooth' })
  }, [logs])

  if (logs.length === 0) {
    return (
      <div className={cn('h-full flex items-center justify-center text-xs text-muted-foreground', className)}>
        No logs yet
      </div>
    )
  }

  return (
    <div className={cn('h-full bg-black font-mono text-xs p-3 overflow-y-auto', className)}>
      <div className="space-y-1">
        {logs.map((log) => (
          <div
            key={log.id}
            className={cn('flex gap-2', logTypeColors[log.type])}
          >
            <span className="text-muted-foreground select-none">{log.time}</span>
            <span className="flex-1 break-words">{log.content}</span>
          </div>
        ))}
      </div>
      {/* Invisible anchor for auto-scroll */}
      <div ref={logEndRef} />
    </div>
  )
}
```

---

### Step 3: Create AgentCapabilities component

**File:** `components/dashboard/agent-capabilities.tsx`

**What:** 显示 Agent 能力信息（模型列表、工具列表），如果无信息则显示占位文本

**Detail:**
```tsx
'use client'

import { Badge } from '@/components/ui/badge'
import { Cpu, Wrench } from 'lucide-react'
import { cn } from '@/lib/utils'
import type { AgentInfo } from '@/stores/gateway/gateway-store'

interface AgentCapabilitiesProps {
  agent: AgentInfo
  className?: string
}

// Placeholder: AgentInfo type doesn't include capabilities yet
// This will be populated when Gateway API provides this data
export function AgentCapabilities({ agent, className }: AgentCapabilitiesProps) {
  return (
    <div className={cn('p-4 border-t border-border space-y-3', className)}>
      {/* Models section */}
      <div className="space-y-2">
        <div className="flex items-center gap-2 text-xs font-semibold text-foreground">
          <Cpu className="h-3 w-3" />
          <span>Models</span>
        </div>
        <div className="text-xs text-muted-foreground">
          No model information available
        </div>
      </div>

      {/* Tools section */}
      <div className="space-y-2">
        <div className="flex items-center gap-2 text-xs font-semibold text-foreground">
          <Wrench className="h-3 w-3" />
          <span>Tools</span>
        </div>
        {agent.currentTool ? (
          <Badge variant="secondary" className="text-xs">
            {agent.currentTool}
          </Badge>
        ) : (
          <div className="text-xs text-muted-foreground">
            No tool information available
          </div>
        )}
      </div>
    </div>
  )
}
```

**Note:** 当前 `AgentInfo` 类型不包含模型/工具列表，此组件先实现占位结构。后续 Phase 可扩展 Gateway API 和类型定义来补充真实数据。

---

### Step 4: Create AgentDetailPanel component

**File:** `components/dashboard/agent-detail-panel.tsx`

**What:** Agent 详情面板容器，处理选中/未选中/加载/错误/invalid-agent 状态

**Detail:**
```tsx
'use client'

import { useGatewayStore } from '@/stores/gateway/gateway-store'
import { selectAgentDetailState } from '@/stores/gateway/p0-selectors'
import { AgentBasicInfo } from './agent-basic-info'
import { AgentLogStream } from './agent-log-stream'
import { AgentCapabilities } from './agent-capabilities'
import { EmptyState } from './empty-state'
import { Loader2, AlertCircle } from 'lucide-react'
import { cn } from '@/lib/utils'

interface AgentDetailPanelProps {
  selectedAgentId: string | null
  className?: string
}

export function AgentDetailPanel({ selectedAgentId, className }: AgentDetailPanelProps) {
  const { state: uiState, data: agent } = selectAgentDetailState(
    selectedAgentId ?? ''
  )(useGatewayStore())

  // No agent selected
  if (!selectedAgentId) {
    return (
      <EmptyState
        title="No agent selected"
        description="Click an agent card to view details"
        className={cn('h-full', className)}
      />
    )
  }

  // Loading state
  if (uiState === 'loading') {
    return (
      <div className={cn('h-full flex items-center justify-center', className)}>
        <Loader2 className="h-6 w-6 animate-spin text-muted-foreground" />
      </div>
    )
  }

  // Error state
  if (uiState === 'error' || uiState === 'disconnected') {
    return (
      <EmptyState
        icon={<AlertCircle className="h-8 w-8 text-destructive" />}
        title="Connection error"
        description="Failed to load agent details"
        className={cn('h-full', className)}
      />
    )
  }

  // Invalid agent (not found)
  if (uiState === 'invalid-agent' || !agent) {
    return (
      <EmptyState
        icon={<AlertCircle className="h-8 w-8 text-destructive" />}
        title="Agent not found"
        description={`Agent "${selectedAgentId}" does not exist`}
        className={cn('h-full', className)}
      />
    )
  }

  // Success: show agent details
  const agentLogs = useGatewayStore((state) => state.agentLogs[selectedAgentId] ?? [])

  return (
    <div className={cn('h-full flex flex-col bg-card', className)}>
      {/* Basic info section */}
      <AgentBasicInfo agent={agent} />

      {/* Log stream section */}
      <div className="flex-1 min-h-0">
        <div className="h-full flex flex-col">
          <div className="px-4 py-2 border-b border-border">
            <h3 className="text-xs font-semibold text-foreground">Logs</h3>
          </div>
          <AgentLogStream logs={agentLogs} />
        </div>
      </div>

      {/* Capabilities section */}
      <AgentCapabilities agent={agent} />
    </div>
  )
}
```

---

### Step 5: Update Dashboard page

**File:** `app/(shell)/dashboard/page.tsx`

**What:** 替换右侧占位面板为 `AgentDetailPanel` 组件

**Detail:**
```tsx
'use client'

import { useState } from 'react'
import { DashboardLeftPanel } from '@/components/dashboard/dashboard-left-panel'
import { DashboardKpiBar } from '@/components/dashboard/dashboard-kpi-bar'
import { AgentSearchFilter } from '@/components/dashboard/agent-search-filter'
import { AgentCardGrid } from '@/components/dashboard/agent-card-grid'
import { AgentDetailPanel } from '@/components/dashboard/agent-detail-panel'
import { useGatewayStore } from '@/stores/gateway/gateway-store'
import type { AgentDisplayStatus } from '@/stores/gateway/gateway-store'

export default function DashboardPage() {
  const agents = useGatewayStore((state) => Array.from(state.agents.values()))

  // Local UI state
  const [selectedAgentId, setSelectedAgentId] = useState<string | null>(null)
  const [searchQuery, setSearchQuery] = useState('')
  const [statusFilter, setStatusFilter] = useState<AgentDisplayStatus | 'all'>('all')

  return (
    <>
      {/* Left panel: 260px */}
      <DashboardLeftPanel
        selectedAgentId={selectedAgentId}
        onAgentClick={setSelectedAgentId}
      />

      {/* Center: 1fr */}
      <div className="flex flex-col min-h-0">
        <DashboardKpiBar />
        <AgentSearchFilter
          searchQuery={searchQuery}
          onSearchChange={setSearchQuery}
          statusFilter={statusFilter}
          onStatusChange={setStatusFilter}
        />
        <div className="flex-1 overflow-y-auto">
          <AgentCardGrid
            agents={agents}
            selectedAgentId={selectedAgentId}
            onAgentClick={setSelectedAgentId}
            searchQuery={searchQuery}
            statusFilter={statusFilter}
          />
        </div>
      </div>

      {/* Right panel: 300px */}
      <AgentDetailPanel selectedAgentId={selectedAgentId} />
    </>
  )
}
```

---

## Store Integration

本计划消费以下 store 状态和 selectors：

**Raw state (from `useGatewayStore()`):**
- `agentLogs: Record<string, LogEntry[]>` — 按 agentId 索引的日志数组（最多 200 条/agent）

**Selectors (from `p0-selectors.ts`):**
- `selectAgentDetailState(agentId)(state)` — 返回 `{ state: AgentDetailUIState, data: AgentInfo | null }`
  - `state` 可能是：`loading` | `success` | `empty` | `error` | `disconnected` | `invalid-agent`
  - 用于处理各种 UI 状态（加载中、错误、Agent 不存在等）

**数据流:**
1. 用户点击 Agent 卡片 → `setSelectedAgentId(agentId)`
2. `AgentDetailPanel` 接收 `selectedAgentId`
3. 调用 `selectAgentDetailState(selectedAgentId)` 获取 UI state 和 Agent 数据
4. 根据 UI state 渲染对应状态（加载/错误/成功）
5. 成功时，从 `agentLogs[selectedAgentId]` 获取日志数组

---

## HUD Visual Tokens Used

**Semantic tokens:**
- `bg-card`, `border-border` — 面板背景和边框
- `text-foreground`, `text-muted-foreground` — 文本颜色
- `text-destructive` — 错误状态
- `bg-black` — 日志流背景（终端风格）

**Custom colors (terminal style):**
- 日志类型颜色（通过 Tailwind 类名）：
  - `lifecycle` → `text-white`
  - `tool` → `text-yellow-400`
  - `assistant` → `text-green-400`
  - `error` → `text-red-400`

**Font:**
- `font-mono` — 日志流使用 JetBrains Mono（已在 `app/globals.css` 中定义）

---

## Success Criteria

实现完成后，以下条件必须为真：

1. ✅ 点击 Agent 卡片时，右侧面板显示该 Agent 的基本信息（头像/名称/状态/工具）
2. ✅ Agent 基本信息区显示当前状态徽章（颜色编码与中心卡片一致）
3. ✅ Agent 基本信息区显示当前工具（如果有）
4. ✅ Agent 基本信息区显示会话时长（如果会话活跃）
5. ✅ 右侧面板显示实时日志流（从 `agentLogs[agentId]` 获取）
6. ✅ 日志流采用终端风格（黑色背景 + 等宽字体）
7. ✅ 日志行按类型彩色编码（lifecycle=白色 / tool=黄色 / assistant=绿色 / error=红色）
8. ✅ 日志流自动滚动到底部（新日志到达时）
9. ✅ 日志流最多显示 200 条（由 store 强制限制，组件无需额外处理）
10. ✅ 未选中 Agent 时，右侧面板显示空状态（"No agent selected"）
11. ✅ 选中 Agent 但数据加载中时，显示 loading spinner
12. ✅ 选中 Agent 但连接错误时，显示错误状态
13. ✅ 选中不存在的 Agent 时，显示 "Agent not found" 状态
14. ✅ 右侧面板固定宽度 300px（由 Shell Grid `grid-cols-[260px_1fr_300px]` 保证）
15. ✅ 日志流区域可滚动（`overflow-y-auto`），面板高度自适应（`flex-1 min-h-0`）
16. ✅ `pnpm build` 成功，无构建错误
17. ✅ light/dark 主题下日志流颜色对比度良好（终端在黑色背景上始终可见）

---

## Commit Strategy

**单个 atomic commit:**

```
feat(dashboard): implement agent detail right panel

- Create AgentBasicInfo component (avatar/name/status/tool/session duration)
- Create AgentLogStream component (terminal-style + auto-scroll + color-coded)
- Create AgentCapabilities component (models/tools placeholder)
- Create AgentDetailPanel component (handle all UI states)
- Update Dashboard page (replace right panel placeholder)

Features:
- Click agent card to show details in right panel
- Real-time log stream with terminal styling
- Auto-scroll to latest log entry
- Color-coded log types (lifecycle/tool/assistant/error)
- Session duration display
- Empty/loading/error/invalid-agent states

Requirements covered:
- WORK-01: Single agent detail view (status/logs/capabilities)
- WORK-02: Real-time log stream (WebSocket push, color-coded events)

Depends on: 04-01-PLAN (dashboard center area + left panel)
```

---

*Plan created: 2026-04-30*
*Phase 4 complete after this plan*
