---
phase: 18-qoder-source-integration
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
  - 18-02
files_modified:
  - ingest/parser/qoder.ts
  - ingest/sync/sources.ts
  - ingest/sync/index.ts
  - ingest/api/sources.ts
  - tests/fixtures/qoder/build-fixture.ts
  - tests/fixtures/qoder/MANIFEST.json
  - tests/unit/ingest/parser/qoder.test.ts
autonomous: true
requirements:
  - QDR-102
  - QDR-103
  - QDR-106
  - QDR-107
  - QDR-108
  - QDR-110
must_haves:
  truths:
    - "discoverQoderSources(dbPaths?) opens each candidate readonly, validates `chat_session`/`chat_record`/`chat_message` tables exist, returns { type:'qoder', path, sessionCount } when valid and { status:'empty'|'error' } without crashing when absent (D-08; QDR-102; QDR-110)."
    - "parseQoderSession(dbPath, rawSessionId) opens the Qoder DB with `{ readonly: true, fileMustExist: true }`; produces a ParseResult with session.id = `qoder:<rawSessionId>` and source_session_id = rawSessionId (D-10; QDR-103)."
    - "Tool messages map to TraceToolCall + TraceToolResultEvent; the seven observed tools (read_file, search_file, grep_code, search_codebase, list_dir, run_in_terminal, Agent) map to canonical categories per SPEC §7 (QDR-106)."
    - "Tool messages with toolCallStatus = 'ERROR' surface `status='error'` and the errorMsg body (QDR-106)."
    - "chat_session.parent_session_id + parent_tool_call_id produce a TraceSubagentLink with relationship='spawned' (matches schema CHECK; subagent-derived type), subagentSource='qoder', and messageOrdinal derived by reverse-lookup of the Agent tool call's toolCallId in the parent session (D-06 part 1, D-07; QDR-107)."
    - "TraceSession.relationshipType='subagent' for the child session row (independent field — not the same as TraceSubagentLink.relationship)."
    - "Token attribution per assistant message: inputTokens=prompt_tokens, outputTokens=completion_tokens, cacheReadTokens=cached_tokens, totalTokens=prompt_tokens+completion_tokens; cached_tokens NEVER added to totalTokens; max_input_tokens stored as metadata only (QDR-108)."
    - "Per-session fingerprint: sha256('qoder-session-v1:<id>:<gmt_modified>:<msg_count>:<max_msg_gmt>') stored via writeSessionToDatabase into sessions.file_hash; existing.file_hash equality skips the session (D-03; QDR-110)."
    - "Locked DB / SQLITE_BUSY handled with a small retry-then-warning policy; ingest never crashes; previous canonical rows are preserved (D-10; QDR-110)."
    - "Synthetic SQLite fixture covers 1 root + 1 subagent session, all 7 tools, 1 ERROR tool, 1 assistant message with all 4 token_info fields, 1 user message, 1 message with malformed tool_result JSON (D-08, D-09)."
    - "Code paths verifiably never reference `machine_token.json`, `supabase_token`, or `secret://` strings (D-10; QDR-110)."
  artifacts:
    - path: "ingest/parser/qoder.ts"
      provides: "parseQoderSession(dbPath, rawSessionId, options) → Promise<ParseResult>"
      exports: ["parseQoderSession", "computeQoderSessionFingerprint", "inferQoderToolCategory"]
      contains: "qoder-session-v1:"
    - path: "ingest/sync/sources.ts"
      provides: "discoverQoderSources(dbPaths?) → Promise<DiscoveredSource[]> + getSourceConfig dispatch branch"
      contains: "discoverQoderSources"
    - path: "ingest/sync/index.ts"
      provides: "SyncSourceType extended with 'qoder' + syncQoderSource(opts) + syncSource/parseFullCandidate dispatch"
      contains: "syncQoderSource"
    - path: "tests/fixtures/qoder/build-fixture.ts"
      provides: "buildQoderFixture(outPath) — synthetic SQLite builder per D-08/D-09"
      exports: ["buildQoderFixture"]
    - path: "tests/fixtures/qoder/MANIFEST.json"
      provides: "Stable JSON manifest documenting fixture row purposes"
    - path: "tests/unit/ingest/parser/qoder.test.ts"
      provides: "Parser unit tests against the synthetic fixture"
      contains: "parseQoderSession"
  key_links:
    - from: "ingest/sync/index.ts (syncQoderSource)"
      to: "ingest/parser/qoder.ts (parseQoderSession)"
      via: "dynamic-import-on-demand or top-level import; DB opened in sync, session parsed per row"
      pattern: "parseQoderSession\\("
    - from: "ingest/sync/index.ts (syncQoderSource)"
      to: "sessions.file_hash"
      via: "buildParserCacheHash('qoder', fingerprint) → writeSessionToDatabase({source:'qoder', file_hash})"
      pattern: "buildParserCacheHash\\(.*qoder"
    - from: "ingest/parser/qoder.ts (parseQoderSession)"
      to: "TraceSubagentLink rows"
      via: "toolCallOrdinalMap reverse-lookup keyed on parent_tool_call_id (mirror codex.ts:842-852)"
      pattern: "subagentSource:\\s*'qoder'"
    - from: "ingest/api/sources.ts (discoverByType)"
      to: "discoverQoderSources"
      via: "dispatch branch on source==='qoder'"
      pattern: "discoverQoderSources\\("
---

<objective>
Wave 2 backend core: implement the Qoder data path end-to-end inside ingest. Build the **readonly SQLite reader** (`ingest/parser/qoder.ts`), **discovery** (`ingest/sync/sources.ts`), **sync engine** (`ingest/sync/index.ts`), the **`/api/v1/sources/qoder` validation+dispatch** in `ingest/api/sources.ts`, plus a hand-built **synthetic SQLite fixture** with parser unit tests proving canonical mapping for the 7 observed Qoder tools, the subagent linkage, the token-attribution rule, and the locked-DB graceful-degradation behaviour.

Purpose: this is the only plan that touches Qoder data. Privacy hardline (D-10) is enforced here — readonly DB, no credential file reads, no PRAGMA writes. After this lands, `POST /api/v1/sources/qoder/sync` indexes Qoder sessions into the canonical DB and re-running with no DB change skips every session via the per-session fingerprint.
Output: 7 files (parser, discovery, sync, API dispatch, fixture builder, manifest, parser unit test) + dispatch branches in 2 existing files.
</objective>

<execution_context>
@/Users/ebbi/Work/ai-dashboard-projects/.qwen/get-shit-done/workflows/execute-plan.md
@/Users/ebbi/Work/ai-dashboard-projects/.qwen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-qoder-source-integration/18-SPEC.md
@.planning/phases/18-qoder-source-integration/18-CONTEXT.md
@.planning/phases/18-qoder-source-integration/18-PATTERNS.md
@.planning/2026-05-17-qoder-source-integration-plan.md
@.planning/phases/17-opencode-source-integration/17-02-PLAN.md
@.planning/phases/17-opencode-source-integration/17-03-PLAN.md
@.planning/phases/18-qoder-source-integration/18-01-SUMMARY.md
@.planning/phases/18-qoder-source-integration/18-02-SUMMARY.md
@ingest/parser/codex.ts
@ingest/parser/claude.ts
@ingest/sync/index.ts
@ingest/sync/sources.ts
@types/trace.ts
</context>

<interfaces>
<!-- Canonical types (already in scope after Plan 18-02). Parser must produce these shapes. -->

types/trace.ts:
  export interface ParseResult {
    session: TraceSession;
    messages: TraceMessage[];
    activities: TraceActivity[];     // includes TraceToolCall, TraceToolResultEvent, TraceThinkingBlock, TraceSystemEvent
    subagentLinks: TraceSubagentLink[];
    errors: ParseError[];
    warnings: ParseWarning[];
  }
  export interface TraceSession { id: string; source: TraceSource; sourceSessionId: string; project: string; ...; tokenUsage?: TokenUsage; }
  export interface TraceToolCall { toolCallId: string; toolName: string; category: ToolCategory; status: 'pending'|'success'|'error'; inputJson: string; results: TraceToolResultEvent[]; ... }
  export interface TraceSubagentLink { parentSessionId: string; parentMessageOrdinal: number; subagentSessionId: string; subagentSource: TraceSource; relationship: 'spawned'|'attached'; spawnedAt?: string; }
  export interface TokenUsage { inputTokens: number; outputTokens: number; cacheReadTokens: number; cacheWriteTokens: number; reasoningTokens: number; totalTokens: number; }

ingest/sync/index.ts (current — pre-Plan 18-02; SyncSourceType widening lives in this plan):
  export type SyncSourceType = 'openclaw' | 'claude-code' | 'codex'           // → add 'qoder'
  export async function syncSource(opts: SyncSourceOptions): Promise<SyncResult>     // dispatch on sourceType
  function parseFullCandidate(...)                                                    // dispatch around line 1844
  export function computeFileHash(filePath: string): string                          // existing helper (lines 476-501)
  function buildParserCacheHash(source: string, fileHash: string): string            // existing helper
  const PARSER_CACHE_VERSION                                                          // existing version stamp (line ~20)
  function writeSessionToDatabase(parseResult, db, filePath, opts)                    // source-agnostic (lines 920-976) — no change needed

ingest/parser/codex.ts:305-951      parseCodexSession — control-flow analog
ingest/parser/codex.ts:842-852      subagent-link emission (toolCallOrdinalMap reverse-lookup)
ingest/parser/claude.ts:1300-1309   inferClaudeToolCategory — tool-name→category mapping reference

Qoder schema (from 2026-05-17-qoder-source-integration-plan.md §2.4-§2.9):
  chat_session(id, gmt_create, gmt_modified, parent_session_id, parent_tool_call_id, session_type, mode, preferred_model_info, extra, ...)
  chat_record(id, session_id, request_id, gmt_create, extra, ...)               -- extra.modelConfig.key fallback
  chat_message(id, session_id, record_id, role, content, gmt_create, model_info, token_info, tool_calls, tool_call_id, tool_result, tool_call_status, ...)
    role ∈ {'user','assistant','tool'}
    token_info JSON: { prompt_tokens, completion_tokens, cached_tokens, max_input_tokens }
    tool_calls JSON array; tool_result JSON; tool_call_status ∈ {'FINISHED','ERROR'}
    model_info JSON: { model_key, ... }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Discovery + API dispatch + Synthetic SQLite fixture builder + MANIFEST</name>
  <files>ingest/sync/sources.ts, ingest/api/sources.ts, tests/fixtures/qoder/build-fixture.ts, tests/fixtures/qoder/MANIFEST.json</files>
  <read_first>
    - ingest/sync/sources.ts (full — locate `discoverCodexSources` lines 245-276 and `getSourceConfig` dispatch lines 277-295; this task adds the Qoder analog)
    - ingest/api/sources.ts (full — locate `SOURCE_TYPES` validation array around line 22 + the `discoverByType()` dispatcher; this task adds 'qoder' to the array AND a dispatch branch)
    - .planning/2026-05-17-qoder-source-integration-plan.md §2.4-§2.9 (definitive schema for chat_session / chat_record / chat_message / token_info / tool_result)
    - .planning/phases/18-qoder-source-integration/18-PATTERNS.md Group 3 (discoverQoderSources skeleton — lines 228-274) and Group 8 (fixture-builder design from scratch — lines 696-749)
    - .planning/phases/18-qoder-source-integration/18-CONTEXT.md (D-08, D-09 — fixture coverage; D-10 — synthetic only, never sanitise real DB)
    - .planning/phases/18-qoder-source-integration/18-SPEC.md §3 (discovery acceptance: configured/empty/error states), §4 (readonly + fileMustExist), §10 (privacy hardline)
    - tests/fixtures/codex/MANIFEST.json (if it exists — copy shape; otherwise design minimal `{ sessions: [{ id, purpose }] }`)
  </read_first>
  <action>
    1) **`ingest/sync/sources.ts`** — add `discoverQoderSources(dbPaths?: string[]): Promise<DiscoveredSource[]>` immediately after `discoverCodexSources`:
       - Resolve candidate paths from argument || `loadToolDirs().get('qoder')` (uses the registry from Plan 18-02). Treat each entry as a **single SQLite file path**, not a directory.
       - For each path: skip if `!fs.existsSync(path)`; otherwise open `new Database(path, { readonly: true, fileMustExist: true })` per SPEC §4 and **never write**.
       - Validate the three expected tables exist: `SELECT name FROM sqlite_master WHERE type='table' AND name IN ('chat_session','chat_record','chat_message')`; if fewer than 3 returned, treat as `status:'error'` (not a Qoder DB) and continue.
       - On success: `SELECT COUNT(*) FROM chat_session` for `sessionCount`; push `{ type:'qoder', path, sessionCount, status:'configured' }`.
       - Wrap in try/catch with `finally { db?.close(); }`. On `SQLITE_BUSY` / locked DB or any throw: log a parser warning, push `{ type:'qoder', path, status:'error', error: msg }`, never re-throw (D-10).
    2) **`ingest/sync/sources.ts`** — extend `getSourceConfig()` dispatch (lines ~277-295) with a `'qoder'` branch returning the Qoder config object (envVar + defaultDirs + discoverFn). Pattern per PATTERNS.md Group 3.
    3) **`ingest/api/sources.ts`** — append `'qoder'` to `SOURCE_TYPES` (line ~22) and add a `'qoder'` branch to `discoverByType()` calling `discoverQoderSources()`. Ensure `/api/v1/sources/qoder` route handler returns `{ path, sessionCount, status }` per SPEC §3 acceptance.
    4) **`tests/fixtures/qoder/build-fixture.ts`** — new file. Export `buildQoderFixture(outPath: string): { sessions: SessionManifest[] }`:
       - `mkdirSync(dirname(outPath), { recursive: true })`; `new Database(outPath)` (write-mode, builder only — never run against a real Qoder DB).
       - DDL covers `chat_session`, `chat_record`, `chat_message` with the column subset enumerated in 2026-05-17-qoder-source-integration-plan.md §2.4-§2.6 (`id`, `session_id`, `record_id`, `request_id`, `role`, `content`, `gmt_create`, `gmt_modified`, `parent_session_id`, `parent_tool_call_id`, `session_type`, `mode`, `model_info` TEXT, `token_info` TEXT, `tool_calls` TEXT, `tool_call_id` TEXT, `tool_result` TEXT, `tool_call_status` TEXT, `extra` TEXT).
       - Insert rows covering D-09's nine cases:
         (a) 1 root session: `session_type='task'`, `parent_session_id=NULL`, `parent_tool_call_id=NULL`.
         (b) 1 subagent session: same shape, with `parent_session_id` pointing at root and `parent_tool_call_id` matching one of root's `Agent` tool_calls.
         (c) 7 tool messages on root, one per observed tool: `read_file`, `search_file`, `grep_code`, `search_codebase`, `list_dir`, `run_in_terminal`, `Agent` — each with `tool_call_status='FINISHED'` except where noted.
         (d) 1 tool message with `tool_call_status='ERROR'` (re-use one of the seven, e.g. `run_in_terminal`).
         (e) 1 assistant message with `token_info='{"prompt_tokens":120,"completion_tokens":80,"cached_tokens":40,"max_input_tokens":200000}'` and `model_info='{"model_key":"experts-ultimate"}'`.
         (f) 1 user message with realistic `content` JSON (use synthetic placeholder text — D-10).
         (g) 1 tool message with malformed `tool_result` JSON (e.g. `'{not valid'`) to exercise the parser-warning path.
       - Close DB.
       - CLI entry: `if (require.main === module) { buildQoderFixture(resolve('tests/fixtures/qoder/sample.db')) }`.
    5) **`tests/fixtures/qoder/MANIFEST.json`** — commit a JSON manifest documenting each session's purpose:
       `{ "sessions": [{ "id": "<root-id>", "purpose": "root with all 7 tools and 1 ERROR" }, { "id": "<subagent-id>", "purpose": "subagent of <root-id>; tests TraceSubagentLink emission" }], "messages": [{ "id": "<asst-id>", "purpose": "token_info coverage" }, ...] }`. Test files reference this MANIFEST for stable IDs.
    Pattern reference: PATTERNS.md Group 3 (discovery), Group 8 (fixture builder). Analog: 17-03-PLAN T-01 (discoverOpencodeSources). Privacy hardline: SPEC §10, D-10 — `fs.existsSync` does NOT touch any auth/cookie/state.vscdb path; no string literal `machine_token.json` / `supabase_token` / `secret://` may appear in this task's diff.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm tsx tests/fixtures/qoder/build-fixture.ts && test -f tests/fixtures/qoder/sample.db</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "discoverQoderSources" ingest/sync/sources.ts` ≥ 2 (definition + getSourceConfig branch).
    - `grep -E "readonly:\s*true.*fileMustExist:\s*true" ingest/sync/sources.ts` returns ≥ 1 match (privacy hardline at discovery point).
    - `grep -E "machine_token|supabase_token|secret://" ingest/sync/sources.ts ingest/api/sources.ts tests/fixtures/qoder/build-fixture.ts` returns 0 matches (D-10 static check).
    - `grep -c "'qoder'" ingest/api/sources.ts` ≥ 2 (validation array + dispatch branch).
    - `pnpm tsx tests/fixtures/qoder/build-fixture.ts` exits 0 and creates `tests/fixtures/qoder/sample.db`.
    - `sqlite3 tests/fixtures/qoder/sample.db "SELECT COUNT(*) FROM chat_session"` returns ≥ 2 (root + subagent).
    - `sqlite3 tests/fixtures/qoder/sample.db "SELECT COUNT(*) FROM chat_message WHERE role='tool'"` returns ≥ 7.
    - `sqlite3 tests/fixtures/qoder/sample.db "SELECT COUNT(*) FROM chat_message WHERE tool_call_status='ERROR'"` returns ≥ 1.
    - `tests/fixtures/qoder/MANIFEST.json` parses as JSON and references at least the root and subagent session IDs.
    - `curl http://localhost:8078/api/v1/sources/qoder` (manual, optional) returns a JSON object whose `status` is one of `configured` / `empty` / `error` per SPEC §3.
  </acceptance_criteria>
  <done>Discovery returns a usable status without crashing on absent/malformed DBs; the synthetic fixture is reproducible and exercises every scenario the parser test will assert.</done>
</task>

<task type="auto">
  <name>Task 2: Implement readonly Qoder parser (parseQoderSession + tool-category mapping + token attribution + subagent links + fingerprint helper)</name>
  <files>ingest/parser/qoder.ts</files>
  <read_first>
    - .planning/2026-05-17-qoder-source-integration-plan.md §2.6, §2.7, §2.8, §2.9, §3.3, §3.4 (canonical mapping spec)
    - .planning/phases/18-qoder-source-integration/18-SPEC.md §4 (parser contract), §7 (tool category map), §8 (token rule), §10 (privacy hardline)
    - .planning/phases/18-qoder-source-integration/18-CONTEXT.md (D-03 fingerprint, D-06 part 1 subagent persist, D-07 child session row, D-10 readonly + no credentials)
    - .planning/phases/18-qoder-source-integration/18-PATTERNS.md Group 4 (parser skeleton lines 278-378) — Pattern F (privacy), Pattern G (tokens)
    - ingest/parser/codex.ts:305-951 (parseCodexSession control flow)
    - ingest/parser/codex.ts:842-852 (subagent-link emission with toolCallOrdinalMap reverse-lookup — KEY pattern)
    - ingest/parser/claude.ts:1300-1309 (inferClaudeToolCategory mapping reference)
    - types/trace.ts (full ParseResult / TraceSession / TraceToolCall / TraceSubagentLink / TokenUsage shapes)
    - tests/fixtures/qoder/MANIFEST.json (after Task 1 — reference IDs for testing)
  </read_first>
  <action>
    Create `ingest/parser/qoder.ts`. Organisation per Claude's Discretion: a single file is acceptable; if it grows past ~600 lines split into `qoder/reader.ts` + `qoder/mapper.ts` + `qoder/normalizer.ts` and re-export from `qoder.ts`. Required exports and behaviour:

    1) **`export async function parseQoderSession(dbPath: string, rawSessionId: string, options: ParseOptions = {}): Promise<ParseResult>`**
       - Open `new Database(dbPath, { readonly: true, fileMustExist: true })` ONCE for the entire body. Wrap entire body in `try { ... } finally { db.close() }`. **Reuse this single `db` handle for every query in this function — including the parent ordinal reverse-lookup below.** Per SPEC §10 / D-10: NO `BEGIN`, `INSERT`, `UPDATE`, `DELETE`, `CREATE`, `ALTER`, `PRAGMA writable_schema`, or any other write statement against this DB.
       - Load session row: `SELECT * FROM chat_session WHERE id = ?`. If no row → return ParseResult with `errors: [{kind:'session_not_found', ...}]` and empty arrays (do not throw).
       - Load records: `SELECT * FROM chat_record WHERE session_id = ? ORDER BY gmt_create, request_id`.
       - Load messages: `SELECT * FROM chat_message WHERE session_id = ? ORDER BY gmt_create, id`.
       - Build TraceSession: `id = 'qoder:' + rawSessionId`, `sourceSessionId = rawSessionId`, `source = 'qoder'`, project from `chat_session.extra` if present (narrow keys only — DO NOT read `extra.firstTurnRulesPrompt` per SPEC §10 / Constraints), `name` from session title heuristic, `relationshipType = parent_session_id ? 'subagent' : 'root'` (note: `TraceSession.relationshipType` is an INDEPENDENT field — NOT the same as `TraceSubagentLink.relationship` emitted below; the link relationship is always `'spawned'` per schema CHECK), `parentSessionId = parent_session_id ? 'qoder:' + parent_session_id : undefined`.
       - Iterate messages in order; maintain `messageOrdinal` counter and a `Map<string, number>` `toolCallOrdinalMap` keyed on each tool call's `tool_call_id` → `messageOrdinal`. Mirror codex.ts:842-852 pattern.
       - For each message dispatch by `role`:
         - `role='user'` → emit `TraceMessage{ role:'user', content: message.content, ordinal: messageOrdinal++ }`.
         - `role='assistant'` → JSON.parse `message.tool_calls` (catch malformed → push warning, treat as empty). For each tool_call, emit `TraceToolCall` with `toolCallId = tc.toolCallId`, `toolName = tc.name`, `category = inferQoderToolCategory(tc.name)`, `status = 'pending'` (filled by matching tool message later), `inputJson = JSON.stringify(tc.parameters)`, and store `toolCallOrdinalMap.set(tc.toolCallId, messageOrdinal)`. JSON.parse `message.token_info` per Pattern G: `inputTokens = prompt_tokens, outputTokens = completion_tokens, cacheReadTokens = cached_tokens, cacheWriteTokens = 0, reasoningTokens = 0, totalTokens = prompt_tokens + completion_tokens` — **never add `cached_tokens` to total** (SPEC §8 acceptance). Store `max_input_tokens` only as session-level metadata (TraceSession metadata blob), not in TokenUsage. Build model display per fallback chain SPEC §9: `message.model_info.model_key` → `record.extra.modelConfig.key` → `chat_session.preferred_model_info` → `'unknown'`. Emit `TraceMessage{ role:'assistant', content, tokenUsage, model, ordinal }`.
         - `role='tool'` → JSON.parse `tool_result` (catch malformed → push parser warning, skip the result body but emit a `TraceToolResultEvent` with empty results to keep the call-result pairing). Look up the matching `TraceToolCall` by `tool_call_id` and update its `status`: `tool_call_status === 'ERROR'` → `'error'` and surface `tool_result.errorMsg` (or fall back to a synthesized message from `tool_result.error`); otherwise `'success'`. Append a `TraceToolResultEvent{ toolCallId, results: <parsed body>, status }` to the call's `results[]`. Emit `TraceMessage{ role:'tool_result', ordinal: messageOrdinal++ }`.
       - After iterating, if `chat_session.parent_session_id` is set, emit one `TraceSubagentLink` row (D-06 part 1 — write path):
         - `parentSessionId = 'qoder:' + parent_session_id`
         - `parentMessageOrdinal = lookupParentOrdinal(parent_tool_call_id)` — REUSE the already-open `db` handle (do NOT open a second `Database` connection). Prepare a separate statement scoped to this lookup, e.g.: `const stmt = db.prepare("SELECT id, gmt_create FROM chat_message WHERE session_id = ? AND tool_call_id = ? ORDER BY gmt_create, id LIMIT 1")`. To compute the ordinal, prepare a second statement that returns the parent's full message ordering keyed by the same ORDER BY used during parse (`SELECT id FROM chat_message WHERE session_id = ? ORDER BY gmt_create, id`) and find the matching index. Both statements run against the same single `db` opened at the top of `parseQoderSession`. If the parent session is not present in this DB, fall back to `parentMessageOrdinal = 0` and push a warning.
         - `subagentSessionId = 'qoder:' + rawSessionId`
         - `subagentSource = 'qoder'`
         - `relationship = 'spawned'` (matches existing CHECK literal at schema.sql:173).
         - `spawnedAt = chat_session.gmt_create` ISO-formatted.
       - Aggregate session token totals from per-message TokenUsage: `total_input_tokens = Σ inputTokens, total_output_tokens = Σ outputTokens, total_cache_read_tokens = Σ cacheReadTokens, total_tokens = Σ inputTokens + Σ outputTokens` — assert the totalTokens identity does NOT include cache_read.
       - Set `pricingStatus = 'unknown'` and `cost = null` on the session per SPEC §9 (cost rendering treats this as `—`). Do NOT call into `ingest/pricing/model-pricing.ts` for qoder.
       - Return `{ session, messages, activities, subagentLinks, errors, warnings }`.
       - On `SQLITE_BUSY` at any prepare/all() call: retry up to 3× with 100ms delay (Phase 17 D-03 default per Claude's Discretion); after exhaustion push a `ParseWarning{ kind:'sqlite_busy', sessionId: rawSessionId, ... }` and return the partially-built ParseResult (D-10: never crash, preserve prior canonical rows).

    2) **`export function inferQoderToolCategory(toolName: string): ToolCategory`**
       - Map per SPEC §7: `read_file` → `'Read'`, `list_dir` → `'Read'`, `search_file` → `'Grep'`, `grep_code` → `'Grep'`, `search_codebase` → `'Grep'`, `run_in_terminal` → `'Bash'`, `Agent` → `'Agent'`. Default `'Other'`.
       - Recommended approach (PATTERNS.md Group 4 note): if `inferClaudeToolCategory` is exported from `claude.ts`, reuse via shared util; otherwise duplicate the mapping here under the qoder-specific name. Both options are acceptable per Claude's Discretion.

    3) **`export function computeQoderSessionFingerprint(row: { id: string; gmt_modified: number; msg_count: number; max_msg_gmt: number }): string`**
       - Returns `crypto.createHash('sha256').update(\`qoder-session-v1:${row.id}:${row.gmt_modified}:${row.msg_count}:${row.max_msg_gmt}\`).digest('hex')` per D-03. Used by syncQoderSource in Task 3.

    4) **Privacy hardline static guard**: in this file the strings `machine_token.json`, `supabase_token`, `secret://`, `state.vscdb`, and `auth.json` MUST NOT appear. The parser MUST NOT open any path other than the `dbPath` argument. The parser MUST NOT call `fs.readFile` / `fs.readFileSync` / `fs.openSync` against any other path. **Critically: only one `new Database(...)` call may exist for the duration of one parseQoderSession invocation** — `grep -c "new Database\(" ingest/parser/qoder.ts` must equal 1.

    Pattern reference: PATTERNS.md Group 4 (skeleton, subagent-link, tool-category mapping, token rule). Analogs: codex.ts:305-951 control flow, codex.ts:842-852 subagent emission, claude.ts:1300 category mapping.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm test:run -- ingest/parser 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "new Database\(.*readonly:\s*true.*fileMustExist:\s*true" ingest/parser/qoder.ts` returns ≥ 1 match (D-10).
    - `grep -c "new Database\(" ingest/parser/qoder.ts` == 1 (single handle reuse — efficiency fix per checker info-1).
    - `grep -E "machine_token|supabase_token|secret://|state\.vscdb|auth\.json" ingest/parser/qoder.ts` returns 0 matches (privacy hardline static check — SPEC §10 acceptance).
    - `grep -E "INSERT|UPDATE|DELETE|CREATE\s+TABLE|ALTER|PRAGMA\s+(writable_schema|journal_mode|synchronous)|BEGIN" ingest/parser/qoder.ts` returns 0 matches against the Qoder DB (only allowed in fixture builder, not parser).
    - `grep -c "qoder-session-v1:" ingest/parser/qoder.ts` ≥ 1 (fingerprint formula).
    - `grep -c "subagentSource:\s*'qoder'" ingest/parser/qoder.ts` ≥ 1 (subagent linking).
    - `grep -c "totalTokens" ingest/parser/qoder.ts` ≥ 1; the surrounding line is `prompt_tokens + completion_tokens` (NOT `+ cached_tokens`). Manual review point: verify with `grep -B1 -A2 "totalTokens" ingest/parser/qoder.ts`.
    - `grep -E "pricingStatus.*unknown|cost.*null" ingest/parser/qoder.ts` returns ≥ 1 match (cost exclusion).
    - `pnpm exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>parseQoderSession returns a valid ParseResult shape for an arbitrary fixture session; privacy/readonly invariants are statically verifiable; tool-category map covers all 7 observed Qoder tools; subagent link emission mirrors codex.ts:842 exactly with `subagentSource:'qoder'`; only one DB handle is opened per invocation.</done>
</task>

<task type="auto">
  <name>Task 3: Sync engine integration (SyncSourceType + syncQoderSource + dispatch) + parser unit tests against the fixture</name>
  <files>ingest/sync/index.ts, tests/unit/ingest/parser/qoder.test.ts</files>
  <read_first>
    - ingest/sync/index.ts:1-100 (locate `PARSER_CACHE_VERSION` at line ~20 and `SyncSourceType` at line 63)
    - ingest/sync/index.ts:476-501 (`computeFileHash` + `buildParserCacheHash` helpers — reused as-is)
    - ingest/sync/index.ts:920-976 (`writeSessionToDatabase` — already source-agnostic, no changes needed)
    - ingest/sync/index.ts:1710-1727 (syncSource dispatcher) and ~1844 (parseFullCandidate dispatcher)
    - ingest/sync/index.ts:2186-2244 (syncOpenClawSource analog — discovery + iterate + skip-cache + writeSessionToDatabase pattern)
    - ingest/sync/index.ts:2289-2430 (syncCodexSource analog — closest control-flow match)
    - ingest/parser/qoder.ts (created in Task 2 — imports parseQoderSession + computeQoderSessionFingerprint)
    - .planning/phases/18-qoder-source-integration/18-CONTEXT.md (D-03 file_hash reuse, D-04 own-source NULL flush — already done in 18-01)
    - .planning/phases/18-qoder-source-integration/18-PATTERNS.md Group 5 (syncQoderSource skeleton lines 432-470)
    - .planning/phases/17-opencode-source-integration/17-03-PLAN.md (sync wire-up analog T-02, T-03)
    - tests/fixtures/qoder/MANIFEST.json (stable IDs)
    - .planning/phases/18-qoder-source-integration/18-SPEC.md §5 acceptance criteria (sync, fingerprint skip, append-one-message re-index)
  </read_first>
  <action>
    A) **`ingest/sync/index.ts` — type + dispatch + new function:**

    1) Line 63: extend `SyncSourceType` union with `| 'qoder'`. Update the doc comment at line 1706 if present.
    2) `syncSource()` dispatcher (~line 1710): add the branch `else if (sourceType === 'qoder') { result = await syncQoderSource(opts); }` BEFORE the final `else throw`.
    3) `parseFullCandidate()` dispatcher (~line 1844): add a `'qoder'` branch that calls `parseQoderSession(candidate.path, candidate.sessionId, opts)` (Qoder candidates are session-keyed, not file-keyed — `candidate.path` is the dbPath, `candidate.sessionId` is the raw id).
    4) Add `async function syncQoderSource(opts: SyncSourceOptions): Promise<SyncResult>` immediately after `syncCodexSource`. Implementation per PATTERNS.md Group 5:
       - `const { discoverQoderSources } = await import('./sources')`
       - `const sources = await discoverQoderSources(loadToolDirs().get('qoder'))`. Skip sources whose `status !== 'configured'`.
       - For each configured source open `new Database(source.path, { readonly: true, fileMustExist: true })` in a `try/finally { db.close() }`.
       - Enumerate session ids ordered by `gmt_modified DESC`:
         ```sql
         SELECT id, gmt_modified,
                (SELECT COUNT(*) FROM chat_message m WHERE m.session_id = chat_session.id) AS msg_count,
                (SELECT MAX(gmt_create) FROM chat_message m WHERE m.session_id = chat_session.id) AS max_msg_gmt
         FROM chat_session ORDER BY gmt_modified DESC
         ```
       - For each row:
         - Compute `fingerprint = computeQoderSessionFingerprint({ id: row.id, gmt_modified: row.gmt_modified, msg_count: row.msg_count, max_msg_gmt: row.max_msg_gmt })`.
         - `cacheKey = buildParserCacheHash('qoder', fingerprint)`.
         - Read existing canonical row: `SELECT file_hash FROM sessions WHERE id = ?` keyed on `'qoder:' + row.id`. If `existing?.file_hash === cacheKey` AND `opts.force !== true` → `skipped++` and continue (D-03 fingerprint skip; SPEC §5 acceptance).
         - Else parse: `const parsed = await parseQoderSession(source.path, row.id, opts)`.
         - Write through canonical pipeline: `await writeSessionToDatabase(parsed, db_canonical, source.path + '#' + row.id, { force: opts.force, fileHash: cacheKey })`. The `writeSessionToDatabase` signature is source-agnostic (lines 920-976) — confirm by reading and adapt the file_hash override path; if no override exists, persist `cacheKey` into `sessions.file_hash` via a follow-on UPDATE within the same transaction so the next sync can compare.
         - Record per-row stats; on parser warnings/errors, increment `errors`/`warnings` counters but never re-throw (D-10).
       - On `SQLITE_BUSY` at the enumerate query: retry 3× × 100ms (Phase 17 D-03 default); on exhaustion, log a structured warning and return the partial SyncResult (do not crash).
       - Return `{ synced, skipped, errors, warnings }`.
    5) **Do NOT** bump `PARSER_CACHE_VERSION` (currently `'parser-v9-token-channel-accounting'`). Per PATTERNS.md Group 5: bump only if Qoder introduces a token-attribution change retroactively affecting the other 3 sources, which it does not (SPEC §8 matches v9).

    B) **`tests/unit/ingest/parser/qoder.test.ts` — unit tests using the synthetic fixture:**

    1) `beforeAll`: invoke `buildQoderFixture` (from `tests/fixtures/qoder/build-fixture.ts`) into a tmpdir; load MANIFEST.json for stable IDs.
    2) Tests (one `it` block each — keep small and focused):
       - **canonical session id prefix**: `parseQoderSession(dbPath, rootId)`'s session.id === `'qoder:' + rootId`; `sourceSessionId === rootId`.
       - **all 7 tool categories map correctly**: walk the root session's TraceToolCall list; assert `categoryByToolName === { read_file:'Read', search_file:'Grep', grep_code:'Grep', search_codebase:'Grep', list_dir:'Read', run_in_terminal:'Bash', Agent:'Agent' }`.
       - **ERROR tool surfaces status='error' and errorMsg**: locate the message inserted with `tool_call_status='ERROR'`; assert the matched TraceToolCall.status === 'error' and the TraceToolResultEvent has a non-empty errorMsg.
       - **token attribution rule (SPEC §8 acceptance)**: assistant message totals `inputTokens=120, outputTokens=80, cacheReadTokens=40, totalTokens=200` (NOT 240 — explicitly assert that adding cache_read would have produced a different value). Session totals match the per-message sums.
       - **subagent link**: parse the subagent session; assert `session.relationshipType === 'subagent'` and `session.parentSessionId === 'qoder:' + rootId`. Also parse the root session; assert exactly one `TraceSubagentLink` was emitted with `subagentSource:'qoder'`, `relationship:'spawned'`, and `parentMessageOrdinal` matching the ordinal of the message containing the `Agent` tool call whose `toolCallId === parent_tool_call_id`.
       - **malformed JSON warning**: the message with malformed `tool_result` produces a `ParseWarning` (kind matches `/json|parse|malformed/i`) and the session still parses successfully (errors empty for that case).
       - **fingerprint determinism**: `computeQoderSessionFingerprint` returns identical sha256 hex for identical inputs; differs when any of the 4 input fields changes.
       - **privacy static check**: at runtime, `fs.readFile` / `fs.readFileSync` is spied; running parseQoderSession against the fixture must invoke neither against any path other than `dbPath`. Equivalently: `expect(parsedDbPaths).toEqual([dbPath])`.
    Pattern reference: PATTERNS.md Group 5 (sync) + Group 8 (test scaffold). Analog: 17-03-PLAN T-02 + 17-02-PLAN T-03.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm test:run -- tests/unit/ingest/parser/qoder.test.ts && pnpm test:run -- ingest/sync 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "SyncSourceType\s*=.*'qoder'" ingest/sync/index.ts` returns 1 match.
    - `grep -c "syncQoderSource" ingest/sync/index.ts` ≥ 3 (definition + syncSource branch + parseFullCandidate branch or wiring).
    - `grep -E "buildParserCacheHash\(\s*'qoder'" ingest/sync/index.ts` returns ≥ 1 match (D-03 cache key reuses sessions.file_hash with the qoder source prefix).
    - `pnpm test:run -- tests/unit/ingest/parser/qoder.test.ts` exits 0.
    - The token-attribution test asserts `totalTokens === 200` and explicitly NOT 240 (no cached double-count) — required acceptance per SPEC §8.
    - The subagent test asserts a TraceSubagentLink with `subagentSource:'qoder'` and `relationship:'spawned'`.
    - End-to-end smoke (manual or scripted): `POST /api/v1/sources/qoder/sync` against a DB pointing at the fixture writes rows; re-running with no DB change returns `skipped == sessions_count` and `synced == 0`.
  </acceptance_criteria>
  <done>Sync engine fully wires Qoder; the fingerprint skip cache works (re-run with no change skips every session); locked DB does not crash ingest; parser unit tests prove canonical mapping + token rule + subagent linkage + privacy invariant.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` exits 0.
- `pnpm test:run -- ingest/parser/qoder` and `pnpm test:run -- ingest/sync` exit 0.
- `pnpm test:run -- ingest/db/migration` (re-run from Plan 18-01) still exits 0.
- Static privacy guard: `grep -RE "machine_token|supabase_token|secret://|state\.vscdb|auth\.json" ingest/parser/qoder.ts ingest/sync/sources.ts ingest/sync/index.ts` returns 0 matches inside any code introduced by this plan (use git diff scope: `git diff main -- ingest/parser/qoder.ts ingest/sync/sources.ts ingest/sync/index.ts | grep -E "..."`).
- Static write-pragma guard: `grep -E "PRAGMA\s+(writable_schema|journal_mode|synchronous)|BEGIN\s+TRANSACTION|INSERT\s+INTO\s+chat_|UPDATE\s+chat_|DELETE\s+FROM\s+chat_" ingest/parser/qoder.ts ingest/sync/sources.ts` returns 0 matches.
- Smoke: with `QODER_DB_PATH` pointed at the fixture, `POST /api/v1/sources/qoder/sync` returns success; canonical DB has rows for `source='qoder'`; re-running skips all sessions; appending one message to one session in the fixture (rebuild via `buildQoderFixture` with new gmt_create) and re-syncing re-indexes only that session.
</verification>

<success_criteria>
1. **QDR-102 (discovery):** `/api/v1/sources/qoder` returns `configured/empty/error` per SPEC §3.
2. **QDR-103 (parser):** parseQoderSession produces canonical ParseResult with `qoder:` prefix; readonly DB; never reads credentials.
3. **QDR-106 (activity rows):** all 7 observed Qoder tools map to canonical categories; ERROR tool surfaces error status + errorMsg.
4. **QDR-107 (subagent link write path):** parent_session_id + parent_tool_call_id produce a TraceSubagentLink with reverse-lookup ordinal — D-06 part 1.
5. **QDR-108 (token attribution):** prompt+completion only; cached_tokens NOT in total; max_input_tokens metadata only.
6. **QDR-110 (privacy + safety + fingerprint skip):** readonly DB; no credential paths; per-session fingerprint stored via sessions.file_hash; locked DB graceful; sync re-run skips all when nothing changed.
7. Existing OpenClaw / Claude Code / Codex / OpenCode parser/sync tests still pass.
</success_criteria>

<output>
Create `.planning/phases/18-qoder-source-integration/18-03-SUMMARY.md` recording: parser entry-point signature, fingerprint formula committed, list of grep-verified privacy invariants, count of fixture rows, and the per-test pass/fail summary from `pnpm test:run -- tests/unit/ingest/parser/qoder.test.ts`.
</output>
