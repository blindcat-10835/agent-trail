---
phase: 18-qoder-source-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - types/trace.ts
  - lib/agent-tools/types.ts
  - lib/agent-tools/types.test.ts
  - ingest/api/sessions.ts
  - ingest/api/overview.ts
  - ingest/api/agents.ts
  - ingest/index.ts
  - app/api/sync/route.ts
  - ingest/config/tool-dirs.ts
  - ingest/config/capabilities.ts
autonomous: true
requirements:
  - QDR-101
  - QDR-102
must_haves:
  truths:
    - "TypeScript treats 'qoder' as a valid TraceSource and SourceToolId throughout the codebase (D-01; QDR-101)."
    - "Every validation array / whitelist that currently lists ('openclaw','claude-code','codex'[,'opencode']) now also accepts 'qoder' (QDR-101)."
    - "TOOL_DIR_REGISTRY exposes QODER_DB_PATH env var, qoder_db_paths config key, and the macOS default `~/Library/Application Support/Qoder/SharedClientCache/cache/db/local.db` (QDR-102)."
    - "SOURCE_CAPABILITIES.qoder declares sessions/replay/activity true and cost/agents/automations false — Qoder is excluded from cost rollups at the capability layer (QDR-101 + cost-exclusion contract)."
    - "Existing OpenClaw / Claude Code / Codex / OpenCode tests still pass without modification."
  artifacts:
    - path: "types/trace.ts"
      provides: "TraceSource union including 'qoder'"
      contains: "'qoder'"
    - path: "lib/agent-tools/types.ts"
      provides: "SourceToolId union including 'qoder'"
      contains: "'qoder'"
    - path: "ingest/config/tool-dirs.ts"
      provides: "TOOL_DIR_REGISTRY entry: { type:'qoder', envVar:'QODER_DB_PATH', configKey:'qoder_db_paths', defaultDirs:['Library/Application Support/Qoder/SharedClientCache/cache/db/local.db'] }"
      contains: "QODER_DB_PATH"
    - path: "ingest/config/capabilities.ts"
      provides: "SOURCE_CAPABILITIES.qoder entry"
      contains: "qoder:"
  key_links:
    - from: "TraceSource union"
      to: "TraceSubagentLink.subagentSource"
      via: "structural typing — TSL.subagentSource is typed as TraceSource so widening propagates automatically"
      pattern: "subagentSource:\\s*TraceSource"
    - from: "lib/agent-tools/types.ts SourceToolId"
      to: "AgentToolId = 'all' | SourceToolId"
      via: "type alias propagation"
      pattern: "AgentToolId\\s*=\\s*'all'\\s*\\|\\s*SourceToolId"
---

<objective>
Wave 1 type-system + validation widening: thread the literal `'qoder'` through every TypeScript union, runtime whitelist, startup array, BFF validation list, and the two `ingest/config/*` registries (tool-dirs and capabilities). This is the mechanical "Pattern A" sweep — no new logic, no behaviour change, but TypeScript compilation must keep passing afterwards. After this lands, downstream plans (18-03 parser/sync, 18-04 BFF/UI) can reference `'qoder'` everywhere without compile errors.

Purpose: lock the source-identity contract before the parser, sync engine, and BFF adapter touch any `source` field. Provides the env var name (`QODER_DB_PATH`) and config key (`qoder_db_paths`) that 18-03's discovery code consumes (QDR-102).
Output: 10 files updated with single-line literal additions plus one tool-dirs registry entry and one capabilities entry; types tests assert the new array shapes.
</objective>

<execution_context>
@/Users/ebbi/Work/ai-dashboard-projects/.qwen/get-shit-done/workflows/execute-plan.md
@/Users/ebbi/Work/ai-dashboard-projects/.qwen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-qoder-source-integration/18-SPEC.md
@.planning/phases/18-qoder-source-integration/18-CONTEXT.md
@.planning/phases/18-qoder-source-integration/18-PATTERNS.md
@.planning/phases/17-opencode-source-integration/17-01-PLAN.md
@types/trace.ts
@lib/agent-tools/types.ts
@lib/agent-tools/types.test.ts
@ingest/config/tool-dirs.ts
@ingest/config/capabilities.ts
</context>

<interfaces>
<!-- Source of truth for the type system. After this plan, every reference to TraceSource and SourceToolId implicitly accepts 'qoder'. -->

types/trace.ts:22                  export type TraceSource = 'openclaw' | 'claude-code' | 'codex'   // → add 'qoder'
types/trace.ts:~301-310            interface TraceSubagentLink { subagentSource: TraceSource; ... } // propagates automatically
lib/agent-tools/types.ts:18        export type SourceToolId = 'openclaw' | 'claude-code' | 'codex'   // → add 'qoder'
lib/agent-tools/types.ts:~30       export type AgentToolId  = 'all' | SourceToolId                  // propagates automatically

ingest/api/sessions.ts:17/36       const VALID_SOURCES (or SOURCE whitelist) — validation gate for query params
ingest/api/overview.ts:27          const VALID_SOURCES                                                — validation gate
ingest/api/agents.ts               source whitelist                                                  — validation gate
ingest/index.ts:76, :267           startup `sourceTypes` array                                        — controls discovery + scheduler

app/api/sync/route.ts:4            BFF route SOURCE_TYPES whitelist                                   — validates POST /api/sync body

ingest/config/tool-dirs.ts         TOOL_DIR_REGISTRY: Array<{ type, displayName, envVar, configKey, defaultDirs }>
ingest/config/capabilities.ts      SOURCE_CAPABILITIES: Record<TraceSource, { agents, automations, cost, activity, sessions, replay }>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Widen TraceSource and SourceToolId unions + types tests</name>
  <files>types/trace.ts, lib/agent-tools/types.ts, lib/agent-tools/types.test.ts</files>
  <read_first>
    - types/trace.ts (full — TraceSource at ~line 22, TraceSubagentLink.subagentSource at ~lines 301-310 propagates automatically)
    - lib/agent-tools/types.ts (full — SourceToolId at line 18, AgentToolId at line ~30, doc-comment around line 214 referencing the source list)
    - lib/agent-tools/types.test.ts (full — lines 20, 29, 33, 192, 199 host the literal arrays that must be updated)
    - .planning/phases/18-qoder-source-integration/18-PATTERNS.md Group 1 (Pattern A walkthrough) and Group 8 (test updates)
    - .planning/phases/17-opencode-source-integration/17-01-PLAN.md T-01 (analog mechanical widening)
  </read_first>
  <action>
    1) `types/trace.ts:22` — extend `TraceSource` from its current union to append `| 'qoder'` (alphabetised after the last existing literal, NOT alphabetically sorted — preserve chronological-introduction order per PATTERNS.md Pattern A).
    2) `lib/agent-tools/types.ts:18` — extend `SourceToolId` to append `| 'qoder'`. Update the doc-comment around line 214 from "openclaw, claude-code, codex" (or 4-source if Phase 17 has landed) to add `, qoder`.
    3) `lib/agent-tools/types.test.ts` — update every literal array assertion (lines 20, 29, 33, 192, 199 per PATTERNS.md, plus any other `['openclaw',...]` literal the file contains) to append `'qoder'` in the matching position. Examples:
       - `const ids: AgentToolId[] = ['all','openclaw','claude-code','codex','qoder']` (or 6 entries with opencode if Phase 17 landed).
       - `expect(SHELL_TOOL_IDS).toContain('qoder')` add a positive assertion that doesn't depend on TOOL_IDS being populated by Plan 18-04 (since that import is delayed).
    4) Do NOT modify `lib/agent-tools/registry.ts` here — that file imports `qoderDef` which is created in Plan 18-04. Its `TOOL_IDS` widening lives in 18-04.
    5) Do NOT modify `ingest/sync/index.ts:63` (`SyncSourceType`) — that lives in Plan 18-03 because the sync dispatcher and `syncQoderSource` function in the same file are also touched there (file-overlap rule).
    Pattern reference: PATTERNS.md Group 1 (Pattern A), Group 8.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm test:run -- lib/agent-tools/types.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'qoder'" types/trace.ts` ≥ 1.
    - `grep -E "TraceSource\s*=.*'qoder'" types/trace.ts` returns 1 match.
    - `grep -E "SourceToolId\s*=.*'qoder'" lib/agent-tools/types.ts` returns 1 match.
    - `grep -c "'qoder'" lib/agent-tools/types.test.ts` ≥ 4 (matches the 5 array sites called out in PATTERNS.md, minus any that don't list literal sources).
    - `pnpm exec tsc --noEmit` exits 0.
    - `pnpm test:run -- lib/agent-tools/types.test.ts` exits 0.
  </acceptance_criteria>
  <done>TypeScript accepts `'qoder'` as a TraceSource and SourceToolId everywhere; the types-suite passes including the new literal-array assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Add 'qoder' to runtime validation arrays across ingest/api + ingest/index + BFF sync route</name>
  <files>ingest/api/sessions.ts, ingest/api/overview.ts, ingest/api/agents.ts, ingest/index.ts, app/api/sync/route.ts</files>
  <read_first>
    - ingest/api/sessions.ts (full — locate the `VALID_SOURCES` / source whitelist around line 17 and the request validator that consumes it)
    - ingest/api/overview.ts (full — `VALID_SOURCES` at line 27; cost rollup queries are NOT touched here, that lives in 18-04)
    - ingest/api/agents.ts (full — source whitelist around line 44 per 17-01-PLAN T-03)
    - ingest/index.ts (full — startup arrays at lines 76 and 267 per PATTERNS.md and 17-01-PLAN T-03)
    - app/api/sync/route.ts (full — `SOURCE_TYPES` array at line 4)
    - .planning/phases/18-qoder-source-integration/18-PATTERNS.md Group 1 (the six-file pattern in this exact group)
  </read_first>
  <action>
    For each file below, append `'qoder'` to every `as const` literal array or `Set` initialiser whose current contents match the four-source-or-fewer pattern. The diff per file is one literal addition. Do NOT introduce any new logic; type-narrowing helpers (`isValidSource`, `assertSourceToolId`) pick up the new literal automatically through the unions widened in Task 1.

    1) `ingest/api/sessions.ts:17` (or wherever the source whitelist lives — verify with `grep -n "openclaw" ingest/api/sessions.ts`) — append `'qoder'`.
    2) `ingest/api/overview.ts:27` `VALID_SOURCES` — append `'qoder'`. (Cost-exclusion query patches happen in Plan 18-04; here only the validation gate widens.)
    3) `ingest/api/agents.ts` source whitelist — append `'qoder'`.
    4) `ingest/index.ts:76` and `:267` — append `'qoder'` to the startup `sourceTypes` array (both sites).
    5) `app/api/sync/route.ts:4` `SOURCE_TYPES` — append `'qoder'`.
    Pattern reference: PATTERNS.md Group 1; analog 17-01-PLAN T-03.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "'qoder'" ingest/api/sessions.ts ingest/api/overview.ts ingest/api/agents.ts ingest/index.ts app/api/sync/route.ts | wc -l` returns ≥ 6 (one per file, with ingest/index.ts contributing 2).
    - `pnpm exec tsc --noEmit` exits 0.
    - No file in this list has been modified beyond a single-literal addition (no new functions, no new imports).
    - Existing `pnpm test:run -- ingest/api` suites continue to pass.
  </acceptance_criteria>
  <done>Every runtime validation gate accepts `source=qoder` as a legal value; the BFF sync route accepts `qoder` in its body whitelist.</done>
</task>

<task type="auto">
  <name>Task 3: Register Qoder in TOOL_DIR_REGISTRY and SOURCE_CAPABILITIES (config layer for QDR-102 / cost-exclusion)</name>
  <files>ingest/config/tool-dirs.ts, ingest/config/capabilities.ts</files>
  <read_first>
    - ingest/config/tool-dirs.ts (full — locate the existing `codex` entry and the `home`-prefix join loop around line 89; verify that the loop tolerates entries whose `defaultDirs[0]` is a single file path rather than a directory)
    - ingest/config/capabilities.ts (full — locate the existing `codex` entry in `SOURCE_CAPABILITIES`)
    - .planning/phases/18-qoder-source-integration/18-PATTERNS.md Group 3 (Discovery / Config — exact analog block)
    - .planning/phases/18-qoder-source-integration/18-SPEC.md §3 (env var + config key + default path) and §6 (capability flags) and §9 (cost exclusion)
    - .planning/phases/18-qoder-source-integration/18-CONTEXT.md (D-08 — env var name) and `<code_context>` listing the capability flags
  </read_first>
  <action>
    1) In `ingest/config/tool-dirs.ts`, append a new `TOOL_DIR_REGISTRY` entry immediately after the `codex` entry (or after `opencode` if Phase 17 has landed):
       - `type: 'qoder'`
       - `displayName: 'Qoder'`
       - `envVar: 'QODER_DB_PATH'`
       - `configKey: 'qoder_db_paths'`
       - `defaultDirs: ['Library/Application Support/Qoder/SharedClientCache/cache/db/local.db']`
       Note: the entry's "dir" is actually a single SQLite file path. Before committing, read line 89's `home`-prefix join loop and confirm it does not append a trailing slash that would corrupt the file path. If the loop unconditionally appends `/`, surface the issue to the discovery consumer rather than altering the loop here — Plan 18-03's `discoverQoderSources` will normalise.
    2) In `ingest/config/capabilities.ts`, append a `SOURCE_CAPABILITIES.qoder` entry:
       - `agents: false, automations: false, cost: false, activity: true, sessions: true, replay: true`
       (`subagents: true` is exposed via `lib/agent-tools/qoder/definition.ts` in Plan 18-04 — `SOURCE_CAPABILITIES` does not currently have a `subagents` key per PATTERNS.md Group 3.)
       The `cost: false` flag is the **first line of defence** for cost exclusion (QDR-109): downstream cost code must short-circuit on this flag.
    Pattern reference: PATTERNS.md Group 3.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm test:run -- ingest/config 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A3 "type: 'qoder'" ingest/config/tool-dirs.ts` shows `envVar: 'QODER_DB_PATH'`, `configKey: 'qoder_db_paths'`, and `defaultDirs` containing `Library/Application Support/Qoder/SharedClientCache/cache/db/local.db`.
    - `grep -E "qoder:\s*\{" ingest/config/capabilities.ts` returns 1 match; the entry has `cost: false`, `sessions: true`, `replay: true`, `activity: true`.
    - `pnpm exec tsc --noEmit` exits 0.
    - If `tests/unit/ingest/config` exists, it passes.
  </acceptance_criteria>
  <done>Discovery code in Plan 18-03 can call `loadToolDirs().get('qoder')` to obtain the configured DB path list; capability gates in BFF/UI return `cost=false` for qoder.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` exits 0 — no orphan literal arrays, no unhandled discriminated-union branches.
- `pnpm test:run -- lib/agent-tools/types.test.ts` exits 0.
- `pnpm test:run -- ingest/api` exits 0 — pre-existing API tests still pass with the widened whitelists.
- `grep -RE "'qoder'" types/ lib/agent-tools/ ingest/api/ ingest/index.ts ingest/config/ app/api/sync/route.ts | wc -l` returns ≥ 12 (rough lower bound: 1 trace, 1 types, 4+ tests, 5 validation, 2 config).
</verification>

<success_criteria>
1. `assertSourceToolId('qoder')` does not throw (acceptance §1 of SPEC).
2. Existing four-source (or three-source if Phase 17 has not landed) tests pass without modification.
3. `TOOL_DIR_REGISTRY` exposes Qoder's env/config/default per QDR-102.
4. `SOURCE_CAPABILITIES.qoder.cost === false` — the capability flag that Plan 18-04 reads for cost-cell rendering and top-models cost-ranking exclusion.
5. No file `lib/agent-tools/registry.ts`, `ingest/sync/index.ts`, `ingest/sync/sources.ts`, `ingest/api/sources.ts` is touched here — those belong to 18-03/18-04 (file-ownership rule for parallel waves).
</success_criteria>

<output>
Create `.planning/phases/18-qoder-source-integration/18-02-SUMMARY.md` recording: list of files touched (with line numbers of the literal additions), confirmation of `tsc --noEmit` clean, and the exact `SOURCE_CAPABILITIES.qoder` shape committed.
</output>
