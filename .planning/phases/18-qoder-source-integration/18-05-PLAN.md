---
phase: 18-qoder-source-integration
plan: 05
type: execute
wave: 2
depends_on:
  - 18-02
  - 18-04
files_modified:
  - components/overview/kpi-hero.tsx
  - components/sessions/session-filter-dropdown.tsx
  - components/overview/starred-sessions.tsx
  - ingest/api/overview.ts
  - docs/CONFIGURATION.md
  - docs/API.md
  - docs/services/ingest.md
  - ERRORS_LEARNED.md
  - components/replay/subagent-block.tsx
autonomous: true
requirements:
  - QDR-104
  - QDR-105
  - QDR-107
  - QDR-109
  - QDR-110
must_haves:
  truths:
    - "SourceSwitcher renders a 'Qoder' option (label + brand colour) wherever it currently renders openclaw/claude-code/codex/opencode (QDR-104; QDR-101 surface)."
    - "Sessions table cost column shows '—' (or `unknown`) for every Qoder row; cost-based top-models excludes Qoder (`AND source != 'qoder'` OR a positive whitelist that omits 'qoder'); provider grouping never assigns 'ultimate'/'experts-ultimate' to Anthropic/OpenAI/Gemini (QDR-109; SPEC §9 acceptance)."
    - "Replay UI parent transcript surfaces D-06 part 2 explicitly: when a parent Agent tool call's `toolCallId` matches a `subagent_links` row, an inline link to `/qoder/sessions/<child>` renders source-agnostically (QDR-107; D-06 part 2)."
    - "Replay UI child session header surfaces D-06 part 3 explicitly: when `session.parentSessionId` is non-null, a 'Spawned by <parent title>' back-link badge renders source-agnostically (QDR-107; D-06 part 3)."
    - "Pre-check task documents whether existing OpenClaw/Codex/OpenCode replay UI already renders subagent linkage; if yes → wire Qoder into the same code path (no Qoder-only component); if no → create source-agnostic UI in the same shared components."
    - "docs/CONFIGURATION.md documents QODER_DB_PATH and qoder_db_paths with the privacy hardline; docs/API.md and docs/services/ingest.md include qoder in source examples; ERRORS_LEARNED.md records the Qoder credential hardline + JSONL-insufficient lesson + token double-counting + cost ambiguity (QDR-105; QDR-110)."
  artifacts:
    - path: "components/overview/kpi-hero.tsx"
      provides: "SOURCE_LABELS map widened to include qoder"
      contains: "qoder"
    - path: "components/sessions/session-filter-dropdown.tsx"
      provides: "SOURCE_LABELS map widened to include qoder"
      contains: "qoder"
    - path: "components/overview/starred-sessions.tsx"
      provides: "SOURCE_COLORS + sourceLabel switch widened to include qoder"
      contains: "qoder"
    - path: "ingest/api/overview.ts"
      provides: "Cost rollup queries explicitly exclude source='qoder'; provider-grouping logic never maps 'ultimate'/'experts-ultimate' to Anthropic/OpenAI/Gemini"
      contains: "source != 'qoder'"
    - path: "docs/CONFIGURATION.md"
      provides: "Qoder env var + config key + default path documentation"
      contains: "QODER_DB_PATH"
    - path: "ERRORS_LEARNED.md"
      provides: "Qoder credential hardline + JSONL-insufficient + cached_tokens + ultimate/experts-ultimate ambiguity lessons appended"
      contains: "machine_token.json"
  key_links:
    - from: "ingest/api/overview.ts cost rollup queries"
      to: "exclude source='qoder' from cost-based top-models / cost aggregations"
      via: "WHERE source IN ('openclaw','claude-code','codex','opencode') OR explicit `AND source != 'qoder'`"
      pattern: "source\\s*(!=|<>)\\s*'qoder'"
    - from: "Replay parent transcript Agent tool call row"
      to: "/qoder/sessions/<child-session-id>"
      via: "subagent_links lookup keyed on toolCallId === parent_tool_call_id"
      pattern: "subagentLink|subagent_links"
    - from: "Session detail HUD header"
      to: "Parent session detail page"
      via: "session.parentSessionId resolved via sessions.id lookup"
      pattern: "Spawned by|parentSessionId"
---

<objective>
Wave 2 BFF surface — half B (parallel-safe with 18-03 and 18-04; zero `files_modified` overlap with either). Make Qoder reachable from the user's browser. Three concerns:

1. **Frontend label/color** in SourceSwitcher's three known consumer files.
2. **Cost-exclusion** (`AND source != 'qoder'` or positive whitelist) in `ingest/api/overview.ts` cost rollup queries, plus a provider-grouping guard so Qoder's `ultimate` / `experts-ultimate` model keys never get attributed to Anthropic / OpenAI / Gemini (SPEC §9 acceptance).
3. **D-06 parts 2 + 3** explicitly: parent transcript inline link to subagent session, child session header back-link to parent. **The grep / decision pre-check is its own task and the wiring/creation is its own task — neither is hidden inside a `<verify>` step or a conditional branch.**

Plus 4 documentation files (CONFIGURATION, API, services/ingest, ERRORS_LEARNED).

Purpose: 18-04 wired the BFF dispatch; this plan finishes the user-visible UX surfacing — labels, cost behaviour, subagent navigation, and developer documentation. After this plan lands, hitting `/qoder/sessions` shows a properly-labelled Qoder source filter, cost cells render `—`, top-models cost rankings exclude Qoder, the replay UI navigates parent ↔ child Qoder sessions, and the docs match reality.

Output: 3 frontend label/color updates, 1 cost-exclusion edit + provider-grouping guard in overview.ts, source-agnostic replay UI subagent surfacing wiring (with explicit grep+decision tasks), and 4 doc updates.
</objective>

<execution_context>
@/Users/ebbi/Work/ai-dashboard-projects/.qwen/get-shit-done/workflows/execute-plan.md
@/Users/ebbi/Work/ai-dashboard-projects/.qwen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-qoder-source-integration/18-SPEC.md
@.planning/phases/18-qoder-source-integration/18-CONTEXT.md
@.planning/phases/18-qoder-source-integration/18-PATTERNS.md
@.planning/phases/17-opencode-source-integration/17-04-PLAN.md
@.planning/phases/18-qoder-source-integration/18-02-SUMMARY.md
@components/overview/kpi-hero.tsx
@components/sessions/session-filter-dropdown.tsx
@components/overview/starred-sessions.tsx
@components/replay/subagent-block.tsx
@docs/CONFIGURATION.md
</context>

<interfaces>
<!-- Pattern A — frontend label/color sites (3 components). -->
components/overview/kpi-hero.tsx:45-50         SOURCE_LABELS (ALL-CAPS)        — ADD: qoder: 'QODER'
components/sessions/session-filter-dropdown.tsx:27-31  SOURCE_LABELS (Title)   — ADD: qoder: 'Qoder'
components/overview/starred-sessions.tsx:13-17 SOURCE_COLORS                   — ADD: qoder: 'var(--<radix-nova-token>)'
components/overview/starred-sessions.tsx:37-44 sourceLabel switch              — ADD: case 'qoder': return 'Qoder'

<!-- Pattern H — cost exclusion in overview cost rollup queries. -->
ingest/api/overview.ts (search for cost-rollup queries that aggregate `source_cost_usd` or estimate cost from pricing registry; add `AND source != 'qoder'` OR positive whitelist excluding qoder).

<!-- Existing replay-UI subagent surface (already discovered by grep at planning time). -->
components/replay/subagent-block.tsx:7  imports TraceSubagentLink — appears to render subagent linkage today; the grep task confirms whether it covers BOTH D-06 part 2 (parent transcript inline link) AND part 3 (child session header back-link) and whether it is source-agnostic.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Frontend label/color additions (3 files) + cost-exclusion + provider-grouping guard in ingest/api/overview.ts</name>
  <files>components/overview/kpi-hero.tsx, components/sessions/session-filter-dropdown.tsx, components/overview/starred-sessions.tsx, ingest/api/overview.ts</files>
  <read_first>
    - components/overview/kpi-hero.tsx:45-50 (SOURCE_LABELS ALL-CAPS)
    - components/sessions/session-filter-dropdown.tsx:27-31 (SOURCE_LABELS Title-case)
    - components/overview/starred-sessions.tsx:13-17 + 37-44 (SOURCE_COLORS + sourceLabel switch)
    - ingest/api/overview.ts (full — locate every cost rollup query: search for `source_cost_usd`, `pricingStatus`, top-models cost ranking, provider grouping logic; SPEC §9 acceptance requires these specific behaviours)
    - .planning/phases/18-qoder-source-integration/18-SPEC.md §9 acceptance criteria
    - .planning/phases/18-qoder-source-integration/18-PATTERNS.md Group 7 (Frontend Registry, lines 618-672) and Pattern H (lines 805-807)
    - .planning/phases/18-qoder-source-integration/18-04-SUMMARY.md (if produced — cite the chosen radix-nova brand colour token to keep starred-sessions colour in sync with the BFF definition)
  </read_first>
  <action>
    A) **Frontend labels/colors (3 files; mechanical):**
    1) `components/overview/kpi-hero.tsx:45-50`: append `qoder: 'QODER'` to `SOURCE_LABELS`.
    2) `components/sessions/session-filter-dropdown.tsx:27-31`: append `qoder: 'Qoder'` to `SOURCE_LABELS`. The type is `Record<TraceSource, string>`; TypeScript will *require* this entry now that 18-02 widened `TraceSource` — surfacing any oversight at compile time.
    3) `components/overview/starred-sessions.tsx:13-17`: append `qoder: 'var(--<radix-nova-token>)'` to `SOURCE_COLORS` matching the `brandColor` chosen in Plan 18-04 Task 1's `definition.ts` (read 18-04-SUMMARY for the exact token). If 18-04-SUMMARY records a fallback to `var(--accent)`, mirror it here.
    4) `components/overview/starred-sessions.tsx:37-44`: append `case 'qoder': return 'Qoder'` to the `sourceLabel` switch.

    B) **Cost exclusion in `ingest/api/overview.ts` (Pattern H, QDR-109):**
    5) Locate cost-aggregation queries. Apply one of two equivalent strategies:
       - **Positive whitelist:** change `WHERE source IN (...)` lists to explicitly enumerate the cost-priced sources (`'openclaw','claude-code','codex'`, plus `'opencode'` if Phase 17 has landed). This is the safer default — it keeps Qoder's exclusion explicit in SQL.
       - **Negative filter:** add `AND source != 'qoder'` to the WHERE clause.
       Apply to: top-models cost ranking (`metric=cost`), per-source cost rollups, dashboard cost KPI aggregations. Do NOT modify token-based aggregations (token-based top-models MAY include qoder model keys per SPEC §9 acceptance).
    6) Provider-grouping guard: locate any code that maps a `model` string to a provider (Anthropic / OpenAI / Gemini / etc.). Add an explicit guard so when `source === 'qoder'` OR the model key is `'ultimate'` / `'experts-ultimate'`, the provider is set to `'qoder'` (or `null` / `'unknown'` — match existing convention) and NEVER Anthropic / OpenAI / Gemini. SPEC §9 acceptance: "Provider grouping never assigns 'ultimate' to Anthropic/OpenAI". If a relevant unit test file exists for the grouping helper, add a test asserting this behaviour; otherwise note in 18-05-SUMMARY for follow-up.

    Pattern reference: PATTERNS.md Group 7 + Pattern H. Analog: 17-04-PLAN T-04.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm test:run -- ingest/api/overview 2>&1 | tail -20 && pnpm lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "qoder" components/overview/kpi-hero.tsx` ≥ 1.
    - `grep -c "qoder" components/sessions/session-filter-dropdown.tsx` ≥ 1.
    - `grep -c "qoder" components/overview/starred-sessions.tsx` ≥ 2 (SOURCE_COLORS + sourceLabel case).
    - `grep -E "source\s*(!=|<>)\s*'qoder'|source\s+IN\s*\([^)]*\)" ingest/api/overview.ts` shows the cost-rollup queries either explicitly exclude qoder (`!= 'qoder'`) OR use a positive whitelist that does not list qoder.
    - `grep -E "ultimate|experts-ultimate" ingest/api/overview.ts` is paired with a guard branch (manual review point — verify with `grep -B2 -A4 "ultimate" ingest/api/overview.ts`); the diff shows a non-Anthropic/OpenAI/Gemini provider value when the model key matches.
    - `pnpm exec tsc --noEmit` exits 0; `pnpm lint` exits 0.
    - Smoke (manual): hitting `/qoder/sessions` shows the cost column rendered as `—` (or `unknown`), never `$0.00`. Hitting `/api/v1/overview/top-models?metric=cost` does not include `'ultimate'` / `'experts-ultimate'` as provider Anthropic / OpenAI / Gemini.
  </acceptance_criteria>
  <done>Frontend renders Qoder labels and colour wherever the four other sources do; cost cells show `—` for Qoder; cost-based top-models excludes Qoder; provider grouping never misattributes `ultimate` to Anthropic/OpenAI/Gemini.</done>
</task>

<task type="auto">
  <name>Task 2: Replay UI subagent-surface AUDIT — grep + document existing rendering for D-06 parts 2 + 3</name>
  <files>.planning/phases/18-qoder-source-integration/18-05-replay-audit.md</files>
  <read_first>
    - components/replay/subagent-block.tsx (full — already imports TraceSubagentLink; this is the prime candidate for D-06 part 2 inline link rendering)
    - .planning/phases/18-qoder-source-integration/18-CONTEXT.md lines 65-69 (D-06 three-pronged definition; D-07 child session row independence)
    - components/replay/ directory listing (look for any other subagent-* / agent-tool-call-* / linkedSession-* files)
    - components/sessions/ directory listing (look for SessionDetailHud / session-header / parentBadge components — D-06 part 3 lives here)
    - app/(tool-shell)/[tool]/sessions/ or wherever per-source session detail pages route (look for existing parent/child header rendering)
  </read_first>
  <action>
    This task is a discovery + decision step. It produces a short audit document and does NOT modify production code. Task 3 acts on the audit's findings.

    1) Run an exhaustive grep across the frontend for existing subagent-link rendering:
       ```
       grep -rn "TraceSubagentLink\|subagent_links\|subagentLink\|relationshipType\s*===\s*'subagent'\|parentSessionId" \
         components/ app/ 2>/dev/null
       ```
       Capture every hit. For each file, classify:
       - **D-06 part 1 (write path)** — backend / parser / schema (out of scope for this task).
       - **D-06 part 2 (parent transcript inline link)** — does this file render a clickable link from an Agent / subagent_links row in the parent session's replay? Note exact rendering predicate (does it filter by `source`?).
       - **D-06 part 3 (child session back-link)** — does this file render a "Spawned by ..." badge in the child session's HUD header? Note exact rendering predicate.

    2) Read `components/replay/subagent-block.tsx` end-to-end. Document:
       - Whether it renders D-06 part 2 (inline link in parent transcript).
       - Whether it filters by `source` (any literal `'openclaw'` / `'codex'` / `'opencode'` / `'claude-code'` in the visibility predicate).
       - Whether it routes to `/<source>/sessions/<id>` source-agnostically.

    3) Locate the session-detail HUD header. Search candidates:
       - `components/sessions/session-detail-hud.tsx`
       - `components/sessions/session-header.tsx`
       - `app/(tool-shell)/[tool]/sessions/[sessionId]/page.tsx`
       Document whether D-06 part 3 (parent back-link badge) currently renders. If yes, where; if no, which file is the natural home for it (which file already renders the title / project / model header for openclaw/codex sessions).

    4) Write `.planning/phases/18-qoder-source-integration/18-05-replay-audit.md` with sections:
       - **Existing rendering for D-06 part 2** — file path(s), line numbers, source-agnosticism verdict (yes / no — if no, which sources are hardcoded).
       - **Existing rendering for D-06 part 3** — file path(s), line numbers, source-agnosticism verdict.
       - **Decision matrix for Task 3:** one of three outcomes per part:
         (a) Already source-agnostic → Task 3 only verifies Qoder routes resolve (no code change).
         (b) Renders for some sources but hardcodes the visibility predicate → Task 3 generalises the predicate (single-line change).
         (c) Does not render at all → Task 3 creates source-agnostic UI in the file identified above (mirrors the existing parent transcript / HUD header style).

    Pattern reference: PATTERNS.md (no specific group — D-06 part 2/3 are first-class scope). Privacy hardline (D-10) does not apply here (frontend-only).
  </action>
  <verify>
    <automated>test -f .planning/phases/18-qoder-source-integration/18-05-replay-audit.md && grep -c "Decision matrix" .planning/phases/18-qoder-source-integration/18-05-replay-audit.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/18-qoder-source-integration/18-05-replay-audit.md` exists.
    - The audit explicitly addresses BOTH D-06 part 2 AND D-06 part 3 — not just one.
    - For each part, the audit cites specific file paths + line numbers from the codebase (not theoretical / "should be in components/replay/").
    - The decision matrix records one of (a) / (b) / (c) for EACH of part 2 and part 3 (so the matrix has at least 2 cells filled).
    - The audit names the exact file Task 3 will modify (or, in outcome (a), explicitly states "no production code change required for this part").
  </acceptance_criteria>
  <done>An on-disk audit document exists and unambiguously tells Task 3 what to do for each of D-06 part 2 and part 3.</done>
</task>

<task type="auto">
  <name>Task 3: Wire D-06 parts 2 + 3 source-agnostically per the audit's decision matrix</name>
  <files>components/replay/subagent-block.tsx</files>
  <read_first>
    - .planning/phases/18-qoder-source-integration/18-05-replay-audit.md (produced in Task 2 — the decision matrix drives this task)
    - components/replay/subagent-block.tsx (full — confirmed D-06 part 2 candidate from planning-time grep)
    - The file(s) named in the audit's "natural home" for D-06 part 3 (session-detail-hud / session-header / page.tsx), full file each
    - .planning/phases/18-qoder-source-integration/18-CONTEXT.md lines 65-69 (D-06 spec)
    - .planning/phases/18-qoder-source-integration/18-SPEC.md §6 acceptance criteria
  </read_first>
  <action>
    Execute exactly the decision matrix written in `18-05-replay-audit.md`. The `<files>` list above is a placeholder for the most likely D-06-part-2 file (`components/replay/subagent-block.tsx`); update the actual diff to include any additional files identified in the audit (e.g. session-detail-hud / session-header). If the audit names a file not in `<files>` above, ADD it to the diff at execution time and record the addition in 18-05-SUMMARY.

    For each of D-06 part 2 and D-06 part 3, branch on the audit verdict:

    **Outcome (a) — Already source-agnostic:** Verify by hitting (or scripting) the Qoder happy path:
    - Part 2 verify: A parent Qoder session whose root has an `Agent` tool call rendering an inline link to `/qoder/sessions/<child>` — confirm the link resolves.
    - Part 3 verify: A child Qoder session whose detail page renders a "Spawned by <parent title>" badge linking back to `/qoder/sessions/<parent>`.
    No code change. Record verification result in SUMMARY.

    **Outcome (b) — Hardcoded predicate:** Make the predicate source-agnostic. Examples (the audit will name the exact line):
    - `source === 'openclaw' || source === 'codex'` → drop the equality check; render whenever a `subagent_links` row exists for the message.
    - `if (subagent.source === 'openclaw' && ...)` → drop the source clause.
    Add a unit / snapshot test that asserts the rendering for `source='qoder'` if a relevant test file exists (`components/replay/__tests__/subagent-block.test.tsx` etc.). If no test file exists, note in SUMMARY.

    **Outcome (c) — Does not render at all:** Create source-agnostic UI in the file the audit identified.
    - **D-06 part 2 (parent transcript inline link):** Inside the rendering path that emits the `Agent` tool call row (or whatever rendering currently exists for tool calls in `components/replay/`), look up the matching `subagent_links` row for the message ordinal; when present, render a clickable element (shadcn radix-nova design tokens — Link / Button-as-link variant) labelled "→ Subagent session" navigating to `/${subagent.subagent_source}/sessions/${subagent.subagent_session_id}` (NOT a hardcoded `/qoder/...`). The component MUST work for openclaw/codex/qoder/etc.
    - **D-06 part 3 (child session header back-link):** Inside the file that already renders the per-session HUD header (title, project, model — identified in the audit), when `session.parentSessionId` is non-null AND the parent session row exists in canonical DB, render a "Spawned by <parent.title>" badge (shadcn Badge variant) linking to `/${parent.source}/sessions/${parent.id}`. Source-agnostic. Match the existing badge style for non-source-specific surfaces (e.g. starred / project tags).

    For BOTH parts, the rendering MUST satisfy:
    - Source-agnostic predicate (no `source === 'qoder'` literal).
    - Routes use `/${source}/sessions/${id}` template (not hardcoded `/qoder/...`).
    - Visual treatment uses existing radix-nova tokens (no new colour / spacing inventions).

    Pattern reference: PATTERNS.md Group 9 (UI surfacing) — no exact analog because D-06 part 2/3 are net-new in this phase if the audit returns (c).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm lint 2>&1 | tail -10 && pnpm test:run -- components/replay 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - **D-06 part 2 (parent transcript inline link) — concrete check:** EXACTLY ONE of the following is true:
      (a) `grep -E "subagentLink|subagent_links|TraceSubagentLink" components/replay/` returns a hit AND that file's rendering predicate is source-agnostic (`grep -E "source\s*===\s*'(openclaw|codex|claude-code|opencode)'" <that file>` returns 0 matches inside the rendering branch); OR
      (b) the audit Outcome (a) was recorded — the smoke test (parent Qoder session, navigate via Agent tool call) succeeds end-to-end without code change.
    - **D-06 part 3 (child session back-link) — concrete check:** EXACTLY ONE of the following is true:
      (a) `grep -E "Spawned by|parentSessionId" components/sessions/ app/\(tool-shell\)/` returns a hit in a HUD-header file AND that rendering is source-agnostic; OR
      (b) the audit Outcome (a) was recorded — the smoke test (child Qoder session detail page, parent back-link) succeeds end-to-end without code change.
    - For Outcome (b) or (c) on either part, the diff for the touched component shows NO new literal `'qoder'` string in the rendering predicate (predicate must remain source-agnostic).
    - Smoke (manual): on a parent Qoder session detail page, an `Agent` tool call shows a clickable "→ Subagent session" link routing to `/qoder/sessions/<child>`. On the child session detail page, a "Spawned by <parent>" badge routes to `/qoder/sessions/<parent>`. Both DOM elements appear in the rendered HTML (verifiable with `curl http://localhost:3000/qoder/sessions/<id> | grep -E 'Spawned by|subagent'` once a fixture session is synced).
    - 18-05-SUMMARY records: which audit outcome was taken for each part, which file(s) were modified, and the test verdict.
  </acceptance_criteria>
  <done>D-06 part 2 (parent transcript inline link) and D-06 part 3 (child session back-link) both render for Qoder sessions, source-agnostically — either by verifying existing UI works, by generalising a hardcoded predicate, or by creating new UI in the file the audit named. Acceptance is observable in the rendered DOM, not buried in a verify step.</done>
</task>

<task type="auto">
  <name>Task 4: Documentation — CONFIGURATION + API + services/ingest + ERRORS_LEARNED (4 files)</name>
  <files>docs/CONFIGURATION.md, docs/API.md, docs/services/ingest.md, ERRORS_LEARNED.md</files>
  <read_first>
    - docs/CONFIGURATION.md (existing Codex / OpenCode sections — copy structure for the new Qoder section)
    - docs/API.md (existing agent-tools/codex/* example block)
    - docs/services/ingest.md (source list + discovery flowchart)
    - ERRORS_LEARNED.md (append-only; locate the bottom)
    - .planning/phases/18-qoder-source-integration/18-CONTEXT.md (D-03 fingerprint, D-10 privacy hardline)
    - .planning/phases/18-qoder-source-integration/18-SPEC.md §3, §8, §9, §10
    - .planning/phases/18-qoder-source-integration/18-03-SUMMARY.md (fingerprint formula committed by Plan 18-03)
    - .planning/phases/18-qoder-source-integration/18-04-SUMMARY.md (registry registration; brand colour token)
  </read_first>
  <action>
    1) **`docs/CONFIGURATION.md`** — add a new `### Qoder` block after the existing source sections, mirroring the Codex / OpenCode structure. Include:
       - Default DB path `~/Library/Application Support/Qoder/SharedClientCache/cache/db/local.db`.
       - Env var `QODER_DB_PATH` (single path).
       - Config key `qoder_db_paths` (array of paths).
       - Explicit privacy notes per SPEC §10: opened readonly; never reads `machine_token.json`, `supabase_token` table, `secret://` keys, or any credential storage.
    2) **`docs/API.md`** — add `agent-tools/qoder/*` parallel examples for sessions, replay, subagents, mirroring the existing codex examples. Include the new `/api/v1/sources/qoder` discovery example with `configured` / `empty` / `error` shape (Plan 18-03 acceptance).
    3) **`docs/services/ingest.md`** — append Qoder to the source list and discovery flowchart. Document `discoverQoderSources` semantics (readonly + table validation), the per-session fingerprint rule (`qoder-session-v1:<id>:<gmt_modified>:<msg_count>:<max_msg_gmt>`) per D-03, and the cost / agent / automation exclusions per SPEC §9. Cross-reference `docs/skip-cache-naming-debt.md` (Plan 18-01).
    4) **`ERRORS_LEARNED.md`** — append a Phase-18 lessons block:
       - **Qoder credential hardline:** Never read `machine_token.json`, `supabase_token` table, `secret://` keys in `state.vscdb`, HTTP cookie storage, or any auth/login storage. Reading these would expose user credentials to the dashboard process.
       - **Qoder JSONL is insufficient:** `~/.qoder/cache/projects/<project>/conversation-history/<session>/<session>.jsonl` lacks tokens, model keys, tool calls, parent/child links — JSONL was rejected as both primary and fallback source; SQLite main DB is the only valid Qoder source.
       - **Token double-counting risk:** `cached_tokens` is non-additive into `totalTokens`; `totalTokens = prompt + completion` only. Adding cached_tokens would inflate dashboards.
       - **Cost ambiguity for product-tier model keys:** `ultimate` / `experts-ultimate` are not provider/model identifiers — never map to Anthropic/OpenAI/Gemini.
    Pattern reference: PATTERNS.md Group 9 (documentation row). Analog: 17-04-PLAN T-06.
  </action>
  <verify>
    <automated>pnpm lint 2>&1 | tail -10 && grep -c "QODER_DB_PATH" docs/CONFIGURATION.md && grep -c "qoder-session-v1" docs/services/ingest.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "QODER_DB_PATH" docs/CONFIGURATION.md` ≥ 1.
    - `grep -c "qoder" docs/API.md` ≥ 1.
    - `grep -c "qoder" docs/services/ingest.md` ≥ 1; `grep -c "qoder-session-v1" docs/services/ingest.md` ≥ 1.
    - `grep -c "machine_token.json" ERRORS_LEARNED.md` ≥ 1; `grep -ci "qoder" ERRORS_LEARNED.md` ≥ 1.
    - All four files include both the privacy hardline reference AND the JSONL-insufficient lesson somewhere across the set (not necessarily each file).
  </acceptance_criteria>
  <done>Documentation reflects Qoder as the fifth source, the credential hardline is recorded, and developers reading the docs can configure Qoder without re-reading the phase plans.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` exits 0.
- `pnpm lint` exits 0.
- `pnpm test:run -- ingest/api/overview` exits 0.
- `pnpm test:run -- components/replay` exits 0.
- Existing visual / replay tests for openclaw / claude-code / codex / opencode continue to pass.
- Manual smoke: `pnpm dev`, navigate `/qoder/dashboard`, `/qoder/sessions`, click into a Qoder session → see HUD header + replay rows; on a parent Qoder session with an `Agent` tool call, click the "→ Subagent session" link → verifies D-06 part 2; on the child session, click the "Spawned by ..." badge → verifies D-06 part 3.
- Cost exclusion smoke: `/api/v1/overview/top-models?metric=cost&source=all` excludes `ultimate` / `experts-ultimate`; `?metric=tokens&source=all` MAY include them.
- The replay audit document exists and was committed alongside the production code changes.
</verification>

<success_criteria>
1. **QDR-104 (sessions browsing — UX surface):** Source switcher renders Qoder; cost cells render `—` for Qoder rows; cost-based top-models excludes Qoder.
2. **QDR-105 (session detail + docs):** HUD header correctly renders Qoder metadata (parser side from 18-03; UI side already source-agnostic — verified or generalised here); CONFIGURATION / API / services/ingest documentation reflects the fifth source.
3. **QDR-107 (subagent surfacing parts 2 + 3):** Parent transcript inline link AND child session back-link both render for Qoder source-agnostically (D-06 parts 2 + 3). Acceptance is observable in the DOM, not buried in a verify step.
4. **QDR-109 (model display + cost exclusion + provider grouping):** Raw model key displayed; cost column shows `—`; cost-based top-models excludes Qoder; provider grouping never misattributes `ultimate` / `experts-ultimate` to Anthropic / OpenAI / Gemini.
5. **QDR-110 (docs hardline):** ERRORS_LEARNED records the credential hardline + JSONL-insufficient + cached_tokens + product-tier-model-key ambiguity.
6. Existing tests for the four prior sources pass without modification.
</success_criteria>

<output>
Create `.planning/phases/18-qoder-source-integration/18-05-SUMMARY.md` recording: the audit-document path + decision matrix outcomes (one of a/b/c per D-06 part); which files were ultimately modified for D-06 part 2 / part 3 (or "no code change required"); cost-exclusion strategy used (positive whitelist vs negative filter); provider-grouping guard placement; and the four docs files updated.

The audit document `.planning/phases/18-qoder-source-integration/18-05-replay-audit.md` is also a deliverable of this plan (produced by Task 2).
</output>
