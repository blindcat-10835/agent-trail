---
phase: 18-qoder-source-integration
plan: 04
type: execute
wave: 2
depends_on:
  - 18-02
files_modified:
  - lib/agent-tools/qoder/definition.ts
  - lib/agent-tools/qoder/server-adapter.ts
  - lib/agent-tools/registry.ts
  - app/api/agent-tools/[tool]/agents/[agentName]/avatar/route.ts
  - app/api/agent-tools/[tool]/agents/route.ts
  - app/api/agent-tools/[tool]/events/route.ts
  - app/api/agent-tools/[tool]/health/route.ts
  - app/api/agent-tools/[tool]/overview/agents/route.ts
  - app/api/agent-tools/[tool]/overview/aggregates/route.ts
  - app/api/agent-tools/[tool]/overview/automations/route.ts
  - app/api/agent-tools/[tool]/overview/capabilities/route.ts
  - app/api/agent-tools/[tool]/overview/daily-tokens/route.ts
  - app/api/agent-tools/[tool]/overview/starred/route.ts
  - app/api/agent-tools/[tool]/overview/status/route.ts
  - app/api/agent-tools/[tool]/overview/timeline/route.ts
  - app/api/agent-tools/[tool]/overview/top-models/route.ts
  - app/api/agent-tools/[tool]/overview/top-projects/route.ts
  - app/api/agent-tools/[tool]/sessions/[sessionId]/messages/route.ts
  - app/api/agent-tools/[tool]/sessions/[sessionId]/route.ts
  - app/api/agent-tools/[tool]/sessions/[sessionId]/search/route.ts
  - app/api/agent-tools/[tool]/sessions/[sessionId]/star/route.ts
  - app/api/agent-tools/[tool]/sessions/[sessionId]/turns/route.ts
  - app/api/agent-tools/[tool]/sessions/lookup/route.ts
  - app/api/agent-tools/[tool]/sessions/route.ts
  - app/api/agent-tools/[tool]/sessions/starred/route.ts
  - app/api/agent-tools/[tool]/sync/route.ts
autonomous: true
requirements:
  - QDR-101
  - QDR-104
  - QDR-110
must_haves:
  truths:
    - "qoderDef is registered in lib/agent-tools/registry.ts (TOOL_IDS, SHELL_TOOL_IDS, AGENT_TOOL_DEFINITIONS) — getAllDefinitions() returns it; SourceSwitcher will pick it up via the registry (QDR-101)."
    - "qoderAdapter is exported from lib/agent-tools/qoder/server-adapter.ts and is structurally identical to codexAdapter except for the SOURCE='qoder' constant (QDR-101)."
    - "All 23 BFF route handlers under app/api/agent-tools/[tool]/ accept 'qoder' as a valid [tool] parameter and dispatch to qoderAdapter (QDR-101; QDR-104)."
    - "/qoder/dashboard, /qoder/sessions, /qoder/sessions/:id, /qoder/activity resolve at the BFF layer without 404 (QDR-104; QDR-110 — readonly access path)."
    - "definition.ts declares capabilities.cost=false and capabilities.subagents=true per SPEC §6 (QDR-101)."
    - "client-adapter.ts is delivered IF AND ONLY IF an analog source (codex OR claude-code) ships its own client-adapter.ts at execution time. No re-export stub. Decision recorded in 18-04-SUMMARY."
  artifacts:
    - path: "lib/agent-tools/qoder/definition.ts"
      provides: "qoderDef AgentToolDefinition with cost:false, subagents:true, sessions/replay/activity:true"
      exports: ["qoderDef"]
      contains: "id: 'qoder'"
    - path: "lib/agent-tools/qoder/server-adapter.ts"
      provides: "qoderAdapter — source-scoped BFF adapter wrapping ingest calls with source='qoder'"
      exports: ["qoderAdapter"]
      contains: "SOURCE = 'qoder'"
    - path: "lib/agent-tools/registry.ts"
      provides: "qoderDef registered in TOOL_IDS, SHELL_TOOL_IDS, AGENT_TOOL_DEFINITIONS"
      contains: "qoderDef"
  key_links:
    - from: "lib/agent-tools/registry.ts"
      to: "lib/agent-tools/qoder/definition.ts"
      via: "import { qoderDef } from './qoder/definition'"
      pattern: "import\\s+\\{\\s*qoderDef"
    - from: "23 BFF route handlers"
      to: "lib/agent-tools/qoder/server-adapter.ts"
      via: "ADAPTERS map adds `qoder: qoderAdapter`"
      pattern: "qoder:\\s*qoderAdapter"
---

<objective>
Wave 2 BFF surface — half A (parallel-safe with 18-03 and 18-05; no file-modified overlap with either). Build the **Qoder adapter source files** under `lib/agent-tools/qoder/` per Pattern E (`definition.ts` + `server-adapter.ts`; `client-adapter.ts` only if an analog source ships one — see Task 1 pre-check), register it in `lib/agent-tools/registry.ts`, and thread it through all **23 BFF route handler dispatch maps** under `app/api/agent-tools/[tool]/...` per Pattern D.

Purpose: this plan exposes Qoder to the rest of the BFF layer. After it lands, `qoderAdapter` is callable from any route handler that previously dispatched on `[tool]` against the existing four sources — every `/api/agent-tools/qoder/*` URL resolves to the qoderAdapter implementation rather than a 404. Frontend label/color changes, cost-exclusion in overview rollups, replay-UI subagent surfacing, and documentation are deliberately deferred to Plan 18-05 to keep this plan within context budget and to keep `files_modified` disjoint from 18-05.

Output: 2 new adapter files (3 if pre-check passes — see Task 1), 1 registry update, 23 mechanical BFF route patches.
</objective>

<execution_context>
@/Users/ebbi/Work/ai-dashboard-projects/.qwen/get-shit-done/workflows/execute-plan.md
@/Users/ebbi/Work/ai-dashboard-projects/.qwen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-qoder-source-integration/18-SPEC.md
@.planning/phases/18-qoder-source-integration/18-CONTEXT.md
@.planning/phases/18-qoder-source-integration/18-PATTERNS.md
@.planning/phases/17-opencode-source-integration/17-03-PLAN.md
@.planning/phases/18-qoder-source-integration/18-02-SUMMARY.md
@lib/agent-tools/codex/definition.ts
@lib/agent-tools/codex/server-adapter.ts
@lib/agent-tools/registry.ts
</context>

<interfaces>
<!-- Pattern E — AgentToolDefinition + ServerAdapter triplet (codex is the canonical analog). -->

lib/agent-tools/codex/definition.ts:
  export const codexDef: AgentToolDefinition = {
    id: 'codex',
    displayName: 'Codex',
    shortName: 'CDX',
    brandColor: 'var(--accent)',
    capabilities: { sessions, replay, activity, office, workspace, subagents, cost, approvals },
    ingest: { sourceType, discoveryEnv },
  }

lib/agent-tools/codex/server-adapter.ts:
  import { buildSourceScopedSessionParams, getSourceScopedSession, requireSourceScopedSession } from '../server-adapter'
  const SOURCE = 'codex'
  export const codexAdapter: AgentToolServerAdapter = { id, listSessions, getSession, getMessages, getTurns, search, star, lookup, ... }

lib/agent-tools/registry.ts:
  export const TOOL_IDS: SourceToolId[]      // → add 'qoder'
  export const SHELL_TOOL_IDS: AgentToolId[] // → add 'qoder'
  export const AGENT_TOOL_DEFINITIONS        // → add qoderDef

<!-- Pattern D — BFF route handler dispatch map (23 sites; one-line patch each). -->
app/api/agent-tools/[tool]/sessions/route.ts:13-29:
  import { codexAdapter } from '@/lib/agent-tools/codex/server-adapter'
  // ADD: import { qoderAdapter } from '@/lib/agent-tools/qoder/server-adapter'
  const ADAPTERS = { openclaw, 'claude-code', codex, /* qoder */ } as const
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create qoder/{definition,server-adapter}.ts (+ deterministic client-adapter pre-check) and register in agent-tools/registry.ts</name>
  <files>lib/agent-tools/qoder/definition.ts, lib/agent-tools/qoder/server-adapter.ts, lib/agent-tools/registry.ts</files>
  <read_first>
    - lib/agent-tools/codex/definition.ts (full — Pattern E analog; copy structure verbatim)
    - lib/agent-tools/codex/server-adapter.ts (full — Pattern E analog; SOURCE constant + adapter object)
    - lib/agent-tools/server-adapter.ts (full — shared helpers `buildSourceScopedSessionParams`, `getSourceScopedSession`, `requireSourceScopedSession` are source-agnostic; do NOT modify)
    - lib/agent-tools/registry.ts (full — locate TOOL_IDS, SHELL_TOOL_IDS, AGENT_TOOL_DEFINITIONS, getAllDefinitions; existing imports of openclawDef/claudeCodeDef/codexDef pattern)
    - .planning/phases/18-qoder-source-integration/18-PATTERNS.md Group 6 (lines 483-569 — adapter triplet excerpts) and Pattern E (lines 789-791)
    - .planning/phases/18-qoder-source-integration/18-SPEC.md §6 (capabilities required: sessions/replay/activity/subagents true; cost/office/workspace/approvals false)
    - .planning/phases/17-opencode-source-integration/17-03-PLAN.md T-04 (analog adapter creation)
  </read_first>
  <action>
    **Step 0 — Deterministic client-adapter pre-check (fix 4 from checker; replaces conditional branching).**
    Run at execution start, BEFORE creating any files:
    ```
    ls lib/agent-tools/codex/client-adapter.ts 2>/dev/null
    ls lib/agent-tools/claude-code/client-adapter.ts 2>/dev/null
    ```
    Branch on filesystem state — record which branch was taken in 18-04-SUMMARY:
    - **Branch A — at least one analog client-adapter exists.** Qoder MUST ship `lib/agent-tools/qoder/client-adapter.ts` mirroring the analog verbatim with `codex|claude-code → qoder` substitution. Add this file path to the actual diff, append `lib/agent-tools/qoder/client-adapter.ts` to the SUMMARY artifacts list, and add an `import { qoderClientAdapter } from './qoder/client-adapter'` to `lib/agent-tools/registry.ts` IF AND ONLY IF the analog source's adapter is imported there (mirror exactly).
    - **Branch B — neither codex nor claude-code has a client-adapter.ts.** Do NOT create `lib/agent-tools/qoder/client-adapter.ts`. Do NOT create a re-export stub. Pattern E parity is the rule: Qoder ships the same files as codex/claude-code, no more, no less. SUMMARY records: "Branch B taken; no analog exists; no client-adapter.ts shipped."

    The decision is made at execution time based on filesystem state. The frontmatter `files_modified` list intentionally OMITS `client-adapter.ts` — if Branch A is taken, the executor extends the diff to include it but does not need to amend the plan.

    1) **`lib/agent-tools/qoder/definition.ts`** — new file. Copy `codex/definition.ts` verbatim, replace literals:
       - `id: 'qoder'`
       - `displayName: 'Qoder'`
       - `shortName: 'QDR'` (3-letter shorthand consistent with the `OPC`/`CDX` pattern in existing definitions; verify against the existing shorthand convention before committing)
       - `brandColor: 'var(--<radix-nova-accent>)'` — pick a CSS custom property already declared in the radix-nova theme that is **visually distinct** from openclaw/claude-code/codex/opencode brand colours. Confirm by reading `app/globals.css` (or wherever radix-nova tokens are defined) for available accent variables. If no distinct accent exists, fall back to `var(--accent)` — this is acceptable per Claude's Discretion. Document the choice in the SUMMARY.
       - `capabilities: { sessions: true, replay: true, activity: true, office: false, workspace: false, subagents: true, cost: false, approvals: false }` — note `subagents: true` (D-06; Qoder DOES expose subagents) and `cost: false` (SPEC §9 — first-line cost-exclusion gate).
       - `ingest: { sourceType: 'qoder', discoveryEnv: 'QODER_DB_PATH' }`.
    2) **`lib/agent-tools/qoder/server-adapter.ts`** — new file. Copy `codex/server-adapter.ts` verbatim, replace every literal `'codex'` with `'qoder'` and `codexAdapter` with `qoderAdapter`. The shared helpers (`buildSourceScopedSessionParams`, `getSourceScopedSession`, `requireSourceScopedSession`) are imported as-is and require no changes (they already accept `SOURCE` as a string parameter — Pattern E).
    3) **`lib/agent-tools/registry.ts`** — register Qoder:
       - Add import: `import { qoderDef } from './qoder/definition'`. Add import: `import { qoderAdapter } from './qoder/server-adapter'` if other adapters are imported here (mirror the existing codexAdapter import sites).
       - Append `'qoder'` to `TOOL_IDS`, `SHELL_TOOL_IDS`, and any sibling whitelist arrays in this file.
       - Append `qoderDef` to the `AGENT_TOOL_DEFINITIONS` array (in chronological-introduction order: openclaw, claude-code, codex, opencode-if-present, qoder).
       - Confirm `getAllDefinitions()` returns the new array; SourceSwitcher will pick it up dynamically with no further UI change in this plan (frontend label/color additions live in 18-05).
    Pattern reference: PATTERNS.md Group 6 + Pattern E. Analog: 17-03-PLAN T-04.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm test:run -- lib/agent-tools 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/agent-tools/qoder/definition.ts && test -f lib/agent-tools/qoder/server-adapter.ts` (always required).
    - **Branch A only** (analog client-adapter exists): `test -f lib/agent-tools/qoder/client-adapter.ts` AND the file is structurally identical to the analog (diff against `codex/client-adapter.ts` or `claude-code/client-adapter.ts` shows only `codex|claude-code → qoder` substitutions).
    - **Branch B only** (no analog): `! test -f lib/agent-tools/qoder/client-adapter.ts` (file MUST NOT exist; do not create a stub).
    - SUMMARY explicitly states which branch was taken and which analog (if any) was mirrored.
    - `grep -c "id: 'qoder'" lib/agent-tools/qoder/definition.ts` ≥ 1.
    - `grep -E "subagents:\s*true" lib/agent-tools/qoder/definition.ts` returns 1 match (D-06; QDR-107 — D-06 part 1 is enabled by capability flag here even though the parser/UI live elsewhere).
    - `grep -E "cost:\s*false" lib/agent-tools/qoder/definition.ts` returns 1 match (SPEC §9; QDR-109 — first-line cost-exclusion gate).
    - `grep -E "SOURCE\s*=\s*'qoder'" lib/agent-tools/qoder/server-adapter.ts` returns 1 match.
    - `grep -c "qoderDef" lib/agent-tools/registry.ts` ≥ 2 (import + AGENT_TOOL_DEFINITIONS membership).
    - `grep -E "TOOL_IDS.*'qoder'|'qoder'.*TOOL_IDS" lib/agent-tools/registry.ts` (or equivalent multi-line match) shows qoder is in TOOL_IDS array.
    - `pnpm exec tsc --noEmit` exits 0.
    - Existing `pnpm test:run -- lib/agent-tools` exits 0.
  </acceptance_criteria>
  <done>qoderAdapter is importable from `@/lib/agent-tools/qoder/server-adapter`; `getAllDefinitions()` includes qoderDef; client-adapter delivery decision is deterministic and recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Patch all 23 BFF route handlers — add qoderAdapter to each ADAPTERS dispatch map (Pattern D)</name>
  <files>app/api/agent-tools/[tool]/agents/[agentName]/avatar/route.ts, app/api/agent-tools/[tool]/agents/route.ts, app/api/agent-tools/[tool]/events/route.ts, app/api/agent-tools/[tool]/health/route.ts, app/api/agent-tools/[tool]/overview/agents/route.ts, app/api/agent-tools/[tool]/overview/aggregates/route.ts, app/api/agent-tools/[tool]/overview/automations/route.ts, app/api/agent-tools/[tool]/overview/capabilities/route.ts, app/api/agent-tools/[tool]/overview/daily-tokens/route.ts, app/api/agent-tools/[tool]/overview/starred/route.ts, app/api/agent-tools/[tool]/overview/status/route.ts, app/api/agent-tools/[tool]/overview/timeline/route.ts, app/api/agent-tools/[tool]/overview/top-models/route.ts, app/api/agent-tools/[tool]/overview/top-projects/route.ts, app/api/agent-tools/[tool]/sessions/[sessionId]/messages/route.ts, app/api/agent-tools/[tool]/sessions/[sessionId]/route.ts, app/api/agent-tools/[tool]/sessions/[sessionId]/search/route.ts, app/api/agent-tools/[tool]/sessions/[sessionId]/star/route.ts, app/api/agent-tools/[tool]/sessions/[sessionId]/turns/route.ts, app/api/agent-tools/[tool]/sessions/lookup/route.ts, app/api/agent-tools/[tool]/sessions/route.ts, app/api/agent-tools/[tool]/sessions/starred/route.ts, app/api/agent-tools/[tool]/sync/route.ts</files>
  <read_first>
    - app/api/agent-tools/[tool]/sessions/route.ts:13-29 (full — canonical Pattern D analog: imports openclawAdapter/claudeCodeAdapter/codexAdapter, then ADAPTERS const map keyed by source id)
    - .planning/phases/18-qoder-source-integration/18-PATTERNS.md Group 6 BFF route handlers (lines 571-614) — exact list of 23 files
    - app/api/agent-tools/[tool]/sessions/lookup/route.ts (special case per PATTERNS.md note: it currently imports only openclawAdapter; verify whether lookup should be source-scoped for qoder)
    - .planning/phases/17-opencode-source-integration/17-03-PLAN.md T-05 (analog batch-patch task)
  </read_first>
  <action>
    For each of the 23 files listed in `<files>`, apply exactly two mechanical edits:
    1) Add the import line (alphabetised after the last existing adapter import):
       `import { qoderAdapter } from '@/lib/agent-tools/qoder/server-adapter'`
    2) Add a new entry to the local `ADAPTERS` (or `adapters`) record map:
       `qoder: qoderAdapter,`
    The map ordering should match the existing `TOOL_IDS` order (openclaw, claude-code, codex, [opencode if Phase 17 landed,] qoder). Do NOT introduce any other change to these files. The route handler already validates `[tool]` against the dispatch map; once the entry is added, `/qoder/...` URLs resolve.

    Special cases:
    - **`app/api/agent-tools/[tool]/sessions/lookup/route.ts`**: PATTERNS.md notes this file currently imports only `openclawAdapter`. Read it first; if its dispatch is genuinely openclaw-only by design (lookup-by-CWD or similar OpenClaw feature), do NOT add a qoder branch — instead, ensure the route returns a graceful "not supported" response for `[tool]==='qoder'`. If the dispatch supports multiple sources, add qoder to the map. Document the choice in the SUMMARY.
    - **`app/api/agent-tools/[tool]/sync/route.ts`**: this routes to `POST /api/v1/sources/<source>/sync` in ingest. Confirm Plan 18-02's earlier widening of `app/api/sync/route.ts` (which is a different file — `/api/sync` not `/api/agent-tools/[tool]/sync`) does not double-cover this; this file still needs the dispatch-map patch.

    Pattern reference: PATTERNS.md Pattern D + Group 6 lines 571-614. Analog: 17-03-PLAN T-05.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm test:run -- app/api/agent-tools 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -lr "qoder: qoderAdapter" app/api/agent-tools/\[tool\]/ | wc -l` returns ≥ 22 (allowing for the `sessions/lookup/route.ts` special case).
    - `grep -lr "from '@/lib/agent-tools/qoder/server-adapter'" app/api/agent-tools/\[tool\]/ | wc -l` returns ≥ 22.
    - `pnpm exec tsc --noEmit` exits 0.
    - Smoke test (manual or scripted): `curl http://localhost:3000/api/agent-tools/qoder/health` returns the qoderAdapter's health response (not a 404 / "unknown tool" error).
    - `curl http://localhost:3000/api/agent-tools/qoder/sessions` does not return 404.
  </acceptance_criteria>
  <done>All 23 BFF routes accept `qoder` as a valid `[tool]` parameter; `/qoder/dashboard`, `/qoder/sessions`, `/qoder/sessions/:id`, `/qoder/activity` resolve at the BFF layer without 404 (SPEC §6 acceptance — frontend label/color/cost-exclusion is finalised in 18-05).</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` exits 0.
- `pnpm lint` exits 0.
- `pnpm test:run -- lib/agent-tools` exits 0.
- `pnpm test:run -- app/api/agent-tools` (or whatever route-handler suite exists) exits 0.
- Existing visual / replay tests for openclaw / claude-code / codex / opencode continue to pass.
- BFF smoke: `curl http://localhost:3000/api/agent-tools/qoder/health` returns 200 (or whatever the analog adapter returns for that route — never 404 / "unknown tool").
</verification>

<success_criteria>
1. **QDR-101 (BFF tool registry / source identity):** qoderDef registered; getAllDefinitions includes it; all 23 BFF routes dispatch to qoderAdapter.
2. **QDR-104 (sessions browsing — backend dispatch):** `/qoder/sessions` resolves at the BFF layer to qoderAdapter without 404; full UX (label, filter dropdown, starred sessions colour) finalised in 18-05.
3. **QDR-110 (privacy hardline at BFF layer):** qoderAdapter inherits the readonly DB invariants from ingest (Plan 18-03); no credential paths are referenced anywhere in the adapter or route patches.
4. Existing tests for the four prior sources pass without modification.

NOTE — frontend label/color, cost-exclusion in overview API, replay UI subagent surfacing, and documentation are explicitly NOT in scope for this plan; they are covered by Plan 18-05 (QDR-104 cost-exclusion, QDR-105, QDR-107, QDR-109).
</success_criteria>

<output>
Create `.planning/phases/18-qoder-source-integration/18-04-SUMMARY.md` recording: chosen brand colour token, count of patched BFF route files (with the lookup-route special case noted), and the **client-adapter pre-check branch taken** (Branch A with analog name, or Branch B with explicit "no analog exists, no file shipped").
</output>
