---
phase: 18-qoder-source-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ingest/db/schema.sql
  - ingest/db/index.ts
  - tests/unit/ingest/db-migration.test.ts
  - docs/skip-cache-naming-debt.md
autonomous: true
requirements:
  - QDR-101
  - QDR-110
must_haves:
  truths:
    - "An existing local SQLite DB migrates without data loss when the next sequential schema version runs (D-01, D-02)."
    - "INSERT INTO sessions (source, ...) VALUES ('qoder', ...) succeeds after migration; existing four-source rows are preserved (D-01)."
    - "subagent_links.subagent_source and ingest_file_cursors.source_type accept 'qoder' after migration (D-01)."
    - "Skip-cache rows owned by source='qoder' have file_hash NULL'd so the new per-session fingerprint scheme is adopted on next sync; rows for the four prior sources are NOT touched (D-04)."
    - "FTS5 external content triggers, indexes, and foreign-key targets continue to fire after the replacement-table swap."
    - "docs/skip-cache-naming-debt.md exists and documents (a) sessions.file_hash column-name vs generic-skip-key semantics, (b) per-source fingerprint algorithms including the Qoder formula sha256('qoder-session-v1:<id>:<gmt_modified>:<msg_count>:<max_msg_gmt>'), (c) future rename path to source_skip_key (D-05)."
    - "Phase 18 migration adds ONLY 'qoder' to the existing CHECK literals — it does NOT defensively add 'opencode' (D-02)."
  artifacts:
    - path: "ingest/db/schema.sql"
      provides: "CHECK literal lists include 'qoder' on sessions.source, subagent_links.subagent_source, ingest_file_cursors.source_type (and subagent_links.source if Phase 17 added that column)"
      contains: "'qoder'"
    - path: "ingest/db/index.ts"
      provides: "Sequential migration step (next version after Phase 17) using replacement-table pattern + own-source NULL flush"
      contains: "qoder"
    - path: "docs/skip-cache-naming-debt.md"
      provides: "ADR documenting sessions.file_hash semantic drift and per-source fingerprint algorithms"
      min_lines: 20
    - path: "tests/unit/ingest/db-migration.test.ts"
      provides: "Positive INSERT 'qoder' test + updated CHECK literal in fixture DDL"
      contains: "'qoder'"
  key_links:
    - from: "ingest/db/index.ts (migration step)"
      to: "sessions.file_hash column"
      via: "UPDATE sessions SET file_hash = NULL WHERE source = 'qoder'"
      pattern: "UPDATE sessions SET file_hash = NULL"
    - from: "ingest/db/schema.sql"
      to: "ingest/db/index.ts emitted CREATE TABLE strings"
      via: "CHECK literal must match between schema.sql and the strings inside runMigrations()"
      pattern: "CHECK\\(source IN \\('openclaw'.*'qoder'\\)\\)"
---

<objective>
Wave 1 foundation: widen the SQLite source-enum CHECK constraints to accept `'qoder'`, NULL-flush any pre-existing own-source skip-cache rows so the new per-session fingerprint scheme is adopted on next sync, and create the `docs/skip-cache-naming-debt.md` ADR documenting the deliberate reuse of `sessions.file_hash` as a generic source skip key (D-03/D-05).

Purpose: every other Phase-18 plan inserts rows with `source = 'qoder'`; that INSERT only succeeds after the CHECK widening lands. The migration must follow Phase 17's replacement-table pattern exactly (SPEC §2; D-01) and must NOT defensively pre-include other source values (D-02).
Output: schema.sql + ingest/db/index.ts updated to next sequential migration version; new ADR doc; migration test green.
</objective>

<execution_context>
@/Users/ebbi/Work/ai-dashboard-projects/.qwen/get-shit-done/workflows/execute-plan.md
@/Users/ebbi/Work/ai-dashboard-projects/.qwen/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-qoder-source-integration/18-SPEC.md
@.planning/phases/18-qoder-source-integration/18-CONTEXT.md
@.planning/phases/18-qoder-source-integration/18-PATTERNS.md
@.planning/phases/17-opencode-source-integration/17-01-PLAN.md
@.planning/phases/17-opencode-source-integration/17-02-PLAN.md
@ingest/db/schema.sql
@ingest/db/index.ts
</context>

<interfaces>
<!-- Current state of the three CHECK constraints in this worktree (pre-Phase-17). -->
<!-- If Phase 17 has merged into the base branch you rebase onto, the literal sets will already include 'opencode'; widening logic is the same — append 'qoder' to whatever set exists. -->

ingest/db/schema.sql:14   sessions.source              CHECK(source IN ('openclaw', 'claude-code', 'codex'))
ingest/db/schema.sql:172  subagent_links.subagent_source CHECK(subagent_source IN ('openclaw', 'claude-code', 'codex'))
ingest/db/schema.sql:258  ingest_file_cursors.source_type CHECK(source_type IN ('openclaw', 'claude-code', 'codex'))

ingest/db/index.ts        targetVersion = 13   (Phase 17 bumps to 14; Phase 18 bumps to next sequential — 15 if rebased onto Phase-17-merged main, 14 if Phase 17 has not landed)
ingest/db/index.ts:140-405 runMigrations() — array-of-steps pattern with try/catch tolerance for "duplicate column" / "already exists"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Widen source CHECK literals in schema.sql + add migration step in ingest/db/index.ts</name>
  <files>ingest/db/schema.sql, ingest/db/index.ts</files>
  <read_first>
    - ingest/db/schema.sql (full — lines 1-280 to inventory every `CHECK(... IN ('openclaw', 'claude-code', 'codex'))` literal; SPEC §1 enumerates: sessions.source, subagent_links.subagent_source, ingest_file_cursors.source_type, plus subagent_links.source IF that column exists post-Phase-17)
    - ingest/db/index.ts:1-100 (current targetVersion declaration + db opener)
    - ingest/db/index.ts:140-405 (runMigrations() — replacement-table analog steps from earlier versions; reuse the same DDL shape: CREATE *_new → INSERT SELECT → DROP → RENAME → recreate indexes/FTS5 triggers/FK targets)
    - .planning/phases/17-opencode-source-integration/17-01-PLAN.md (T-02 — the canonical replacement-table+NULL-flush pattern this plan mirrors)
    - .planning/phases/18-qoder-source-integration/18-PATTERNS.md Group 2 (Pattern B walkthrough)
    - .planning/phases/18-qoder-source-integration/18-CONTEXT.md (D-01, D-02, D-04 — migration ordering and own-source NULL-flush)
  </read_first>
  <action>
    1) In `ingest/db/schema.sql`, append `'qoder'` (after the last existing literal) to every CHECK constraint that currently lists `('openclaw', 'claude-code', 'codex')` — minimum three sites: line 14 (`sessions.source`), line 172 (`subagent_links.subagent_source`), line 258 (`ingest_file_cursors.source_type`). If the file under your working tree also has a `subagent_links.source` column with its own CHECK (introduced if Phase 17 has landed), widen that too. Do NOT add any other source value — D-02 forbids defensive widening.
    2) In `ingest/db/index.ts`, increment the `targetVersion` constant by exactly 1 from its current value (per D-01: next sequential after Phase 17). Do not skip versions.
    3) In the `migrationSteps` array, append the new step block guarded by `currentVersion < <newVersion>`:
       a) Build `sessions_new` with the **complete current column list** plus the widened CHECK; `INSERT INTO sessions_new SELECT * FROM sessions`; `DROP TABLE sessions`; `ALTER TABLE sessions_new RENAME TO sessions`. Then re-issue every CREATE INDEX statement that targets `sessions` and re-issue the FTS5 external-content triggers that mirror `sessions` rows into the search index.
       b) Repeat (a) for `subagent_links` (widening `subagent_source`, plus `source` if that column exists) — re-create `idx_subagent_links_session_id`, `idx_subagent_links_message_ordinal`, `idx_subagent_links_unique`.
       c) Repeat (a) for `ingest_file_cursors` (widening `source_type`) — re-create its indexes.
       d) Append a NULL-flush step **scoped to own-source rows only** per D-04: `UPDATE sessions SET file_hash = NULL WHERE source = 'qoder'`. Do NOT touch rows for `'openclaw'`, `'claude-code'`, `'codex'`, or `'opencode'`.
    4) Wrap each `db.exec(step.sql)` call in the existing try/catch that tolerates `'duplicate column'` and `'already exists'` so re-runs are idempotent.
    5) After the loop, ensure `db.pragma('user_version = ' + targetVersion)` is set so the migration is not re-attempted.
    6) Confirm the literal CHECK list inside any embedded `CREATE TABLE` strings emitted by earlier migration steps (per PATTERNS.md, lines 250 and 311 are referenced) still match `schema.sql`. If those embedded literals are part of an *earlier* migration version they must NOT be edited (would alter history); only the new version's `*_new` CREATE TABLE strings include `'qoder'`.
    Pattern reference: PATTERNS.md Group 2 / Pattern B; analog file 17-01-PLAN.md T-02.
  </action>
  <verify>
    <automated>pnpm test:run -- ingest/db/migration 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'qoder'" ingest/db/schema.sql` returns at least 3 (one per widened CHECK; 4 if Phase 17 added subagent_links.source).
    - `grep -E "CHECK\(source IN \('openclaw', 'claude-code', 'codex'(, 'opencode')?\)\)" ingest/db/schema.sql` returns 0 matches (i.e. no un-widened CHECK remains for source).
    - `grep -n "targetVersion" ingest/db/index.ts` shows the bumped integer (one greater than its previous value).
    - `grep -c "UPDATE sessions SET file_hash = NULL WHERE source = 'qoder'" ingest/db/index.ts` returns ≥ 1.
    - `grep -E "WHERE source IN \('openclaw', 'claude-code', 'codex'\)" ingest/db/index.ts` against the new migration step returns 0 (D-04: own-source flush only).
    - `pnpm exec tsc --noEmit` exits 0.
    - `pnpm test:run -- ingest/db/migration` exits 0; the suite passes against a fresh DB and against a synthesised v(N-1) DB (covers data-preservation case).
  </acceptance_criteria>
  <done>An existing local SQLite DB migrates with no data loss; INSERT with `source='qoder'` succeeds against every widened CHECK; the migration is idempotent on re-run.</done>
</task>

<task type="auto">
  <name>Task 2: Add positive INSERT-qoder test + updated CHECK literal in tests/unit/ingest/db-migration.test.ts</name>
  <files>tests/unit/ingest/db-migration.test.ts</files>
  <read_first>
    - tests/unit/ingest/db-migration.test.ts (full — line 33 hosts the existing CHECK literal that the test asserts against; PATTERNS.md Group 8 calls this out as exact-mechanical)
    - .planning/phases/17-opencode-source-integration/17-01-PLAN.md T-05 (analog test scaffold for opencode migration)
    - .planning/phases/18-qoder-source-integration/18-SPEC.md Acceptance §2 + §1
  </read_first>
  <action>
    1) Update the existing CHECK-literal expectation at line ~33 (and any sibling assertion lines for `subagent_source` / `source_type`) to include `'qoder'` after the last existing literal — match exactly the new schema.sql.
    2) Add a new positive test "qoder source insert succeeds after migration": open a fresh DB through `runMigrations()`, then `INSERT INTO sessions (id, source, project, status) VALUES ('qoder:test-1','qoder','demo','active')` and assert the row persists.
    3) Add a negative-control test: `INSERT INTO sessions (... source='qoder-typo' ...)` must throw a CHECK constraint error (proves the CHECK is still enforced for unknown values).
    4) Add a data-preservation test: pre-insert a row with `source='codex'` *before* invoking the new migration step (simulate by manually setting `user_version` to (newVersion-1) on a fresh DB, inserting via the older CHECK form, then re-running migrations) — assert the row is still readable after migration.
    5) Add an own-source-only NULL-flush test: pre-insert `source='claude-code'` row with non-NULL `file_hash`, run migration, assert that row's `file_hash` is unchanged (proves D-04).
    Pattern reference: PATTERNS.md Group 8.
  </action>
  <verify>
    <automated>pnpm test:run -- tests/unit/ingest/db-migration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:run -- tests/unit/ingest/db-migration.test.ts` exits 0.
    - Test file contains assertions for at least: (a) `'qoder'` literal in the widened CHECK, (b) successful INSERT with `source='qoder'`, (c) rejected INSERT with `source='qoder-typo'`, (d) preservation of pre-existing rows of other sources, (e) own-source-only NULL-flush behaviour.
    - `grep -c "'qoder'" tests/unit/ingest/db-migration.test.ts` returns ≥ 4.
  </acceptance_criteria>
  <done>Migration test suite enforces both the positive ('qoder' accepted) and negative (other typos rejected) shapes of the widened CHECK and proves D-04's own-source-only flush.</done>
</task>

<task type="auto">
  <name>Task 3: Create docs/skip-cache-naming-debt.md ADR per D-05</name>
  <files>docs/skip-cache-naming-debt.md</files>
  <read_first>
    - .planning/phases/18-qoder-source-integration/18-CONTEXT.md (D-03, D-05 — this doc is the canonical artefact for the technical-debt acknowledgement)
    - .planning/phases/17-opencode-source-integration/17-02-PLAN.md (skip-cache fingerprint precedent — opencode formula `sha256(time_updated + message_count + part_count)`)
    - .planning/phases/18-qoder-source-integration/18-PATTERNS.md Group 9 (Documentation row referencing this file as "NO ANALOG — small ADR-style note")
    - docs/db-schema.md (existing schema reference doc — establishes tone/structure; do not modify)
    - docs/CONFIGURATION.md (cross-reference target)
  </read_first>
  <action>
    Create a short standalone Markdown doc (~30-60 lines) with these sections — content, not formatting, is what matters:
    1) **Title** — `# Skip-Cache Naming Debt`.
    2) **Status** — `Acknowledged debt; deferred rename` with date 2026-05-18.
    3) **Background** — explain that `sessions.file_hash` (added in v1.0 for JSONL sources) is now reused as a generic per-source skip key; the column name no longer matches its semantics for SQLite-backed sources.
    4) **Per-source fingerprint algorithms** — table with columns `source | algorithm | input fields`:
       - `openclaw` / `claude-code` / `codex` → `sha256(file content bytes)` over the JSONL file.
       - `opencode` → `sha256("opencode:<session_id>:<time_updated>:<message_count>:<part_count>")` (per Phase 17 D-02).
       - `qoder` → `sha256("qoder-session-v1:<session_id>:<gmt_modified>:<message_count>:<max_message_gmt>")` (per Phase 18 D-03). State explicitly: whole-DB hash MUST NOT be used (would invalidate every session on any IDE write).
    5) **Why reuse the column** — D-03 rationale: aligns with Phase 17's pattern; skip comparison path is identical for all sources (`SELECT file_hash FROM sessions WHERE id = ?`); avoids a churn-heavy schema change.
    6) **Future rename path** — outline migration to rename `sessions.file_hash` → `sessions.source_skip_key` once all sources adopt the pattern: replacement-table copy, drop view dependents, recreate indexes; rename is deferred and tracked under FUT.
    7) **Cross-references** — link to `docs/db-schema.md` (column origin), `docs/services/ingest.md` (skip-cache mechanism), Phase 17/18 PLAN files.
    Pattern reference: PATTERNS.md Group 9 (no structural analog — write as short ADR).
  </action>
  <verify>
    <automated>test -f docs/skip-cache-naming-debt.md && grep -c "qoder-session-v1" docs/skip-cache-naming-debt.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/skip-cache-naming-debt.md` exists.
    - `grep -c "qoder-session-v1:" docs/skip-cache-naming-debt.md` returns ≥ 1 (Qoder formula present verbatim).
    - `grep -Ec "openclaw|claude-code|codex|opencode|qoder" docs/skip-cache-naming-debt.md` returns ≥ 5 (per-source table covers all five sources).
    - `grep -ci "rename" docs/skip-cache-naming-debt.md` returns ≥ 1 (future rename path documented).
    - `wc -l docs/skip-cache-naming-debt.md` reports between 25 and 120 lines (not a stub, not a sprawl).
  </acceptance_criteria>
  <done>The doc is committed at `docs/skip-cache-naming-debt.md`; subsequent plans can `@docs/skip-cache-naming-debt.md` as the canonical reference for the column-name debt.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` exits 0.
- `pnpm test:run -- ingest/db/migration` and `pnpm test:run -- tests/unit/ingest/db-migration` both exit 0.
- A pre-migration DB (containing rows from each prior source) migrates cleanly: `node -e "require('./ingest/db').runMigrations()"` followed by `sqlite3 data/ingest.db "SELECT source, COUNT(*) FROM sessions GROUP BY source"` shows preserved row counts.
- `sqlite3 <fresh-db> "INSERT INTO sessions (id,source,project,status) VALUES ('qoder:demo','qoder','x','active')"` succeeds.
- `grep -RE "'qoder'" ingest/db/` returns hits in both schema.sql and index.ts.
</verification>

<success_criteria>
1. Schema CHECK constraints accept `'qoder'` on `sessions.source`, `subagent_links.subagent_source`, and `ingest_file_cursors.source_type` (and `subagent_links.source` if that column has been introduced by Phase 17 in the rebased base).
2. Existing rows for the four prior sources are preserved across the migration.
3. `sessions.file_hash` for `source='qoder'` rows (if any pre-existed) is NULL after migration; rows for other sources are not touched.
4. Migration is idempotent on re-run (try/catch swallows "already exists"/"duplicate column").
5. `docs/skip-cache-naming-debt.md` exists and documents the per-source fingerprint algorithms and deferred rename plan.
6. Plans 18-03 and 18-04 can subsequently insert/query `source='qoder'` rows without CHECK violations.
</success_criteria>

<output>
Create `.planning/phases/18-qoder-source-integration/18-01-SUMMARY.md` recording: bumped migration version number, list of widened CHECKs, exact NULL-flush WHERE clause, and a one-line note on whether Phase 17 had already landed in the rebased base (this informs Plan 18-03's expectations).
</output>
