---
phase: 02-local-ingest-core-openclaw-parser
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - package.json
  - tsconfig.json
  - ingest/tsconfig.json
  - README.md
autonomous: true
requirements:
  - DATA-01
user_setup:
  - service: development
    why: "Set up development workflow to run both Next.js and ingest service concurrently"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "Single pnpm dev command starts both Next.js and ingest service"
    - "Both services log output to same terminal"
    - "Services can be stopped together with Ctrl+C"
    - "TypeScript compilation works for both Next.js and ingest code"
    - "Project documentation explains development setup"
  artifacts:
    - path: "package.json"
      provides: "Updated dev scripts with concurrently"
      contains: "\"dev\": \"concurrently\""
    - path: "tsconfig.json"
      provides: "Root TypeScript config including ingest/ directory"
      contains: "ingest/**/*"
    - path: "ingest/tsconfig.json"
      provides: "Ingest-specific TypeScript config"
      min_lines: 15
    - path: "README.md"
      provides: "Development setup documentation"
      contains: "pnpm dev"
  key_links:
    - from: "package.json"
      to: "ingest/index.ts"
      via: "dev script starts ingest service"
      pattern: "concurrently.*node.*ingest"
    - from: "tsconfig.json"
      to: "ingest/tsconfig.json"
      via: "TypeScript project references"
      pattern: "references.*\\[.*{.*path.*ingest"

---

<objective>
Set up development workflow to run both Next.js frontend and ingest service concurrently with a single command.

Purpose: Enable convenient development where both services start together, logs are visible in one terminal, and both can be stopped cleanly. Per D-10, use concurrently to manage both processes.

Output: Single `pnpm dev` command that starts Next.js on port 3000 and ingest service on port 8078, with proper TypeScript compilation for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

@.planning/phases/02-local-ingest-core-openclaw-parser/02-01-PLAN.md
@package.json
@tsconfig.json
</execution_context>

<context>
<interfaces>
<!-- From package.json - Current scripts configuration -->
Current dev script:
```json
"dev": "node server/index.mjs"
```

Current build script:
```json
"build": "next build"
```
</interfaces>

<implementation_notes>
Per 02-CONTEXT.md D-10: Use concurrently to run Next.js and ingest service in a single terminal via pnpm dev.

Required setup:
1. Install concurrently as dev dependency
2. Update package.json scripts to use concurrently
3. Create ingest/tsconfig.json for ingest-specific TypeScript config
4. Update root tsconfig.json to include ingest/ directory
5. Add TypeScript build script for ingest/ (optional, can use ts-node in dev)
6. Update README.md with development setup instructions

Development workflow options:
- Option A: Use ts-node for ingest in dev (faster startup, no build step)
- Option B: Build ingest to JS then run (slower startup, production-like)
- Option C: Use tsc --watch for ingest (incremental compilation)

Recommendation: Option A (ts-node) for development speed. Add ts-node-dev for hot reloading if needed.

Concurrently configuration:
- Name: "next" and "ingest" for clear log prefixes
- Prefix colors: blue for Next.js, green for ingest
- Kill others on exit (default behavior)

Port configuration:
- Next.js: 3000 (default)
- Ingest: 8078 (per D-11)

TypeScript configuration:
- Root tsconfig.json references ingest/tsconfig.json
- Ingest tsconfig.json extends root config
- Both use same strict mode settings
- Shared types (types/trace.ts) accessible from both

README updates:
- Add Development section
- Explain pnpm dev command
- Document both services and ports
- Add troubleshooting for common issues (port conflicts, WORKSPACE_PATH)
</implementation_notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install concurrently and ts-node for development workflow</name>
  <files>package.json</files>
  <read_first>
    - package.json (current dependencies and scripts)
  </read_first>
  <action>
Add dev dependencies to package.json:
- concurrently (latest) - Run multiple commands concurrently
- ts-node (latest) - TypeScript execution for ingest service
- tsx (latest) - Faster TypeScript execution (alternative to ts-node)

Update scripts section:
```json
"scripts": {
  "dev": "concurrently \"npm:dev:*\" --names \"NEXT,INGEST\" --prefix-colors \"blue,green\"",
  "dev:next": "node server/index.mjs",
  "dev:ingest": "tsx watch ingest/index.ts",
  "build": "next build",
  "build:ingest": "tsc -p ingest/tsconfig.json",
  "start": "NODE_ENV=production node server/index.mjs",
  "start:ingest": "NODE_ENV=production node ingest/dist/index.js",
  "lint": "eslint",
  "typecheck": "tsc --noEmit",
  "typecheck:ingest": "tsc --noEmit -p ingest/tsconfig.json",
  "test": "vitest",
  "test:run": "vitest run"
}
```

Script explanations:
- dev: Runs both dev:next and dev:ingest concurrently with colored prefixes
- dev:next: Starts Next.js development server
- dev:ingest: Starts ingest service with tsx watch (auto-restart on file changes)
- build:ingest: Compiles ingest TypeScript to JavaScript
- start:ingest: Runs compiled ingest service for production
- typecheck:ingest: Type-check ingest code only

Run: pnpm install to add dependencies
  </action>
  <verify>
    <automated>grep -E "concurrently|ts-node|tsx" package.json | wc -l | xargs test 3 -eq</automated>
    <automated>grep "dev:next\|dev:ingest" package.json | wc -l | xargs test 2 -eq</automated>
    <automated>grep "concurrently.*npm:dev:\*" package.json | wc -l | xargs test 1 -eq</automated>
  </verify>
  <done>
- concurrently, ts-node, tsx added to devDependencies
- dev script starts both services with colored prefixes
- dev:next starts Next.js on port 3000
- dev:ingest starts ingest with tsx watch on port 8078
- build:ingest compiles ingest TypeScript
- typecheck:ingest validates ingest code
- All scripts added to package.json
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ingest-specific TypeScript configuration</name>
  <files>ingest/tsconfig.json</files>
  <read_first>
    - tsconfig.json (root TypeScript config)
  </read_first>
  <action>
Create ingest/tsconfig.json:

```json
{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./",
    "composite": true,
    "tsBuildInfoFile": "./.tsbuildinfo"
  },
  "include": [
    "./**/*.ts"
  ],
  "exclude": [
    "./dist",
    "node_modules"
  ],
  "references": [
    { "path": "../" }
  ]
}
```

Configuration details:
- Extends root tsconfig.json (inherits strict mode, path aliases)
- outDir: ./dist for compiled JavaScript output
- rootDir: ./ to include all ingest source files
- composite: true for project references (enables incremental builds)
- include: All .ts files in ingest/
- exclude: Build output and node_modules
- references: References root project for shared types (types/trace.ts)

This enables:
- Incremental compilation with tsBuildInfoFile
- Project references for type checking across packages
- Shared @/* path aliases from root config
  </action>
  <verify>
    <automated>grep -c "\"extends\"" ingest/tsconfig.json | xargs test 1 -eq</automated>
    <automated>grep "\"outDir\|\"rootDir\|\"composite\"" ingest/tsconfig.json | wc -l | xargs test 3 -eq</automated>
    <automated>grep "\"references\"" ingest/tsconfig.json | wc -l | xargs test 1 -eq</automated>
  </verify>
  <done>
- ingest/tsconfig.json extends root config
- Output directory set to ./dist
- Composite mode enabled for project references
- Include/exclude patterns correct
- References root project for shared types
- TypeScript can compile ingest/ directory
  </done>
</task>

<task type="auto">
  <name>Task 3: Update root TypeScript configuration to include ingest directory</name>
  <files>tsconfig.json</files>
  <read_first>
    - tsconfig.json (current root config)
    - ingest/tsconfig.json (ingest config just created)
  </read_first>
  <action>
Update tsconfig.json to include ingest/ and add project references:

1. Add "ingest/**/*" to include array if not present
2. Add references array for project references

Updated tsconfig.json should have:
```json
{
  "compilerOptions": {
    // ... existing options ...
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    "ingest/**/*"
  ],
  "exclude": [
    "node_modules",
    ".next",
    "ingest/dist"
  ],
  "references": [
    { "path": "./ingest" }
  ]
}
```

Changes:
- Add "ingest/**/*" to include (types/trace.ts accessible from ingest/)
- Add "ingest/dist" to exclude (don't type-check compiled output)
- Add references array with ingest project reference

This enables:
- Root type-check includes both Next.js and ingest code
- Project references for incremental compilation
- Shared @/* path aliases work across both packages
  </action>
  <verify>
    <automated>grep "ingest/\*\*/\*" tsconfig.json | wc -l | xargs test 1 -eq</automated>
    <automated>grep "\"references\"" tsconfig.json | wc -l | xargs test 1 -eq</automated>
    <automated>grep "ingest/dist" tsconfig.json | wc -l | xargs test 1 -eq</automated>
  </verify>
  <done>
- ingest/**/* added to include array
- ingest/dist added to exclude array
- references array added with ingest project
- TypeScript can resolve imports between packages
- Shared types/trace.ts accessible from both Next.js and ingest
  </done>
</task>

<task type="auto">
  <name>Task 4: Update README with development setup instructions</name>
  <files>README.md</files>
  <read_first>
    - README.md (current documentation)
  </read_first>
  <action>
Update README.md to add Development section:

```markdown
## Development

### Prerequisites

- Node.js 20+ and pnpm
- OpenClaw workspace (if testing OpenClaw source)

### Starting Development Environment

The project consists of two services:
- **Next.js Frontend** (port 3000) - Web dashboard
- **Ingest Service** (port 8078) - Session parsing and API

Start both services with a single command:

\`\`\`bash
pnpm dev
\`\`\`

This will start:
- Next.js on http://localhost:3000
- Ingest service on http://localhost:8078

Both services run concurrently with colored log prefixes:
- \`[NEXT]\` - Next.js frontend logs
- \`[INGEST]\` - Ingest service logs

Stop both services with \`Ctrl+C\`.

### Individual Service Control

Start only Next.js:
\`\`\`bash
pnpm run dev:next
\`\`\`

Start only ingest service:
\`\`\`bash
pnpm run dev:ingest
\`\`\`

### Configuration

#### Ingest Service

Configure OpenClaw source discovery:

\`\`\`bash
export WORKSPACE_PATH=/path/to/openclaw/workspace
pnpm run dev:ingest
\`\`\`

Or set in \`.env.local\`:
\`\`\`bash
WORKSPACE_PATH=/path/to/openclaw/workspace
INGEST_PORT=8078
INGEST_DB_PATH=./data/ingest.db
\`\`\`

#### API Endpoints

Ingest service provides:
- \`GET /health\` - Service health check
- \`GET /version\` - Service version and info
- \`GET /api/v1/sources\` - List discovered sources
- \`POST /api/v1/sources/:type/sync\` - Trigger source sync
- \`GET /api/v1/sessions\` - List sessions
- \`GET /api/v1/sessions/:id\` - Get session details
- \`GET /api/v1/sessions/:id/turns\` - Get session turns
- \`GET /api/v1/sessions/:id/messages\` - Get session messages

See [API Documentation](docs/api.md) for full endpoint reference.

### Troubleshooting

**Port 3000 already in use:**
- Kill process using port 3000: \`lsof -ti:3000 | xargs kill\`
- Or change Next.js port: set \`PORT=3001 pnpm dev:next\`

**Port 8078 already in use:**
- Kill process using port 8078: \`lsof -ti:8078 | xargs kill\`
- Or change ingest port: set \`INGEST_PORT=3001 pnpm dev:ingest\`

**WORKSPACE_PATH not configured:**
- Ingest service will start but OpenClaw source discovery will fail
- Set WORKSPACE_PATH env var or pass via command line

**TypeScript errors:**
- Run type check: \`pnpm typecheck\`
- Check ingest types: \`pnpm typecheck:ingest\`
- Ensure \`pnpm install\` has been run

**Database errors:**
- Ensure \`./data/\` directory exists and is writable
- Delete \`data/ingest.db\` and restart to reinitialize schema
```

Add this section after any existing content in README.md. If no README exists, create one with this content plus basic project description.
  </action>
  <verify>
    <automated>grep "## Development" README.md | wc -l | xargs test 1 -eq</automated>
    <automated>grep "pnpm dev" README.md | wc -l | xargs test 1 -ge</automated>
    <automated>grep "port 8078\|port 3000" README.md | wc -l | xargs test 2 -ge</automated>
  </verify>
  <done>
- Development section added to README
- pnpm dev command documented
- Both services and ports explained
- Individual service control documented
- Configuration section added (WORKSPACE_PATH, env vars)
- API endpoints listed
- Troubleshooting section covers common issues
- Port conflict resolution provided
- TypeScript error troubleshooting included
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete development workflow with single pnpm dev command starting both services</what-built>
  <how-to-verify>
1. Stop any running services (Ctrl+C or kill processes)
2. Start development environment:
   - `pnpm dev`
3. Verify both services start:
   - Look for [NEXT] prefix with Next.js startup logs
   - Look for [INGEST] prefix with ingest service startup logs
   - Should see "Ingest service started on port 8078"
   - Should see "Ready on http://localhost:3000" (or similar)
4. Verify Next.js is running:
   - Open http://localhost:3000 in browser
   - Should see the dashboard (or error page if routes not set up)
5. Verify ingest service is running:
   - `curl http://localhost:8078/health`
   - Should return: {"status":"ok","version":"0.1.0",...}
6. Check log prefixes:
   - Next.js logs should have [NEXT] prefix in blue
   - Ingest logs should have [INGEST] prefix in green
7. Test auto-restart (ts-node watch):
   - Make a small change to ingest/index.ts (e.g., add a comment)
   - Save file
   - Should see ingest service restart automatically
   - Verify it still responds to /health endpoint
8. Stop both services:
   - Press Ctrl+C
   - Both services should stop
   - Verify no processes left on ports 3000 or 8078
9. Test individual service commands:
   - `pnpm run dev:next` - Should start only Next.js
   - Stop with Ctrl+C
   - `pnpm run dev:ingest` - Should start only ingest
   - Stop with Ctrl+C
10. Verify TypeScript compilation:
    - `pnpm typecheck` - Should pass with no errors
    - `pnpm typecheck:ingest` - Should check only ingest code
11. Test build:
    - `pnpm run build:ingest` - Should compile ingest to ./dist
    - Check that ingest/dist/index.js exists
  </how-to-verify>
  <resume-signal>Type "approved" if pnpm dev starts both services, log prefixes work correctly, auto-restart works, and both services can be stopped together. Describe any issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → Development Environment | Local development workflow with file system access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | S | Development ports (3000, 8078) | accept | Local-only binding (localhost). No exposure to external network. |
| T-02-18 | E | File watcher (tsx watch) | mitigate | Watches only project directory. Ignores node_modules. No risk of watching malicious paths outside project. |
| T-02-19 | I | Environment variables | accept | Developer controls environment. No user input involved. Same security model as existing development workflow. |

No new security risks introduced. Development workflow follows standard Node.js patterns.
</threat_model>

<verification>
## Phase Verification

- [ ] pnpm dev starts both Next.js and ingest service
- [ ] Log prefixes ([NEXT], [INGEST]) visible and colored
- [ ] Next.js runs on port 3000
- [ ] Ingest service runs on port 8078
- [ ] Both services respond to HTTP requests
- [ ] Ctrl+C stops both services
- [ ] No orphaned processes after stop
- [ ] pnpm run dev:next starts only Next.js
- [ ] pnpm run dev:ingest starts only ingest
- [ ] tsx watch auto-restarts ingest on file changes
- [ ] TypeScript compilation succeeds for both packages
- [ ] pnpm typecheck validates all code
- [ ] pnpm typecheck:ingest validates ingest code
- [ ] pnpm run build:ingest compiles to ./dist
- [ ] README.md documents development workflow
- [ ] Troubleshooting section covers common issues
</verification>

<success_criteria>
1. **Single Command**: pnpm dev starts both services with proper logging
2. **Auto-restart**: File changes trigger automatic restart (tsx watch)
3. **Type Safety**: TypeScript compilation works for both Next.js and ingest code
4. **Documentation**: README explains setup, configuration, and troubleshooting
5. **Developer Experience**: Clear log prefixes, easy startup/shutdown, individual service control
</success_criteria>

<output>
After completion, create `.planning/phases/02-local-ingest-core-openclaw-parser/02-04-SUMMARY.md`
</output>
