---
phase: 15-ingest-sync-performance-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 15-01-PLAN.md
  - 15-02-PLAN.md
files_modified:
  - ingest/index.ts
  - ingest/api/overview.ts
  - ingest/api/sources.ts
  - ingest/src/sync-scheduler.ts
  - ingest/sync/index.ts
  - ingest/db/index.ts
  - ingest/db/schema.sql
  - tests/integration/ingest/api.test.ts
  - tests/unit/ingest/db-migration.test.ts
  - tests/unit/ingest/sync.test.ts
autonomous: true
requirements:
  - PERF-101
  - PERF-103
  - PERF-106
  - OPEN-103
  - TEST-103
---

<objective>
Make the repair observable and regression-safe: expose real sync scheduler state, add metrics for skipped/parsed files, prepare cursor fields only if needed for incremental follow-up, and run the ingest regression gate.
</objective>

<context>
@.planning/phases/15-ingest-sync-performance-hardening/15-CONTEXT.md
@.planning/phases/15-ingest-sync-performance-hardening/15-RESEARCH.md
@.planning/phases/15-ingest-sync-performance-hardening/15-VALIDATION.md
@.planning/debug/ingest-memory-performance-fix-policy.md
@ingest/index.ts
@ingest/api
@ingest/db/index.ts
@ingest/db/schema.sql
@ingest/sync/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose scheduler and sync metrics through health/debug status</name>
  <files>ingest/index.ts, ingest/api/overview.ts, ingest/src/sync-scheduler.ts</files>
  <read_first>
    - ingest/index.ts
    - ingest/api/overview.ts
    - ingest/src/sync-scheduler.ts
  </read_first>
  <action>
Extend health/debug output so operators can distinguish watcher state from sync state.

Required fields:

- `sync.active`
- `sync.activeReason`
- `sync.activeScope`
- `sync.queued`
- `sync.queuedReasons`
- `sync.startedAt`
- `sync.lastCompletedAt`
- `sync.lastDurationMs`
- `sync.lastError`
- `sync.filesSkippedBeforeParse`
- `sync.filesParsed`

Keep the existing health response backward compatible where practical. If adding a new debug endpoint is safer than changing `/health`, keep `/health` stable and add the richer payload under a clearly named route.
  </action>
  <acceptance_criteria>
    - Health/debug tests prove active and queued scheduler state is returned.
    - Existing frontend/BFF callers of health do not break.
    - The response can no longer report only `phase: idle` while scheduler active is true.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add sync metrics plumbing</name>
  <files>ingest/sync/index.ts, ingest/src/sync-scheduler.ts, tests/unit/ingest/sync.test.ts</files>
  <read_first>
    - ingest/sync/index.ts
    - ingest/src/sync-scheduler.ts
  </read_first>
  <action>
Track the minimum metrics needed to validate performance behavior.

Required metrics:

- files considered
- files skipped before parse
- files parsed
- largest file size seen
- sync reason and scope
- duration

These metrics can live in scheduler status, sync result extensions, or a small internal stats object. Preserve existing `SyncResult` consumers or update them safely.
  </action>
  <acceptance_criteria>
    - Tests prove skipped-before-parse and parsed counts update correctly.
    - Metrics are reset per sync run and last-run values remain readable after completion.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Decide incremental cursor scope and add only safe schema if needed</name>
  <files>ingest/db/index.ts, ingest/db/schema.sql, tests/unit/ingest/db-migration.test.ts</files>
  <read_first>
    - ingest/db/index.ts
    - ingest/db/schema.sql
    - .planning/debug/ingest-memory-performance-fix-policy.md
  </read_first>
  <action>
Evaluate whether this phase needs DB cursor fields for follow-up append-only JSONL parsing.

If P0/P1 results satisfy incident criteria without cursor schema, do not add schema churn. Document the deferral in `15-SUMMARY.md` during execution.

If implementation needs cursor fields now, add an additive migration only:

- `last_indexed_offset`
- `last_indexed_ordinal`
- `file_inode`
- `file_device`
- `parser_version`

Do not implement broad delete/insert rewrite changes in this task. Full append-only DB upsert is a separate follow-up if it expands beyond this phase.
  </action>
  <acceptance_criteria>
    - Either no schema change is made and the reason is documented, or additive migration tests pass.
    - Existing DBs still migrate without manual deletion.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Run final ingest regression gate</name>
  <files>tests/unit/ingest, tests/integration/ingest, ingest/src</files>
  <read_first>
    - .planning/phases/15-ingest-sync-performance-hardening/15-VALIDATION.md
  </read_first>
  <action>
Run the targeted and broad ingest test gates. Fix regressions in the touched scope.
  </action>
  <acceptance_criteria>
    - Watcher tests pass.
    - Scheduler tests pass.
    - Sync tests pass.
    - Codex relationship tests pass.
    - Ingest API integration tests pass or any environment limitation is documented.
    - `pnpm typecheck:ingest` passes.
  </acceptance_criteria>
</task>

</tasks>

<verify>
  <automated>pnpm vitest run ingest/src/watcher.test.ts tests/unit/ingest/sync-scheduler.test.ts tests/unit/ingest/sync.test.ts tests/unit/ingest/codex-relationships.test.ts tests/unit/ingest/db-migration.test.ts tests/integration/ingest/api.test.ts</automated>
  <automated>pnpm typecheck:ingest</automated>
</verify>

<done>
Phase 15 complete: high-memory sync amplification has scheduler protection, path-scoped sync, pre-parse skip, streaming-safe hash behavior, and health/debug visibility.
</done>
