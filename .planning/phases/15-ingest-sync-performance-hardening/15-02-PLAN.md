---
phase: 15-ingest-sync-performance-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01-PLAN.md
files_modified:
  - ingest/sync/index.ts
  - tests/unit/ingest/sync.test.ts
  - tests/unit/ingest/codex-relationships.test.ts
autonomous: true
requirements:
  - PERF-102
  - PERF-104
  - PERF-105
  - TEST-103
---

<objective>
Reduce the cost of each sync run: changed-path sync must avoid scanning whole source history, unchanged historical files must skip before parser work, and large file hashing must not use whole-file reads.
</objective>

<context>
@.planning/phases/15-ingest-sync-performance-hardening/15-CONTEXT.md
@.planning/phases/15-ingest-sync-performance-hardening/15-RESEARCH.md
@.planning/debug/ingest-memory-performance-fix-policy.md
@ingest/sync/index.ts
@tests/unit/ingest/sync.test.ts
@tests/unit/ingest/codex-relationships.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add path-scoped sync API</name>
  <files>ingest/sync/index.ts, tests/unit/ingest/sync.test.ts</files>
  <read_first>
    - ingest/sync/index.ts
    - ingest/config/tool-dirs.ts
    - ingest/sync/sources.ts
  </read_first>
  <action>
Add `syncPaths(paths, options?)` or an equivalent exported API consumed by the scheduler.

Required behavior:

- classify each path to `openclaw`, `claude-code`, or `codex` based on configured source roots and file naming rules
- ignore non-session files by default
- parse only matched changed session files
- preserve source-specific project extraction
- aggregate and return the same `SyncResult` shape used by `syncSource`
- emit SSE and update sync status consistently with existing full sync behavior

Refactor common per-candidate parse/write logic if needed, but keep the patch focused.
  </action>
  <acceptance_criteria>
    - `syncPaths([changedCodexJsonl])` calls `parseCodexSession` once and does not collect every Codex candidate.
    - `syncPaths([nonSessionMdOrJson])` returns no parser work and no error.
    - Existing `syncSource(sourceType)` behavior remains available for explicit full sync.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add pre-parse skip for unchanged files</name>
  <files>ingest/sync/index.ts, tests/unit/ingest/sync.test.ts</files>
  <read_first>
    - ingest/sync/index.ts
    - ingest/db/schema.sql
  </read_first>
  <action>
Move skip logic before parser work.

Required behavior:

- stat candidate file before parsing
- look up existing session by `file_path` when available
- skip before parser if `file_size`, `file_mtime`, and parser cache version indicate unchanged
- preserve `force=true` to bypass skip
- count skipped files for Plan 03 observability
- if session cannot be resolved by `file_path`, fall back to existing parser/write behavior

Do not remove the existing DB-level safety in `writeSessionToDatabase()` until tests prove pre-parse skip covers the needed cases.
  </action>
  <acceptance_criteria>
    - Unchanged file test proves parser mock is not called.
    - `force=true` test proves parser mock is called even when metadata matches.
    - Changed mtime or size test proves parser mock is called.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Replace whole-file hash reads and guard Codex relationship work</name>
  <files>ingest/sync/index.ts, tests/unit/ingest/codex-relationships.test.ts</files>
  <read_first>
    - ingest/sync/index.ts
    - tests/unit/ingest/codex-relationships.test.ts
  </read_first>
  <action>
Remove `fs.readFileSync(filePath)` from the hot hash path.

Required behavior:

- replace `computeFileHash()` with an async streaming implementation, or avoid hash calculation for files skipped by metadata
- ensure full sync remains deterministic
- ensure path-scoped Codex sync does not run `collectCodexRelationships(sources)` across all Codex files for every changed file
- keep relationship backfill behavior for explicit full Codex sync
- document any deliberate relationship tradeoff in code comments near the changed path
  </action>
  <acceptance_criteria>
    - Static test/source assertion shows `computeFileHash` no longer uses `fs.readFileSync`.
    - Path-scoped Codex sync does not call full `collectCodexRelationships`.
    - Existing Codex relationship tests still pass for full sync/backfill behavior.
  </acceptance_criteria>
</task>

</tasks>

<verify>
  <automated>pnpm vitest run tests/unit/ingest/sync.test.ts tests/unit/ingest/codex-relationships.test.ts</automated>
  <automated>pnpm typecheck:ingest</automated>
</verify>

<done>
P1 complete: changed-path sync is scoped, unchanged files skip before parser allocation, and large file hashing no longer performs whole-file reads on the hot path.
</done>
