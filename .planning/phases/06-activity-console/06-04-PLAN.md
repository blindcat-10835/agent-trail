---
phase: 06-activity-console
plan: 06-04-PLAN
type: execute
wave: 4
depends_on: [06-01-PLAN, 06-02-PLAN, 06-03-PLAN]
files_modified:
  - components/dashboard/overview-tab.tsx
  - components/dashboard/dashboard-right-rail.tsx
  - components/dashboard/sidebar-nav.tsx
  - app/(shell)/dashboard/page.tsx
autonomous: true
requirements:
  - ACTV-02

must_haves:
  truths:
    - "Overview tab's ALERTS section is replaced with ACTIVITY section showing Top 10 log entries"
    - "Activity section shows 'View Activity Console' link that navigates to /activity"
    - "Right rail's ALERTS tab is replaced with ACTIVITY tab showing Top 10 log entries"
    - "ACTIVITY tab shows log entry count badge when unacked errors exist"
    - "Sidebar navigation's ALR button is replaced with ACT button"
    - "ACT button navigates to /activity page (not /dashboard?tab=alerts)"
    - "All modified components retain existing functionality for other tabs/sections"
  artifacts:
    - path: "components/dashboard/overview-tab.tsx"
      provides: "Overview tab with ACTIVITY section replacing ALERTS"
      min_lines: 50 (modified)
    - path: "components/dashboard/dashboard-right-rail.tsx"
      provides: "Right rail with ACTIVITY tab replacing ALERTS"
      min_lines: 60 (modified)
    - path: "components/dashboard/sidebar-nav.tsx"
      provides: "Sidebar nav with ACT button replacing ALR"
      min_lines: 25 (modified)
  key_links:
    - from: "components/dashboard/overview-tab.tsx"
      to: "/activity"
      via: "router.push('/activity')"
      pattern: "router\\.push.*activity"
    - from: "components/dashboard/sidebar-nav.tsx"
      to: "/activity"
      via: "href='/activity'"
      pattern: "href.*activity"
    - from: "components/dashboard/dashboard-right-rail.tsx"
      to: "types/activity.ts"
      via: "import type { LogEntry }"
      pattern: "import.*LogEntry.*from.*types/activity"
---

# Dashboard Integration — Replace ALERT with ACTIVITY

## Objective

Replace the existing ALERT system with the new Activity Console across the Dashboard: update the Overview tab to show Top 10 Activity entries instead of alerts, replace the Right Rail's ALERTS tab with ACTIVITY tab, and update the Sidebar navigation to replace ALR with ACT (linking to `/activity` page).

**Purpose**: Complete the migration from simple ALERT system to comprehensive Activity Console, ensuring users can access activity logs from multiple entry points in the Dashboard.

**Output**: Dashboard components updated to use Activity Console instead of ALERTS, with proper navigation to the `/activity` page.

## Execution Context

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## Context

@.planning/phases/06-activity-console/DATA-RESEARCH.md
@.planning/phases/06-activity-console/06-03-PLAN.md
@.planning/phases/04-agent-dashboard/04-01-PLAN.md
@/Users/ebbi/Work/openclaw-projects/ovao/components/dashboard/overview-tab.tsx
@/Users/ebbi/Work/openclaw-projects/ovao/components/dashboard/dashboard-right-rail.tsx
@/Users/ebbi/Work/openclaw-projects/ovao/components/dashboard/sidebar-nav.tsx
@/Users/ebbi/Work/openclaw-projects/ovao/types/activity.ts

## Tasks

<task type="auto">
  <name>Task 1: Replace ALERTS section with ACTIVITY in Overview tab</name>
  <files>components/dashboard/overview-tab.tsx</files>
  <read_first>
    components/dashboard/overview-tab.tsx
    app/(shell)/activity/page.tsx
  </read_first>
  <action>
    Modify `components/dashboard/overview-tab.tsx` to replace the ALERTS section (lines ~258-280) with ACTIVITY section:

    1. **Remove** the existing ALERTS section:
       - Delete the `<Section title="ALERTS · RECENT">` block
       - Remove `unackedAlerts` and `criticalAlerts` calculations (lines 96-97)
       - Remove `alerts` from `useGatewayStore` selectors

    2. **Add ACTIVITY data fetching**:
       - Add state for activity logs:
         ```typescript
         const [activityLogs, setActivityLogs] = useState<LogEntry[]>([])
         const [activityLoading, setActivityLoading] = useState(true)
         ```
       - Add `useEffect` to fetch from `/api/logs` on mount:
         ```typescript
         useEffect(() => {
           fetch('/api/logs')
             .then(r => r.json())
             .then((data: { entries: LogEntry[] }) => {
               setActivityLogs(data.entries.slice(0, 10)) // Top 10
               setActivityLoading(false)
             })
             .catch(() => setActivityLoading(false))
         }, [])
         ```
       - Add import: `import type { LogEntry } from '@/types/activity'`

    3. **Create ACTIVITY section** (replace ALERTS section):
       - Title: "ACTIVITY · RECENT"
       - Meta: Show error count if > 0, else "all clear"
       - Show Top 10 log entries:
         - Status dot (same colors as LogBrowser: destructive for error, accent for info, blue for cron)
         - Summary text (truncate to 80 chars)
         - Timestamp (use `fmtAgo()` helper)
         - Source badge (small, "CRON" or "CONFIG")
       - Bottom link: "View Activity Console →" (click navigates to `/activity`)

    4. **Navigation**:
       - Add `useRouter` import: `import { useRouter } from 'next/navigation'`
       - Add const: `const router = useRouter()`
       - Link handler: `onClick={() => router.push('/activity')}`

    5. **Loading state**:
       - Show 3 skeleton rows while `activityLoading` is true

    6. **Empty state**:
       - Show "No recent activity" if `activityLogs.length === 0`

    **Keep all other sections unchanged** (Agents, Channels, Cron, Providers, Skills).
  </action>
  <verify>
    <automated>
      grep -q "ACTIVITY · RECENT" components/dashboard/overview-tab.tsx && \
      grep -q "View Activity Console" components/dashboard/overview-tab.tsx && \
      grep -q "router.push.*activity" components/dashboard/overview-tab.tsx && \
      grep -q "LogEntry" components/dashboard/overview-tab.tsx && \
      ! grep -q "ALERTS · RECENT" components/dashboard/overview-tab.tsx
    </automated>
  </verify>
  <done>
    Overview tab's ALERTS section is replaced with ACTIVITY section showing Top 10 log entries with "View Activity Console" link. Navigation works correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace ALERTS tab with ACTIVITY in Right Rail</name>
  <files>components/dashboard/dashboard-right-rail.tsx</files>
  <read_first>
    components/dashboard/dashboard-right-rail.tsx
    app/(shell)/activity/page.tsx
  </read_first>
  <action>
    Modify `components/dashboard/dashboard-right-rail.tsx` to replace the ALERTS tab with ACTIVITY tab:

    1. **Update tab list** (line ~44):
       - Change `{ id: 'alerts' as const, label: 'ALERTS' }` to `{ id: 'activity' as const, label: 'ACTIVITY' }`
       - Update `RailTab` type: `type RailTab = 'feed' | 'activity' | 'providers'`

    2. **Remove ALERTS-specific code**:
       - Remove `selectAlertsState` import and usage
       - Remove `alerts` and `unackedCount` calculations
       - Remove `ackAlert` function from store

    3. **Add ACTIVITY tab content** (replace `{tab === 'alerts' && ...}` block):
       - Fetch activity logs on component mount:
         ```typescript
         const [activityLogs, setActivityLogs] = useState<LogEntry[]>([])
         useEffect(() => {
           fetch('/api/logs')
             .then(r => r.json())
             .then((data: { entries: LogEntry[] }) => {
               setActivityLogs(data.entries.slice(0, 10))
             })
         }, [])
         ```
       - Add import: `import type { LogEntry } from '@/types/activity'`

    4. **Render ACTIVITY tab** (similar to ALERTS layout):
       - Show Top 10 log entries
       - Each entry shows:
         - Status dot (color by level)
         - Summary text (truncate to 100 chars)
         - Metadata: source + time ago
       - Error count badge in tab header (if `level === 'error'` count > 0)
       - Show "No recent activity" if empty

    5. **Tab header badge**:
       - Calculate error count: `const errorCount = activityLogs.filter(e => e.level === 'error').length`
       - Show badge in tab label if `errorCount > 0`:
         ```typescript
         {t.id === 'activity' && errorCount > 0 && (
           <span className="ml-1 bg-destructive text-background px-1 text-[9px]">
             {errorCount}
           </span>
         )}
         ```

    **Keep FEED and PROVIDERS tabs unchanged.**
  </action>
  <verify>
    <automated>
      grep -q "ACTIVITY" components/dashboard/dashboard-right-rail.tsx && \
      grep -q "LogEntry" components/dashboard/dashboard-right-rail.tsx && \
      ! grep -q "ALERTS" components/dashboard/dashboard-right-rail.tsx && \
      ! grep -q "selectAlertsState" components/dashboard/dashboard-right-rail.tsx
    </automated>
  </verify>
  <done>
    Right Rail's ALERTS tab is replaced with ACTIVITY tab showing Top 10 log entries. Error count badge displays in tab header. FEED and PROVIDERS tabs unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace ALR with ACT in Sidebar navigation</name>
  <files>components/dashboard/sidebar-nav.tsx</files>
  <read_first>
    components/dashboard/sidebar-nav.tsx
    app/(shell)/dashboard/page.tsx
  </read_first>
  <action>
    Modify `components/dashboard/sidebar-nav.tsx` to replace the ALR (Alerts) button with ACT (Activity) button:

    1. **Update NAV_ITEMS array** (line ~6):
       - Find the alerts item: `{ id: 'alerts', label: 'ALR', title: 'Alerts', href: '/dashboard?tab=alerts' }`
       - Replace with: `{ id: 'activity', label: 'ACT', title: 'Activity Console', href: '/activity' }`
       - **Important**: Change `href` from `/dashboard?tab=alerts` to `/activity` (direct route, not query param)

    2. **Update TypeScript types** (if needed):
       - The `activeTab` prop type in `SidebarNavProps` should remain as `string` (no changes needed)
       - The `onTabChange` callback should handle 'activity' the same way as other tabs

    3. **No other changes needed**:
       - The navigation logic remains the same
       - Clicking ACT will navigate to `/activity` page (using Next.js Link or router.push)

    **Verify**: The ACT button should appear in the sidebar and navigate to `/activity` when clicked.
  </action>
  <verify>
    <automated>
      grep -q "ACT" components/dashboard/sidebar-nav.tsx && \
      grep -q "Activity Console" components/dashboard/sidebar-nav.tsx && \
      grep -q "href.*activity" components/dashboard/sidebar-nav.tsx && \
      ! grep -q "ALR" components/dashboard/sidebar-nav.tsx && \
      ! grep -q "tab=alerts" components/dashboard/sidebar-nav.tsx
    </automated>
  </verify>
  <done>
    Sidebar navigation's ALR button is replaced with ACT button. Clicking ACT navigates to `/activity` page. All other navigation items unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 4: Remove unused alert state and selectors</name>
  <files>stores/gateway/gateway-store.ts stores/gateway/p0-selectors.ts stores/gateway/p0-types.ts</files>
  <read_first>
    stores/gateway/p0-types.ts
    stores/gateway/p0-selectors.ts
    stores/gateway/gateway-store.ts
  </read_first>
  <action>
    Clean up unused alert-related code from the gateway store (optional but recommended):

    **IMPORTANT**: Per CLAUDE.md constraint "数据层锁定" (data layer locked), **DO NOT** modify core store logic. Only remove unused exports if they exist:

    1. **Check if alert exports are unused**:
       - Run: `grep -r "selectAlertsState\|ackAlert" components/ --include="*.tsx"`
       - If no results (except deleted files), the exports are unused

    2. **If unused** (and only if), you MAY remove:
       - `selectAlertsState` from `stores/gateway/p0-selectors.ts`
       - `ackAlert` action from `stores/gateway/gateway-store.ts`
       - `AlertItem`, `AlertSeverity` types from `stores/gateway/p0-types.ts`

    3. **If still used** (e.g., by other components), **DO NOT REMOVE** — leave the data layer intact.

    **Skip this task** if you're unsure about data layer dependencies. The primary integration changes are in Tasks 1-3.
  </action>
  <verify>
    <automated>
      # If alerts are truly unused, this should return empty
      ! grep -r "selectAlertsState\|ackAlert" components/ --include="*.tsx" | grep -v "node_modules"
    </automated>
  </verify>
  <done>
    (Optional) Unused alert-related exports removed from gateway store. Core store logic remains intact per "data layer locked" constraint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Dashboard integration complete:
    - Overview tab shows ACTIVITY section with Top 10 log entries
    - Right rail has ACTIVITY tab showing Top 10 log entries
    - Sidebar navigation has ACT button linking to /activity
    - All ALERT references replaced with ACTIVITY
  </what-built>
  <how-to-verify>
    1. Start dev server: `pnpm dev`
    2. Visit http://localhost:3000/dashboard
    3. Verify Overview tab shows ACTIVITY section (not ALERTS)
    4. Verify ACTIVITY section shows log entries and "View Activity Console →" link
    5. Click "View Activity Console →" link, verify navigation to /activity page
    6. Verify Right rail shows ACTIVITY tab (not ALERTS)
    7. Click ACTIVITY tab in right rail, verify log entries appear
    8. Verify error count badge shows in tab header if errors exist
    9. Verify Sidebar shows ACT button (not ALR)
    10. Click ACT button, verify navigation to /activity page
    11. Verify all other Dashboard tabs work correctly (Overview, Agents, Costs, Skills)
    </how-to-verify>
  <resume-signal>
    Type "approved" if integration works correctly, or describe issues to fix.
  </resume-signal>
</task>

## Threat Model

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component → API | Dashboard components fetch activity logs from `/api/logs` |
| Component → Router | Navigation uses Next.js router to navigate to `/activity` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-16 | Spoofing | API request | accept | No authentication required (localhost dev only) |
| T-06-17 | Information Disclosure | Activity display | accept | Non-sensitive log data, safe to display |
| T-06-18 | Denial of Service | Multiple fetches | mitigate | Each component fetches once on mount, 10-entry limit prevents large responses |
| T-06-19 | Elevation of Privilege | N/A | N/A | N/A |

## Verification

- [ ] Overview tab ACTIVITY section displays Top 10 log entries
- [ ] ACTIVITY section has "View Activity Console →" link
- [ ] Clicking link navigates to `/activity` page
- [ ] Right rail ACTIVITY tab displays Top 10 log entries
- [ ] Error count badge shows in tab header when errors exist
- [ ] Sidebar ACT button replaces ALR button
- [ ] Clicking ACT button navigates to `/activity` page
- [ ] All other Dashboard functionality remains intact
- [ ] No console errors related to missing alert imports
- [ ] Activity logs load correctly from `/api/logs`

## Success Criteria

1. **Overview Updated**: ALERTS section replaced with ACTIVITY showing Top 10 log entries
2. **Navigation Works**: "View Activity Console" link navigates to `/activity`
3. **Right Rail Updated**: ALERTS tab replaced with ACTIVITY tab
4. **Tab Badge**: Error count badge shows in ACTIVITY tab header
5. **Sidebar Updated**: ALR button replaced with ACT button
6. **ACT Navigation**: Clicking ACT navigates to `/activity` (not `/dashboard?tab=activity`)
7. **No Regressions**: All other Dashboard tabs/features work correctly
8. **Clean Code**: No unused alert imports or references
9. **Data Layer Intact**: Gateway store modifications respect "data layer locked" constraint

## Output

After completion, create `.planning/phases/06-activity-console/06-04-SUMMARY.md`
