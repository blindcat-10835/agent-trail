---
phase: 09-batch-2-session-replay-and-codex-subagent-relationship-fixes
plan: 01
title: Starred session route collision repair
type: execute
wave: 1
depends_on: []
files_modified:
  - ingest/index.ts
  - tests/unit/ingest/stars-route-order.test.ts
autonomous: true
requirements:
  - DATA-05
requirements_addressed: [DATA-05]
tags:
  - ingest
  - hono
  - starred-sessions
  - route-order
must_haves:
  truths:
    - "GET /api/v1/sessions/starred 返回 persisted star ids, 不再被 /api/v1/sessions/:id 捕获。"
    - "BFF contract /api/agent-tools/[tool]/sessions/starred 和 /sessions/[sessionId]/star 保持不变。"
  artifacts:
    - path: "tests/unit/ingest/stars-route-order.test.ts"
      provides: "composed Hono route-order regression for starred route"
    - path: "ingest/index.ts"
      provides: "static stars routes mounted before dynamic sessions routes"
  key_links:
    - from: "ingest/index.ts"
      to: "ingest/api/stars.ts"
      via: "app.route('/', starsRoutes) before app.route('/', sessionsRoutes)"
      pattern: "starsRoutes[\\s\\S]*sessionsRoutes"
---

## Goal

修复 B2-01: persisted starred sessions 刷新后消失的问题。根因是 Hono route 注册顺序让 `GET /api/v1/sessions/starred` 先命中 `GET /api/v1/sessions/:id`。本计划只调整 ingest route order 并补回归测试, 不改变前端 BFF URL。

<execution_context>
@/Users/ebbi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ebbi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-batch-2-session-replay-and-codex-subagent-relationship-fixes/09-CONTEXT.md
@.planning/phases/09-batch-2-session-replay-and-codex-subagent-relationship-fixes/09-RESEARCH.md
@.planning/phases/09-batch-2-session-replay-and-codex-subagent-relationship-fixes/09-VALIDATION.md
@.planning/phases/09-batch-2-session-replay-and-codex-subagent-relationship-fixes/09-PATTERNS.md
@.planning/bugs-by-user/2026-05-10-bugs-found-by-user-batch-2-research.md
@AGENTS.md
@ERRORS_LEARNED.md
</context>

## Source Coverage Audit

| Source | ID | Requirement or decision | Plan | Status |
|---|---|---|---|---|
| GOAL | Phase 09 | Starred, aggregate pagination, Markdown search, edit display, Codex subagent relationship fixes | 09-01 to 09-05 | COVERED |
| REQ | DATA-04 | Sync/backfill remains correct after limited/full sync | 09-05 | COVERED |
| REQ | DATA-05 | REST starred route reachable and BFF contract preserved | 09-01 | COVERED |
| REQ | SRC-03 | Codex parser handles patch tool category and subagent events | 09-04 | COVERED |
| REQ | SRC-04 | Canonical tool calls and subagent links remain emitted | 09-04, 09-05 | COVERED |
| REQ | TURN-03 | Tool call/result pairing not regressed by edit/category fixes | 09-03, 09-04 | COVERED |
| REQ | TURN-04 | Skill block dispatch remains independent from tool display changes | 09-03 | COVERED |
| REQ | TURN-05 | Codex subagent child session ids are backfilled for lazy replay | 09-05 | COVERED |
| REQ | REPLAY-01 | Replay turn rendering remains stable with Markdown/search | 09-03, 09-05 | COVERED |
| REQ | REPLAY-02 | All-source right rail has incremental pagination | 09-02 | COVERED |
| REQ | REPLAY-03 | Tool blocks render readable edit/diff/patch display | 09-03, 09-04 | COVERED |
| REQ | REPLAY-04 | Subagent inline view receives correct Codex child relationship data | 09-05 | COVERED |
| REQ | REPLAY-06 | Copy output remains usable for tool/debug reuse | 09-03 | COVERED |
| CONTEXT | D-starred | Fix route collision, preserve BFF contract, add regression | 09-01 | COVERED |
| CONTEXT | D-aggregate | Per-source aggregate pagination, true totals, dedupe and freshness sort | 09-02 | COVERED |
| CONTEXT | D-markdown | Do not clone top-level ReactMarkdown with non-string children | 09-03 | COVERED |
| CONTEXT | D-edit | Format Claude Edit/MultiEdit/Write and Codex apply_patch | 09-03, 09-04 | COVERED |
| CONTEXT | D-codex-subagent | Idempotent relationship backfill, no hide_single_turn substitute | 09-05 | COVERED |
| RESEARCH | Route order pitfall | Static route before dynamic route per Hono routing priority | 09-01 | COVERED |
| RESEARCH | BFF boundary | Frontend keeps `/api/agent-tools/[tool]/...` only | 09-01 to 09-03 | COVERED |

<tasks>
<task type="auto" tdd="true">
  <id>09-01-01</id>
  <title>Create starred route-order regression</title>
  <name>Task 1: Create starred route-order regression</name>
  <wave>1</wave>
  <files>tests/unit/ingest/stars-route-order.test.ts</files>
  <read_first>
    - `.planning/phases/09-batch-2-session-replay-and-codex-subagent-relationship-fixes/09-CONTEXT.md`: starred sessions persistence locked decision.
    - `.planning/phases/09-batch-2-session-replay-and-codex-subagent-relationship-fixes/09-PATTERNS.md`: Hono mount-order regression pattern.
    - `ingest/api/stars.ts`: `starsRoutes.get('/api/v1/sessions/starred')` response shape.
    - `ingest/api/sessions.ts`: dynamic `sessionsRoutes.get('/api/v1/sessions/:id')` collision target.
    - `tests/unit/ingest/sessions-api.test.ts`: temp DB setup with `openDatabase`, `initSchema`, `closeDatabase`.
  </read_first>
  <behavior>
    - Test 1: a composed `new Hono()` app with `starsRoutes` mounted before `sessionsRoutes` returns HTTP 200 for `/api/v1/sessions/starred`.
    - Test 2: the JSON body is exactly `{ session_ids: ['session-a'] }` when `session_stars` contains `session-a`.
    - Test 3: the response body must not include `{ error: 'Session not found', sessionId: 'starred' }`.
  </behavior>
  <action>
    Create `tests/unit/ingest/stars-route-order.test.ts`. Use a temp SQLite DB path like `sessions-api.test.ts`, call `openDatabase({ path })`, `initSchema()`, insert one row into `session_stars`, and build the composed app with `const app = new Hono(); app.route('/', starsRoutes); app.route('/', sessionsRoutes);`. Do not test `starsRoutes` in isolation, because the bug is route composition. Keep all SQL parameterized. Do not add ORM schema push, Prisma, Drizzle, Payload, Supabase, or TypeORM tasks; this project uses local `ingest/db/schema.sql` with `better-sqlite3`.
  </action>
  <acceptance_criteria>
    - `tests/unit/ingest/stars-route-order.test.ts` imports `Hono`, `starsRoutes`, `sessionsRoutes`, and DB helpers.
    - The test inserts into `session_stars` and asserts `session_ids`.
    - The test would fail if `starsRoutes` were mounted after `sessionsRoutes`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test:run tests/unit/ingest/stars-route-order.test.ts</automated>
  </verify>
  <done>Focused starred route-order regression exists and proves `/api/v1/sessions/starred` reaches `session_stars`.</done>
</task>

<task type="auto" tdd="true">
  <id>09-01-02</id>
  <title>Mount stars before sessions in ingest app</title>
  <name>Task 2: Mount stars before sessions in ingest app</name>
  <wave>1</wave>
  <files>ingest/index.ts</files>
  <read_first>
    - `ingest/index.ts`: current route order around `sourcesRoutes`, `sessionsRoutes`, `turnsRoutes`, `agentsRoutes`, `starsRoutes`, `eventsRoutes`.
    - `app/api/agent-tools/[tool]/sessions/starred/route.ts`: BFF contract to preserve.
    - `app/api/agent-tools/[tool]/sessions/[sessionId]/star/route.ts`: BFF star/unstar contract to preserve.
    - `.planning/phases/09-batch-2-session-replay-and-codex-subagent-relationship-fixes/09-RESEARCH.md`: Hono routing priority note.
  </read_first>
  <action>
    In `ingest/index.ts`, move `app.route('/', starsRoutes);` above `app.route('/', sessionsRoutes);`. Keep `sourcesRoutes` first, keep `turnsRoutes`, `agentsRoutes`, and `eventsRoutes` mounted after the session/star group, and do not change route paths inside `ingest/api/stars.ts` or BFF files. This implements the starred sessions persistence decision from `09-CONTEXT.md` without changing the public BFF contract.
  </action>
  <acceptance_criteria>
    - `ingest/index.ts` has `app.route('/', starsRoutes);` before `app.route('/', sessionsRoutes);`.
    - `app/api/agent-tools/[tool]/sessions/starred/route.ts` remains a proxy to `/api/v1/sessions/starred`.
    - `app/api/agent-tools/[tool]/sessions/[sessionId]/star/route.ts` remains a proxy to `/api/v1/sessions/:id/star`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test:run tests/unit/ingest/stars-route-order.test.ts tests/hooks/client-hooks.test.tsx</automated>
    <automated>pnpm typecheck:ingest</automated>
  </verify>
  <done>Starred sessions survive refresh because the static starred route is reachable through ingest and BFF URLs stay unchanged.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser -> BFF | Star load/toggle requests must remain same-origin `/api/agent-tools/[tool]/...`. |
| BFF -> ingest | BFF forwards only known star endpoints to localhost ingest. |
| ingest route table -> SQLite | Route matching determines whether the static starred endpoint reads `session_stars` or dynamic session detail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-01 | Tampering | `ingest/index.ts` route order | mitigate | Register `starsRoutes` before `sessionsRoutes` and test the composed Hono app. |
| T-09-02 | Information Disclosure | BFF star routes | mitigate | Preserve existing BFF routes and do not expose file paths, stacks, or direct ingest URLs to client code. |
| T-09-03 | Denial of Service | starred endpoint | accept | Endpoint returns only persisted IDs from local SQLite; no unbounded file scan is introduced. |
</threat_model>

## Verification

Run:

```bash
pnpm test:run tests/unit/ingest/stars-route-order.test.ts tests/hooks/client-hooks.test.tsx
pnpm typecheck:ingest
```

## Acceptance Criteria

- `GET /api/v1/sessions/starred` returns `{ session_ids: [...] }` from `session_stars`.
- `/api/agent-tools/all/sessions/starred` can keep proxying unchanged.
- No production file outside `ingest/index.ts` changes for this repair.

<output>
After completion, create `.planning/phases/09-batch-2-session-replay-and-codex-subagent-relationship-fixes/09-01-SUMMARY.md`.
</output>
