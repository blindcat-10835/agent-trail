---
phase: 03-shell-layout-base-components
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/(shell)/layout.tsx
  - components/hud/shell-header.tsx
  - components/hud/shell-status-bar.tsx
  - components/hud/status-indicator.tsx
  - app/layout.tsx
autonomous: false
requirements:
  - ENGR-03
user_setup: []

must_haves:
  truths:
    - "Shell 布局在所有 (shell) 路由组的页面统一渲染"
    - "Header 包含品牌区域、顶级导航、控制区（连接状态+主题切换）"
    - "顶级导航按钮显示当前激活页面的高亮状态"
    - "Status Bar 显示 Gateway 连接状态和系统信息"
    - "ThemeToggle 从临时位置移动到 Header 右侧控制区"
  artifacts:
    - path: "app/(shell)/layout.tsx"
      provides: "Shell Grid 布局结构"
      min_lines: 15
      contains:
        - "grid-rows-[48px_1fr_26px]"
        - "grid-cols-[260px_1fr_300px]"
        - "ShellHeader"
        - "ShellStatusBar"
    - path: "components/hud/shell-header.tsx"
      provides: "Header 组件（品牌+导航+控制）"
      min_lines: 80
      contains:
        - "usePathname"
        - "navItems"
        - "StatusIndicator"
        - "ThemeToggle"
    - path: "components/hud/shell-status-bar.tsx"
      provides: "Status Bar 组件（Gateway 状态+统计）"
      min_lines: 40
      contains:
        - "connectionStatus"
        - "WS"
        - "PROTO"
    - path: "components/hud/status-indicator.tsx"
      provides: "连接状态指示器（dot+文本+动画）"
      min_lines: 30
      contains:
        - "connectionUIState"
        - "animate-pulse"
  key_links:
    - from: "app/(shell)/layout.tsx"
      to: "components/hud/shell-header.tsx"
      via: "import { ShellHeader }"
      pattern: "import.*ShellHeader"
    - from: "app/(shell)/layout.tsx"
      to: "components/hud/shell-status-bar.tsx"
      via: "import { ShellStatusBar }"
      pattern: "import.*ShellStatusBar"
    - from: "components/hud/shell-header.tsx"
      to: "stores/gateway/gateway-store.ts"
      via: "useGatewayStore()"
      pattern: "useGatewayStore"
    - from: "components/hud/shell-header.tsx"
      to: "components/hud/theme-toggle.tsx"
      via: "import { ThemeToggle }"
      pattern: "import.*ThemeToggle"
---

# Plan 03-02: Shell 布局实现

## 目标

实现全站通用的 Shell 布局结构，包括 Header（品牌+顶级导航+控制区）、3-column Main（左面板+内容+右面板）、Status Bar（Gateway 连接状态+系统信息）。

**目的**: 建立统一的页面骨架，所有 (shell) 路由组的页面自动继承 Header + Main + Status Bar 结构

**产出**: Shell 布局组件（ShellHeader, ShellStatusBar, StatusIndicator）和 `app/(shell)/layout.tsx` Grid 结构

## 执行上下文

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## 上下文

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@../ovao-design/dashboard-hud.html — 设计稿的 Shell Grid 结构（权威参考）
@app/(shell)/layout.tsx — 当前占位 Shell 布局（需替换）
@app/layout.tsx — 根布局（移除 ThemeToggle fixed position）
@components/hud/theme-toggle.tsx — ThemeToggle 组件（移入 Header）
@stores/gateway/gateway-store.ts — Gateway 状态（ConnectionStatus 类型）
@stores/gateway/p0-selectors.ts — Gateway selectors（connectionUIState 函数）

## 决策追溯

**D-01**: 采用混合布局 — 设计稿 dashboard-hud.html 的 header + 3-column main + status bar 骨架
**D-02**: Header 区域包含：左侧品牌（logo+名称）、中间顶级导航（Dashboard / Office / Workspace 页面切换）、右侧控制区（连接状态指示+时钟+主题切换器）
**D-03**: 左侧面板（260px）是导航+数据混合模式 — 上方有子导航标签，下方显示对应的数据面板
**D-04**: 中心区域（1fr）展示当前页面的主要内容
**D-05**: 右侧面板（300px）展示详情/上下文信息。Phase 3 先做空状态或最小实现
**D-06**: Status Bar（26px）显示 Gateway 连接状态、协议版本、conn ID 等
**D-07**: Shell 整体使用 CSS Grid：`grid-template-rows: 48px 1fr 26px`，Main 区域 `grid-template-columns: 260px 1fr 300px`
**D-13**: ThemeToggle 从 Phase 2 的临时 fixed 位置移动到 Header 右侧控制区
**D-14**: 使用设计稿的 hud-btn 风格（clip-path 切角边框按钮），与其他 header 按钮视觉统一
**D-15**: 顶级导航（Dashboard / Office / Workspace）在 Header 中用类似 hud-btn 的标签按钮实现
**D-16**: 当前激活页面的按钮使用 `.active` 状态（cyan 边框+背景），参考设计稿的 hud-btn.active 样式

## 任务

<task type="auto">
  <name>任务 1: 创建 StatusIndicator 连接状态指示器组件</name>
  <files>components/hud/status-indicator.tsx</files>

  <read_first>
  - stores/gateway/gateway-store.ts（ConnectionStatus 类型和 store 结构）
  - stores/gateway/p0-selectors.ts（connectionUIState 函数）
  - ../ovao-design/dashboard-hud.html（设计稿的 conn-ind 样式参考）
  </read_first>

  <action>
  创建新文件 `components/hud/status-indicator.tsx`：

  ```typescript
  'use client'

  import { useGatewayStore } from '@/stores/gateway/gateway-store'
  import { connectionUIState } from '@/stores/gateway/p0-selectors'

  export function StatusIndicator() {
    const connectionState = connectionUIState(useGatewayStore())

    return (
      <div className="conn-ind hud-clip-sm flex items-center gap-1.5 border border-border/40 px-2.5 py-1 text-[11px] font-semibold">
        <div
          className="conn-dot w-1.5 h-1.5 rounded-full bg-current animate-pulse"
          style={{
            backgroundColor: connectionState.color,
            boxShadow: `0 0 8px ${connectionState.color}`,
          }}
        />
        <span>{connectionState.label}</span>
        <span className="text-fg-mute">· {connectionState.latency}ms</span>
      </div>
    )
  }
  ```

  **关键点**:
  - 使用 `'use client'` 指令（需要 `useGatewayStore()` 和 `connectionUIState()` 客户端 hooks）
  - `connectionUIState()` 返回对象包含 `{ color, label, latency }` 字段（来自 p0-selectors）
  - 连接状态点（`.conn-dot`）使用 `animate-pulse` 动画（Tailwind 内置）
  - `hud-clip-sm` @utility 来自 Plan 03-01，实现切角效果
  - 颜色和阴影使用 `connectionState.color` 动态设置（根据连接状态变化：connected=green, error=red 等）

  **不要**硬编码状态颜色或标签，全部从 `connectionUIState()` 获取。
  </action>

  <verify>
  <automated>test -f components/hud/status-indicator.tsx && grep -q "connectionUIState" components/hud/status-indicator.tsx</automated>
  </verify>

  <done>
  - `components/hud/status-indicator.tsx` 文件存在
  - 导出 `StatusIndicator` 组件
  - 使用 `useGatewayStore()` 和 `connectionUIState()` 获取连接状态
  - 连接状态点使用 `connectionState.color` 和 `connectionState.label`
  - 组件使用 `hud-clip-sm` @utility（来自 Plan 03-01）
  </done>

  <acceptance_criteria>
  - 文件 `components/hud/status-indicator.tsx` 存在
  - 文件包含 `'use client'` 指令
  - 导出 `StatusIndicator` 函数组件
  - 调用 `connectionUIState(useGatewayStore())`
  - 访问 `connectionState.color`, `connectionState.label`, `connectionState.latency`
  - className 包含 `hud-clip-sm`
  - 连接状态点包含 `animate-pulse` 动画
  </acceptance_criteria>
</task>

<task type="auto">
  <name>任务 2: 创建 ShellHeader 组件（品牌+导航+控制区）</name>
  <files>components/hud/shell-header.tsx</files>

  <read_first>
  - ../ovao-design/dashboard-hud.html（设计稿的 hud-head 结构和样式）
  - components/hud/theme-toggle.tsx（ThemeToggle 组件）
  - components/hud/status-indicator.tsx（StatusIndicator 组件，任务 1 创建）
  </read_first>

  <action>
  创建新文件 `components/hud/shell-header.tsx`：

  ```typescript
  'use client'

  import Link from 'next/link'
  import { usePathname } from 'next/navigation'
  import { StatusIndicator } from './status-indicator'
  import { ThemeToggle } from './theme-toggle'

  const navItems = [
    { href: '/dashboard', label: 'Dashboard' },
    { href: '/office', label: 'Office' },
    { href: '/workspace', label: 'Workspace' },
  ] as const

  export function ShellHeader() {
    const pathname = usePathname()

    return (
      <header className="hud-header grid grid-cols-[280px_1fr_auto] items-center px-5 h-12 border-b border-border border-strong bg-gradient-to-b from-bg-panel to-bg relative">
        {/* Brand */}
        <div className="hud-brand flex items-center gap-3">
          <div className="hud-logo hud-clip-sm w-7 h-7 bg-accent flex items-center justify-center text-bg font-bold text-sm">
            ◆
          </div>
          <div className="hud-brand-name text-base font-bold tracking-[0.3em] text-accent">
            OVAO
          </div>
          <div className="text-[10px] text-fg-mute tracking-[0.2em] pl-2.5 border-l border-border">
            GATEWAY · v3.2.1
          </div>
        </div>

        {/* Top-level navigation */}
        <nav className="flex items-center justify-center gap-2">
          {navItems.map((item) => (
            <Link
              key={item.href}
              href={item.href}
              className={`hud-clip-sm border border-border px-2.5 py-1 text-xs tracking-[0.14em] font-semibold transition-all ${
                pathname === item.href
                  ? 'border-accent text-accent bg-accent/10'
                  : 'text-fg-mute hover:border-accent hover:text-accent'
              }`}
            >
              {item.label}
            </Link>
          ))}
        </nav>

        {/* Controls */}
        <div className="hud-meta flex items-center gap-3.5 text-xs tracking-[0.12em]">
          <StatusIndicator />
          <ThemeToggle />
        </div>

        {/* Bottom gradient line */}
        <div className="absolute bottom-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-accent via-amber via-accent to-transparent opacity-60" />
      </header>
    )
  }
  ```

  **关键点**:
  - 使用 `'use client'` 指令（需要 `usePathname()` hook）
  - `usePathname()` 获取当前路由，用于高亮激活的导航按钮
  - 导航按钮使用 `pathname === item.href` 判断激活状态（三元运算符动态 className）
  - 激活状态：`border-accent text-accent bg-accent/10`（cyan 边框+背景）
  - 非激活状态：`text-fg-mute hover:border-accent hover:text-accent`（灰色文本，hover 时 cyan）
  - `hud-clip-sm` @utility 来自 Plan 03-01，实现切角效果
  - 底部渐变发光线使用 `bg-gradient-to-r from-transparent via-accent via-amber via-accent to-transparent`（cyan-amber-cyan 渐变）
  - `ThemeToggle` 组件已在 Phase 2 创建，直接导入使用

  **不要**硬编码导航项，使用 `navItems` 数组便于后续扩展。
  **不要**在 Header 中实现子导航逻辑（左面板的子导航标签属于 Phase 4-6）。
  </action>

  <verify>
  <automated>test -f components/hud/shell-header.tsx && grep -q "usePathname" components/hud/shell-header.tsx</automated>
  </verify>

  <done>
  - `components/hud/shell-header.tsx` 文件存在
  - 导出 `ShellHeader` 组件
  - 使用 `usePathname()` 获取当前路由
  - 品牌区域包含 logo（◆ 符号）、OVAO 名称、版本标签
  - 顶级导航包含 Dashboard / Office / Workspace 三个按钮
  - 导航按钮根据当前路由显示激活状态（cyan 边框+背景）
  - 右侧控制区包含 `StatusIndicator` 和 `ThemeToggle`
  - Header 底部有渐变发光线（cyan-amber-cyan）
  </done>

  <acceptance_criteria>
  - 文件 `components/hud/shell-header.tsx` 存在
  - 文件包含 `'use client'` 指令
  - 导出 `ShellHeader` 函数组件
  - 导入 `usePathname` from 'next/navigation'
  - 定义 `navItems` 常量数组包含 3 个导航项（/dashboard, /office, /workspace）
  - className 包含 `hud-clip-sm`（导航按钮）
  - 导航按钮激活状态使用三元运算符 `pathname === item.href ? ... : ...`
  - 导入 `StatusIndicator` 和 `ThemeToggle` 组件
  - 底部渐变线 className 包含 `bg-gradient-to-r from-transparent via-accent via-amber via-accent to-transparent`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>任务 3: 创建 ShellStatusBar 组件（Gateway 状态+统计）</name>
  <files>components/hud/shell-status-bar.tsx</files>

  <read_first>
  - ../ovao-design/dashboard-hud.html（设计稿的 hud-status 结构和内容）
  - stores/gateway/gateway-store.ts（ConnectionStatus 类型和 store）
  </read_first>

  <action>
  创建新文件 `components/hud/shell-status-bar.tsx`：

  ```typescript
  'use client'

  import { useGatewayStore } from '@/stores/gateway/gateway-store'

  export function ShellStatusBar() {
    const connectionStatus = useGatewayStore((state) => state.connectionStatus)

    return (
      <footer className="hud-status flex items-center justify-between px-3.5 h-6 border-t border-border text-[10px] tracking-[0.12em] text-fg-mute relative">
        {/* Top gradient line */}
        <div className="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-accent to-transparent opacity-40" />

        {/* Left section */}
        <div className="flex items-center gap-4">
          <span>
            WS <b className={connectionStatus === 'connected' ? 'text-accent' : ''}>{connectionStatus.toUpperCase()}</b>
          </span>
          <span>PROTO <b>v3</b></span>
          <span>CONN <b>conn_8f2e</b></span>
          <span>SCOPES <b>workspace:* · agents:rw</b></span>
        </div>

        {/* Right section */}
        <div className="flex items-center gap-4">
          <span>MEM <b>42.1MB</b></span>
          <span>FPS <b>60</b></span>
          <span className="text-accent font-bold tracking-[0.2em]" style={{ textShadow: '0 0 8px var(--color-accent)' }}>
            ◆ OVAO
          </span>
        </div>
      </footer>
    )
  }
  ```

  **关键点**:
  - 使用 `'use client'` 指令（需要 `useGatewayStore()` hook）
  - `connectionStatus` 直接从 store 获取（类型：`"connecting" | "connected" | "reconnecting" | "disconnected" | "error"`）
  - 左侧显示：WS 连接状态（大写）、PROTO 协议版本、CONN 连接 ID、SCOPES 权限范围
  - 右侧显示：MEM 内存使用、FPS 帧率、◆ OVAO 品牌（带发光效果）
  - WS 状态使用 `connectionStatus === 'connected' ? 'text-accent' : ''` 条件样式（connected 时 cyan 高亮）
  - 顶部渐变发光线使用 `bg-gradient-to-r from-transparent via-accent to-transparent`（单色 cyan 渐变）
  - 右侧品牌符号使用 `textShadow: '0 0 8px var(--color-accent)'` 实现霓虹发光（内联样式，Tailwind 不支持 text-shadow）

  **不要**实时更新 MEM 和 FPS 值（Phase 3 显示静态占位数据，Phase 4+ 根据 Gateway 或浏览器 API 更新）。
  **不要**实现连接重连逻辑（Gateway WebSocket 连接逻辑在 gateway/ 目录，本组件只显示状态）。
  </action>

  <verify>
  <automated>test -f components/hud/shell-status-bar.tsx && grep -q "useGatewayStore" components/hud/shell-status-bar.tsx</automated>
  </verify>

  <done>
  - `components/hud/shell-status-bar.tsx` 文件存在
  - 导出 `ShellStatusBar` 组件
  - 使用 `useGatewayStore()` 获取 `connectionStatus`
  - 左侧显示 WS、PROTO、CONN、SCOPES 信息
  - 右侧显示 MEM、FPS、◆ OVAO 品牌
  - WS 状态根据 `connectionStatus === 'connected'` 显示 cyan 高亮
  - 顶部有渐变发光线（cyan 单色渐变）
  - 右侧品牌符号有霓虹发光效果（text-shadow）
  </done>

  <acceptance_criteria>
  - 文件 `components/hud/shell-status-bar.tsx` 存在
  - 文件包含 `'use client'` 指令
  - 导出 `ShellStatusBar` 函数组件
  - 调用 `useGatewayStore((state) => state.connectionStatus)`
  - 左侧区域包含 `<span>WS <b>{connectionStatus.toUpperCase()}</b></span>`
  - 右侧区域包含 `<span>MEM <b>42.1MB</b></span>` 和 `<span>FPS <b>60</b></span>`
  - className 包含 `hud-status`
  - 顶部渐变线 className 包含 `bg-gradient-to-r from-transparent via-accent to-transparent`
  - 右侧品牌符号包含 `style={{ textShadow: '0 0 8px var(--color-accent)' }}`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>任务 4: 实现 Shell Grid 布局（app/(shell)/layout.tsx）</name>
  <files>app/(shell)/layout.tsx</files>

  <read_first>
  - app/(shell)/layout.tsx（当前占位 Shell 布局）
  - ../ovao-design/dashboard-hud.html（设计稿的 Shell Grid 结构）
  - components/hud/shell-header.tsx（ShellHeader 组件，任务 2 创建）
  - components/hud/shell-status-bar.tsx（ShellStatusBar 组件，任务 3 创建）
  </read_first>

  <action>
  替换 `app/(shell)/layout.tsx` 的全部内容为：

  ```typescript
  import { ShellHeader } from '@/components/hud/shell-header'
  import { ShellStatusBar } from '@/components/hud/shell-status-bar'

  export default function ShellLayout({
    children,
  }: {
    children: React.ReactNode
  }) {
    return (
      <div className="shell-layout grid grid-rows-[48px_1fr_26px] h-screen w-screen overflow-hidden bg-bg text-fg">
        <ShellHeader />
        <main className="grid grid-cols-[260px_1fr_300px] min-h-0">
          {children}
        </main>
        <ShellStatusBar />
      </div>
    )
  }
  ```

  **关键点**:
  - Shell 使用 `grid grid-rows-[48px_1fr_26px]`（Header 48px + Main 1fr + Status Bar 26px）
  - Main 使用 `grid grid-cols-[260px_1fr_300px]`（左面板 260px + 内容 1fr + 右面板 300px）
  - `h-screen w-screen overflow-hidden` 确保全屏显示且无滚动条
  - `min-h-0` 在 main 上确保 Grid 子项可以正确收缩（CSS Grid 最佳实践）
  - 不使用 `'use client'` 指令（layout.tsx 默认是 Server Component，ShellHeader 和 ShellStatusBar 自身是 Client Component）

  **不要**在 layout.tsx 中实现左面板和右面板的内容（各页面自行决定使用哪个面板区域）。
  **不要**添加滚动条（`overflow-hidden` 在 Shell 上，各页面内容区域自行管理滚动）。
  </action>

  <verify>
  <automated>grep -n "grid-rows-\[48px_1fr_26px\]" app/\(shell\)/layout.tsx</automated>
  </verify>

  <done>
  - `app/(shell)/layout.tsx` 使用 `grid grid-rows-[48px_1fr_26px]` 定义 Shell Grid
  - Main 区域使用 `grid grid-cols-[260px_1fr_300px]` 定义三列布局
  - 导入并渲染 `ShellHeader` 和 `ShellStatusBar` 组件
  - Shell 使用 `h-screen w-screen overflow-hidden` 确保全屏显示
  - Main 使用 `min-h-0` 确保 Grid 子项正确收缩
  </done>

  <acceptance_criteria>
  - 文件 `app/(shell)/layout.tsx` 包含 `grid-rows-[48px_1fr_26px]`
  - 文件包含 `grid-cols-[260px_1fr_300px]`
  - 文件包含 `import { ShellHeader } from '@/components/hud/shell-header'`
  - 文件包含 `import { ShellStatusBar } from '@/components/hud/shell-status-bar'`
  - Shell div 包含 `className="shell-layout grid grid-rows-[48px_1fr_26px] h-screen w-screen overflow-hidden bg-bg text-fg"`
  - Main 元素包含 `className="grid grid-cols-[260px_1fr_300px] min-h-0"`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>任务 5: 从根布局移除 ThemeToggle 临时位置</name>
  <files>app/layout.tsx</files>

  <read_first>
  - app/layout.tsx（当前根布局，包含 ThemeToggle 的 fixed position）
  - components/hud/theme-toggle.tsx（确认 ThemeToggle 已在 Header 中使用）
  </read_first>

  <action>
  从 `app/layout.tsx` 中移除 Phase 2 添加的 ThemeToggle 临时位置（fixed position 的 div）。

  查找并删除类似以下代码块（Phase 2 临时添加的）：

  ```typescript
  <!-- 删除这段代码 -->
  <div className="fixed bottom-4 right-4 z-50">
    <ThemeToggle />
  </div>
  ```

  **保留**根布局中的其他内容：
  - `html` 标签的 `data-theme` 属性和 `suppressHydrationWarning`
  - FOUC 防护 script（`!(function() { ... }())`）
  - `body` 标签的 className
  - JetBrains Mono 和 Inter 字体加载

  **不要**删除 fonts 导入（`JetBrainsMono`, `Inter` from `next/font/google`）。
  **不要**删除 FOUC 防护 script（`!(function() { ... }())`）。
  </action>

  <verify>
  <automated>grep -n "ThemeToggle" app/layout.tsx || echo "ThemeToggle removed from root layout"</automated>
  </verify>

  <done>
  - `app/layout.tsx` 不包含 `ThemeToggle` 组件引用
  - `app/layout.tsx` 不包含 fixed position 的 div（`fixed bottom-4 right-4`）
  - `app/layout.tsx` 保留 FOUC 防护 script
  - `app/layout.tsx` 保留 fonts 导入（JetBrains Mono + Inter）
  </done>

  <acceptance_criteria>
  - `app/layout.tsx` 不包含 `import.*ThemeToggle` 或 `<ThemeToggle`
  - `app/layout.tsx` 不包含 `className="fixed bottom-4 right-4`
  - `app/layout.tsx` 包含 `suppressHydrationWarning` 在 html 标签上
  - `app/layout.tsx` 包含 `!(function() { ... }())` FOUC 防护 script
  </acceptance_criteria>
</task>

## 威胁模型

## 信任边界

| Boundary | Description |
|----------|-------------|
| Client → Gateway | 连接状态从 Gateway WebSocket 获取，不可信输入需验证 |

## STRIDE 威胁登记表

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Spoofing | Gateway connectionStatus | accept | 状态显示仅供 UI 反馈，无敏感操作，直接显示 store 值 |
| T-03-03 | Tampering | connectionUIState 数据 | mitigate | connectionUIState 是 selector，只读访问 store，防止篡改 |

## 验证

- [ ] `pnpm lint` 通过（无 ESLint 错误）
- [ ] `pnpm build` 成功（构建无错误）
- [ ] `pnpm dev` 启动后，访问 `/dashboard` 显示完整 Shell 布局（Header + Main + Status Bar）
- [ ] Header 中的导航按钮点击可切换页面（Dashboard / Office / Workspace）
- [ ] 当前激活页面的导航按钮显示 cyan 高亮（`border-accent text-accent bg-accent/10`）
- [ ] Status Bar 显示 Gateway 连接状态（WS CONNECTED / DISCONNECTED 等）
- [ ] ThemeToggle 在 Header 右侧控制区可点击（不显示在 fixed position）

## 成功标准

1. Shell 布局在所有 (shell) 路由组的页面统一渲染（Header + 3-column Main + Status Bar）
2. Header 包含品牌区域（◆ OVAO + GATEWAY 版本）、顶级导航（Dashboard/Office/Workspace）、控制区（StatusIndicator + ThemeToggle）
3. 顶级导航按钮显示当前激活页面的高亮状态（usePathname() 检测路由）
4. Status Bar 显示 Gateway 连接状态（WS 状态大写显示，connected 时 cyan 高亮）和系统信息（PROTO、CONN、SCOPES、MEM、FPS）
5. ThemeToggle 从 Phase 2 的临时 fixed 位置移除，集成到 Header 右侧控制区

## 输出

完成后创建 `.planning/phases/03-shell-layout-base-components/03-02-SUMMARY.md`
