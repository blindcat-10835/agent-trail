---
phase: 17
plan: 01
wave: 1
status: planned
created: 2026-05-17
---

# Plan 17-01: Source Identity, Schema Migration, and Configuration

Wave 1 — foundation layer with no external dependencies.

## Goal

Add `opencode` as a valid source identity throughout the codebase, migrate the SQLite schema to accept it in CHECK constraints, add source-reported cost columns, and configure opencode DB discovery.

## Tasks

### T-01: Add `opencode` to type unions

**Files:** `types/trace.ts`, `lib/agent-tools/types.ts`

1. In `types/trace.ts:22`: change `TraceSource` from `'openclaw' | 'claude-code' | 'codex'` to `'openclaw' | 'claude-code' | 'codex' | 'opencode'`
2. In `lib/agent-tools/types.ts:18`: change `SourceToolId` from `'openclaw' | 'claude-code' | 'codex'` to `'openclaw' | 'claude-code' | 'codex' | 'opencode'`
3. In `ingest/sync/index.ts:63`: change `SyncSourceType` to include `'opencode'`

**Verify:** `pnpm exec tsc --noEmit` passes

### T-02: Schema migration — rebuild tables with updated CHECK + cost columns

**Files:** `ingest/db/schema.sql`, `ingest/db/index.ts`

1. Update `schema.sql` to replace all 3 CHECK constraints:
   - `sessions.source`: add `'opencode'` to the IN list
   - `subagent_links.subagent_source`: add `'opencode'` to the IN list
   - `ingest_file_cursors.source_type`: add `'opencode'` to the IN list
2. Add 3 new columns to `sessions` table in schema.sql:
   - `source_cost_usd REAL` (nullable)
   - `cost_source TEXT` (nullable)
   - `cost_pricing_status TEXT` (nullable)
3. In `ingest/db/index.ts`: increment `targetVersion` from `13` to `14`
4. Add migration steps (within the cumulative migration array):
   - Step: Create `sessions_new` with all existing columns + 3 new cost columns + updated CHECK. Copy data from `sessions`. Drop `sessions`. Rename `sessions_new` to `sessions`. Recreate all indexes on sessions.
   - Step: Create `subagent_links_new` with updated CHECK. Copy data. Drop. Rename. Recreate indexes.
   - Step: Create `ingest_file_cursors_new` with updated CHECK. Copy data. Drop. Rename. Recreate indexes.
   - Step: Invalidate skip cache: `UPDATE sessions SET file_hash = NULL WHERE source = 'openclaw' OR source = 'claude-code' OR source = 'codex'`
5. All migration steps must be idempotent (wrapped in try/catch with "already exists" tolerance)

**Verify:** Existing ingest tests pass; `pnpm test:run -- ingest/db/` passes; manual `pnpm dev` starts without migration error on existing DB

### T-03: Add opencode source validation to all whitelist arrays

**Files:** `ingest/api/overview.ts`, `ingest/api/sources.ts`, `ingest/api/sessions.ts`, `ingest/api/agents.ts`, `ingest/index.ts`, `app/api/sync/route.ts`

1. `ingest/api/overview.ts:27`: add `'opencode'` to `VALID_SOURCES`
2. `ingest/api/sources.ts:22`: add `'opencode'` to `SOURCE_TYPES`
3. `ingest/api/sessions.ts:36`: add `'opencode'` to the source whitelist
4. `ingest/api/agents.ts:44`: add `'opencode'` to the source whitelist
5. `ingest/index.ts:76`: add `'opencode'` to the source array
6. `ingest/index.ts:267`: add `'opencode'` to sourceTypes
7. `app/api/sync/route.ts:4`: add `'opencode'` to `SOURCE_TYPES`

**Verify:** `pnpm exec tsc --noEmit` passes

### T-04: Configure opencode tool directory and capabilities

**Files:** `ingest/config/tool-dirs.ts`, `ingest/config/capabilities.ts`

1. In `tool-dirs.ts`: add to `TOOL_DIR_REGISTRY`:
   ```ts
   { type: 'opencode', displayName: 'OpenCode', envVar: 'OPENCODE_DB_PATH', configKey: 'opencode_db_paths', defaultDirs: ['.local/share/opencode'] }
   ```
2. In `capabilities.ts`: add to `SOURCE_CAPABILITIES`:
   ```ts
   opencode: { agents: false, automations: false, cost: true, activity: true, sessions: true, replay: true }
   ```

**Verify:** `pnpm exec tsc --noEmit` passes

### T-05: Write and run unit tests for type system and migration

**Files:** New `ingest/db/opencode-migration.test.ts`

1. Test: migration v13→v14 applies cleanly on a fresh DB
2. Test: migration v13→v14 preserves existing session data
3. Test: `INSERT INTO sessions (..., source, ...) VALUES (..., 'opencode', ...)` succeeds after migration
4. Test: `INSERT INTO sessions (..., source_cost_usd, cost_source, cost_pricing_status, ...) VALUES (..., 1.23, 'source-reported', 'priced', ...)` succeeds

**Verify:** `pnpm test:run -- ingest/db/opencode-migration` passes

## Acceptance Criteria

- [ ] `tsc --noEmit` passes with `opencode` in all type unions
- [ ] All existing ingest tests pass without modification
- [ ] Migration v14 applies cleanly and preserves data
- [ ] `INSERT` with `source = 'opencode'` succeeds after migration
- [ ] `source_cost_usd`, `cost_source`, `cost_pricing_status` columns exist on sessions table
