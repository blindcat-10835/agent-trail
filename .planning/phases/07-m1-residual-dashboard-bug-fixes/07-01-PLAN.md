---
phase: 07-m1-residual-dashboard-bug-fixes
plan: 01
title: Fix session rail, source switching, Codex freshness, and replay keys
status: completed
---

# Plan 07-01: Fix session rail, source switching, Codex freshness, and replay keys

## Goal

Resolve the complete bug batch in `.planning/2026-05-07-bugs-found-by-user-batch-1.md` without changing product scope beyond stabilization.

## Tasks

1. Fix `components/shell/source-switcher.tsx` so source switching from `sessions/[sessionId]` routes to the target source's session overview/list entry, not the old id.
2. Replace right-rail detail-only behavior with a compact persistent session list in `components/shell/right-rail.tsx`.
3. Add `components/sessions/sessions-right-rail.tsx` for compact session rows showing name, project, updated time, and tool/source, with refresh and selection navigation.
4. Update `app/(tool-shell)/[tool]/sessions/page.tsx` to render the tool overview/statistics when no session is selected instead of duplicating the session table in children.
5. Add a client refresh event in `lib/agent-tools/client-hooks.tsx`; dispatch it from `components/shell/shell-header.tsx` after `/api/sync` completes.
6. Fix replay keys in `components/replay/turn-card.tsx`, `components/replay/turn-timeline.tsx`, `components/replay/replay-right-rail.tsx`, and `components/replay/subagent-block.tsx` where needed.
7. Fix Codex freshness by using current payload timestamps, persisting file mtime/last-sync metadata, and sorting session lists by the freshest timestamp in `ingest/api/sessions.ts`.
8. Add focused tests for source switching helpers, session ordering, refresh notifications, Codex timestamp parsing, and key fallback helpers.
9. Run typecheck, ingest typecheck, and targeted tests.

## Verification

- `pnpm typecheck`
- `pnpm typecheck:ingest`
- `pnpm test:run tests/unit/bff/source-switcher-routing.test.ts tests/unit/bff/replay-key-utils.test.ts tests/unit/ingest/sessions-api.test.ts tests/hooks/client-hooks.test.tsx tests/unit/ingest/codex-parser.test.ts`
- Manual smoke if dev server is started: `/openclaw/sessions`, `/codex/sessions`, source switch from a selected session page, header sync button.

## Acceptance Criteria

- Source switch from `/openclaw/sessions/{id}` to Codex lands on `/codex/sessions`.
- Right rail shows sessions persistently and selection opens main replay/detail.
- Main children show overview/statistics when no session is selected.
- Duplicate key warning is addressed for null/missing ids.
- Codex sessions use parser/file/sync freshness metadata and recent sessions sort to the top.
- Header sync refreshes currently visible session lists.
