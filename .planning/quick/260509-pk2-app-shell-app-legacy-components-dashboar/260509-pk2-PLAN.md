---
phase: 260509-pk2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(shell)/** (DELETE all)
  - app/(legacy)/** (DELETE all)
  - components/dashboard/** (DELETE 19 files + overview/ subdirectory, KEEP empty-state.tsx)
  - components/hud/glow-effect.tsx (DELETE)
  - components/hud/hud-card.tsx (DELETE)
  - components/hud/ingest-status.tsx (DELETE)
  - components/hud/shell-header.tsx (DELETE)
  - components/hud/shell-status-bar.tsx (DELETE)
  - components/sessions/chat-bubble.tsx (DELETE)
  - components/sessions/session-explorer-table.tsx (DELETE)
  - components/sessions/sessions-detail-rail.tsx (DELETE)
  - components/sessions/sessions-filter-bar.tsx (DELETE)
  - components/sessions/sessions-table.tsx (DELETE)
autonomous: true
requirements: []

must_haves:
  truths:
    - "Next.js build completes without errors"
    - "All (tool-shell)/[tool] routes still render correctly"
    - "No console errors from missing imports in browser or terminal"
  artifacts:
    - path: "app/(tool-shell)/[tool]/tool-layout-client.tsx"
      provides: "Gateway bootstrap still functional"
    - path: "components/dashboard/empty-state.tsx"
      provides: "Kept alive — used by tool-shell dashboards"
    - path: "components/hud/gateway-bootstrap.tsx"
      provides: "Kept alive — Gateway connection bootstrap"
    - path: "components/hud/hud-panel.tsx"
      provides: "Kept alive — used by empty-state.tsx"
    - path: "components/hud/ingest-health-overlay.tsx"
      provides: "Kept alive — used by app/layout.tsx"
    - path: "components/hud/status-indicator.tsx"
      provides: "Kept alive — used by components/shell/shell-header.tsx"
    - path: "components/hud/theme-toggle.tsx"
      provides: "Kept alive — used by components/shell/shell-header.tsx"
    - path: "components/sessions/aggregate-sessions-view.tsx"
      provides: "Kept alive — used by tool-shell sessions + dashboard"
    - path: "components/sessions/sessions-right-rail.tsx"
      provides: "Kept alive — used by components/shell/right-rail.tsx"
    - path: "components/sessions/sessions-stats-bar.tsx"
      provides: "Kept alive — used by aggregate-sessions-view.tsx"
  key_links: []
---

<objective>
Remove dead code: two unreachable route groups (`(shell)` and `(legacy)`) and their exclusively-used component files from `components/dashboard/`, `components/hud/`, and `components/sessions/`. Keep files still imported by the alive `(tool-shell)` route group and `components/shell/`.

Purpose: Eliminate build conflicts between `(shell)` and `(legacy)` route groups, reduce codebase noise, and remove dead components with zero alive references.
Output: Cleaned codebase that builds without errors; all `(tool-shell)` routes functional.
</objective>

<execution_context>
@/Users/ebbi/Work/ai-dashboard-projects/agents-tracing-dashboard/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/ebbi/Work/ai-dashboard-projects/agents-tracing-dashboard/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

## Import Audit Results (verified against codebase)

### ALIVE importers (NOT in dead zones):
- `app/(tool-shell)/` — the active routing group
- `components/shell/` — the new shell components
- `app/layout.tsx` — root layout
- `components/replay/` — replay components
- `lib/` — utility code

### Files KEPT (imported by alive code):

**components/dashboard/:**
- `empty-state.tsx` — imported by `(tool-shell)/[tool]/dashboard/session-stats-dashboard.tsx`, `(tool-shell)/[tool]/dashboard/openclaw-dashboard.tsx`, and `components/sessions/aggregate-sessions-view.tsx`

**components/hud/:**
- `gateway-bootstrap.tsx` — imported by `(tool-shell)/[tool]/tool-layout-client.tsx`
- `hud-panel.tsx` — imported by `components/dashboard/empty-state.tsx`
- `ingest-health-overlay.tsx` — imported by `app/layout.tsx`
- `status-indicator.tsx` — imported by `components/shell/shell-header.tsx`
- `theme-toggle.tsx` — imported by `components/shell/shell-header.tsx`

**components/sessions/:**
- `aggregate-sessions-view.tsx` — imported by `(tool-shell)/[tool]/sessions/page.tsx` and `(tool-shell)/[tool]/dashboard/page.tsx`
- `sessions-right-rail.tsx` — imported by `components/shell/right-rail.tsx`
- `sessions-stats-bar.tsx` — imported by `components/sessions/aggregate-sessions-view.tsx`

### Files DELETED (zero alive references):

All other files in `components/dashboard/`, `components/hud/`, and `components/sessions/` have zero alive references. The `ingest-status.tsx` component is NOT the `IngestStatus` type used in `types/trace.ts` and `lib/agent-tools/client-hooks.tsx` — those define a standalone TypeScript type, not a component import.

### Route groups:
- `app/(shell)/` — contains 6 route files (layout.tsx + 5 page.tsx). Routes conflict with `(legacy)` at same URL paths. All imports point to dead components or hud files also being deleted.
- `app/(legacy)/` — contains 5 page.tsx redirect files. All do 307 redirects to `/openclaw/*`. Unreachable due to conflict with `(shell)`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead route groups and component files</name>
  <files>
    app/(shell)/** (entire directory, all files)
    app/(legacy)/** (entire directory, all files)
    components/dashboard/agent-basic-info.tsx
    components/dashboard/agent-capabilities.tsx
    components/dashboard/agent-card-grid.tsx
    components/dashboard/agent-card.tsx
    components/dashboard/agent-detail-panel.tsx
    components/dashboard/agent-drawer.tsx
    components/dashboard/agent-log-stream.tsx
    components/dashboard/agent-search-filter.tsx
    components/dashboard/agents-tab.tsx
    components/dashboard/dashboard-kpi-bar.tsx
    components/dashboard/dashboard-left-panel.tsx
    components/dashboard/dashboard-right-rail.tsx
    components/dashboard/overview-tab.tsx
    components/dashboard/quick-actions.tsx
    components/dashboard/radar-widget.tsx
    components/dashboard/sidebar-nav.tsx
    components/dashboard/skills-tab.tsx
    components/dashboard/tab-nav.tsx
    components/dashboard/overview/agent-avatar.tsx
    components/dashboard/overview/agent-card.tsx
    components/dashboard/overview/agent-drawer.tsx
    components/dashboard/overview/agent-status-meta.ts
    components/dashboard/overview/cron-drawer.tsx
    components/dashboard/overview/skills-list.tsx
    components/hud/glow-effect.tsx
    components/hud/hud-card.tsx
    components/hud/ingest-status.tsx
    components/hud/shell-header.tsx
    components/hud/shell-status-bar.tsx
    components/sessions/chat-bubble.tsx
    components/sessions/session-explorer-table.tsx
    components/sessions/sessions-detail-rail.tsx
    components/sessions/sessions-filter-bar.tsx
    components/sessions/sessions-table.tsx
  </files>
  <action>
    **Step 1 — Delete route groups (entire directories):**
    ```bash
    rm -rf app/'(shell)' app/'(legacy)'
    ```

    **Step 2 — Delete dead dashboard files (keep empty-state.tsx):**
    ```bash
    rm components/dashboard/agent-basic-info.tsx
    rm components/dashboard/agent-capabilities.tsx
    rm components/dashboard/agent-card-grid.tsx
    rm components/dashboard/agent-card.tsx
    rm components/dashboard/agent-detail-panel.tsx
    rm components/dashboard/agent-drawer.tsx
    rm components/dashboard/agent-log-stream.tsx
    rm components/dashboard/agent-search-filter.tsx
    rm components/dashboard/agents-tab.tsx
    rm components/dashboard/dashboard-kpi-bar.tsx
    rm components/dashboard/dashboard-left-panel.tsx
    rm components/dashboard/dashboard-right-rail.tsx
    rm components/dashboard/overview-tab.tsx
    rm components/dashboard/quick-actions.tsx
    rm components/dashboard/radar-widget.tsx
    rm components/dashboard/sidebar-nav.tsx
    rm components/dashboard/skills-tab.tsx
    rm components/dashboard/tab-nav.tsx
    rm -rf components/dashboard/overview
    ```

    **Step 3 — Delete dead hud files (keep 5 alive files):**
    ```bash
    rm components/hud/glow-effect.tsx
    rm components/hud/hud-card.tsx
    rm components/hud/ingest-status.tsx
    rm components/hud/shell-header.tsx
    rm components/hud/shell-status-bar.tsx
    ```

    **Step 4 — Delete dead sessions files (keep 3 alive files):**
    ```bash
    rm components/sessions/chat-bubble.tsx
    rm components/sessions/session-explorer-table.tsx
    rm components/sessions/sessions-detail-rail.tsx
    rm components/sessions/sessions-filter-bar.tsx
    rm components/sessions/sessions-table.tsx
    ```

    **Step 5 — Verify kept files exist:**
    ```bash
    ls components/dashboard/empty-state.tsx \
       components/hud/gateway-bootstrap.tsx \
       components/hud/hud-panel.tsx \
       components/hud/ingest-health-overlay.tsx \
       components/hud/status-indicator.tsx \
       components/hud/theme-toggle.tsx \
       components/sessions/aggregate-sessions-view.tsx \
       components/sessions/sessions-right-rail.tsx \
       components/sessions/sessions-stats-bar.tsx
    ```
    
    All 9 files must return success. If any are missing, STOP — they were incorrectly deleted.
  </action>
  <verify>
    <automated>ls components/dashboard/empty-state.tsx components/hud/gateway-bootstrap.tsx components/hud/hud-panel.tsx components/hud/ingest-health-overlay.tsx components/hud/status-indicator.tsx components/hud/theme-toggle.tsx components/sessions/aggregate-sessions-view.tsx components/sessions/sessions-right-rail.tsx components/sessions/sessions-stats-bar.tsx && echo "PASS: All 9 alive files present" || echo "FAIL: Missing alive file"</automated>
  </verify>
  <done>
    - `app/(shell)/` and `app/(legacy)/` directories no longer exist
    - `components/dashboard/` contains only `empty-state.tsx` (+ maybe `overview/` directory is gone too)
    - `components/hud/` contains exactly 5 files: gateway-bootstrap.tsx, hud-panel.tsx, ingest-health-overlay.tsx, status-indicator.tsx, theme-toggle.tsx
    - `components/sessions/` contains exactly 3 files: aggregate-sessions-view.tsx, sessions-right-rail.tsx, sessions-stats-bar.tsx
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify build and (tool-shell) routes</name>
  <files>(no new files — verification only)</files>
  <action>
    **Step 1 — Type-check the project:**
    Run `pnpm typecheck` to verify no broken import references from the deletions.

    Expected: Zero TypeScript errors. If any errors appear, they reference deleted files — STOP and investigate which kept file imports a deleted one.

    **Step 2 — Run the production build:**
    Run `pnpm build` to verify Next.js compiles cleanly without the conflicting `(shell)` and `(legacy)` route groups.

    Expected: Build completes without errors. The documented route conflict (two route groups competing for `/dashboard`, `/sessions`, etc.) should be resolved.

    **Step 3 — Verify kept component integrity:**
    Confirm the kept files compile without their deleted dependencies:
    - `empty-state.tsx` only imports `hud-panel` from hud (kept) — no broken refs
    - `aggregate-sessions-view.tsx` imports `sessions-stats-bar` (kept) and `empty-state` (kept) — no broken refs
    - `sessions-stats-bar.tsx` has no internal component imports — safe
    - `sessions-right-rail.tsx` has no internal component imports — safe

    **Step 4 — Verify (tool-shell) routes exist in build output:**
    Check that the `(tool-shell)/[tool]` dynamic routes are present in the build:
    ```bash
    ls .next/server/app/'(tool-shell)'/ 2>/dev/null || echo "Check .next build output for (tool-shell) routes"
    ```
  </action>
  <verify>
    <automated>pnpm typecheck && echo "PASS: TypeScript compilation clean"</automated>
  </verify>
  <done>
    - `pnpm typecheck` passes with zero errors
    - `pnpm build` completes successfully
    - No import errors referencing deleted `(shell)`, `(legacy)`, or dead component files
    - `(tool-shell)` routes remain functional in the build output
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify:
1. `pnpm typecheck` — zero TypeScript errors
2. `pnpm build` — Next.js builds without errors
3. No files in the codebase import from deleted paths (verify with `rg "from.*@/components/dashboard/(agent-basic-info|agent-capabilities|agent-card-grid|agent-card[^-]|agent-detail|agent-drawer|agent-log|agent-search|agents-tab|dashboard-kpi|dashboard-left|dashboard-right|overview-tab|quick-actions|radar-widget|sidebar-nav|skills-tab|tab-nav)" --glob '*.tsx' --glob '*.ts'` returns nothing outside dead directories)
</verification>

<success_criteria>
- Dead route groups `(shell)` and `(legacy)` removed from `app/`
- 19 dead dashboard files + 6 overview/ subdirectory files deleted
- 5 dead hud files deleted (5 kept)
- 5 dead sessions files deleted (3 kept)
- Project type-checks and builds cleanly
- All 9 kept component files present and functional
</success_criteria>

<output>
After completion, create `.planning/quick/260509-pk2-app-shell-app-legacy-components-dashboar/260509-PLAN-SUMMARY.md`
</output>
