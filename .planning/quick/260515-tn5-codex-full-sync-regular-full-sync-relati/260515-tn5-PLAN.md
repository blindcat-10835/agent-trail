---
quick_id: 260515-tn5
status: planned
created: 2026-05-15
---

# Quick Task 260515-tn5: Codex Full Sync Relationship Scope

## Goal

Implement regular Codex full sync as directory consistency checking plus per-file skip/incremental parsing, without an up-front all-history relationship scan. Keep manual refresh available and change the periodic fallback interval to 15 minutes.

## Tasks

1. Update Codex sync relationship handling
   - Files: `ingest/sync/index.ts`
   - Action: remove regular `syncCodexSource()` pre-pass over `collectCodexRelationships(sources)` and instead collect relationships from parsed Codex `subagent_link` activities during changed-file processing, with targeted backfill for relationships discovered in that run.
   - Verify: targeted Codex sync and relationship tests pass.

2. Change periodic fallback interval to 15 minutes
   - Files: `ingest/config/index.ts`, `ingest/src/watcher.ts`, docs if needed
   - Action: change default `INGEST_RESYNC_INTERVAL_MS` from 300000 to 900000 while preserving env override and manual sync route behavior.
   - Verify: config/source references and watcher tests remain consistent.

3. Add regression coverage
   - Files: `tests/unit/ingest/sync-performance.test.ts`, `tests/unit/ingest/codex-relationships.test.ts`
   - Action: assert regular Codex sync source no longer calls the global relationship collector before per-file skip/parse, and assert parsed parent links can backfill an existing child row.
   - Verify: run focused Vitest suites and TypeScript check if feasible.
