---
quick_id: 260515-xa7
status: planned
created: 2026-05-15
---

# Quick Task 260515-xa7: Ingest Session Display Name Extraction

## Goal

Optimize `extractSessionName()` / `deriveDisplayNameFromUserMessage()` so large user messages do not trigger full-content `trim`, broad regex capture, or whole-message line splitting during ingest.

## Scope

- File: `ingest/sync/index.ts`
- Test: `tests/unit/ingest/sync-performance.test.ts`
- Planning state: `.planning/STATE.md`

## Tasks

1. Replace full-message string transforms with bounded preview scanning.
2. Preserve current display-name behavior for command args, Codex request blocks, OpenClaw metadata blocks, metadata-only prefixes, and normal first-line fallback.
3. Add regression coverage that asserts the helper path avoids full-content transforms and still derives a name from a large user message.
4. Run targeted ingest performance tests and TypeScript check if feasible.
