---
phase: quick/20260506-001-dev-compile-storm-fix
status: complete
date: 2026-05-06
---

# Quick Task 20260506-001: Dev Compile Storm Fix

**Commit:** d8ddefe

## Problem

`pnpm dev` caused a noisy Next.js dev compile loop and high CPU usage during route compilation. The user-visible macOS symptom was repeated `node(...) MallocStackLogging: can't turn off malloc stack logging because it was not enabled` lines while Next compiled `/`.

## Root Cause

1. Several frontend modules expected the restored OpenClaw dashboard state/type layer, but this workspace was missing `stores/`, `types/activity.ts`, and `types/log.ts`. That produced module-resolution failures during route compilation.
2. After the missing modules were restored, `next dev` with Turbopack still reproduced a dev-only resolver storm. The dev compiler repeatedly tried to resolve `tailwindcss` from the parent directory `/Users/ebbi/Work/ai-dashboard-projects` instead of the project root, spawning many worker processes/log lines.
3. Production build was not affected: `pnpm build` completed successfully with Turbopack.

## Changes

1. Restored the missing frontend state/type files:
   - `stores/activity-store.ts`
   - `stores/log-store.ts`
   - `stores/sidebar-store.ts`
   - `types/activity.ts`
   - `types/log.ts`
2. Changed `package.json` development script:
   - `dev:next` now runs `next dev --webpack`
   - `pnpm dev` still starts both Next.js and the ingest service through `concurrently`
3. Cleaned TypeScript project configuration:
   - kept Next-generated JSX/include settings
   - removed the invalid root TypeScript project reference that made `pnpm typecheck` fail
4. Fixed tests to match current trace/ingest contracts:
   - `TraceTurn.startedAt` / `endedAt` use `null`
   - `insertToolCall(...)` test call matches the current function signature

## Verification

- `pnpm build` passed
- `pnpm typecheck` passed
- `pnpm test:run` passed: 13 test files, 153 tests
- `pnpm dev` was verified with:
  - `GET /dashboard` -> `200`
  - `GET /` -> `307`
  - no repeated Tailwind resolver storm during those requests

## Operational Note

Keep `next dev --webpack` for local development until the Next.js/Turbopack dev CSS resolver behavior is fixed or this workspace is reorganized so Turbopack resolves Tailwind from the project root consistently. Production build can continue using the existing build path because `pnpm build` passed.
