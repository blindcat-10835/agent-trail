---
phase: quick/20260502-003-session-fixes
status: complete
---

# Quick Task 20260502-003: Session Fixes

**Commit:** a150ccc

## What Was Done

1. **sessions-detail-rail.tsx** — Removed error state display on fetch failure. Non-ok API responses now return `[]` silently; catch handler sets `messages=[]` instead of `setError(...)`. Result: drawer shows "No messages" instead of "Failed to fetch messages" when the gateway sessions directory is unavailable.

2. **app/(shell)/sessions/page.tsx** — Added `max-w-5xl mx-auto w-full` wrapper inside the scroll container. Content (stats bar + filter bar + table) is now centered with a reasonable max width instead of stretching full viewport width.
