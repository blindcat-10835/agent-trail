---
phase: 01-trace-contract-brownfield-reset
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/preserved-capabilities.md
autonomous: true
requirements:
  - FOUND-04
user_setup: []
must_haves:
  truths:
    - "All existing OpenClaw overview capabilities are documented"
    - "Capabilities are categorized by dependency source (Gateway-exclusive vs File-replaceable)"
    - "Gateway-exclusive capabilities are marked as 'preserved but isolated'"
    - "Document provides a clear audit trail for Phase 4 frontend migration"
  artifacts:
    - path: "docs/preserved-capabilities.md"
      provides: "Inventory of OpenClaw overview capabilities"
      min_lines: 80
      contains: "## Gateway-Exclusive Capabilities", "## File-Replaceable Capabilities"
  key_links:
    - from: "docs/preserved-capabilities.md"
      to: ".planning/REQUIREMENTS.md"
      via: "requirement mapping"
      pattern: "OPEN-01, OPEN-02, OPEN-03"
    - from: "docs/preserved-capabilities.md"
      to: "Phase 4 frontend architecture"
      via: "migration reference"
      pattern: "Preserved capabilities list"
---

<objective>
Document all existing OpenClaw overview capabilities and categorize them by dependency source.

Purpose: Create a clear audit trail of what must be preserved during the brownfield reset from OVAO to agent-tracing-dashboard. This document ensures Phase 4 frontend migration doesn't accidentally remove working features.

Output: Comprehensive capabilities inventory categorized as Gateway-exclusive (requires real-time connection) or file-replaceable (can work with local file parsing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-trace-contract-brownfield-reset/01-CONTEXT.md
@.planning/phases/01-trace-contract-brownfield-reset/01-RESEARCH.md
@.planning/REQUIREMENTS.md

@/Users/ebbi/Work/ai-dashboard-projects/agents-tracing-dashboard/app/(shell)/dashboard
@/Users/ebbi/Work/ai-dashboard-projects/agents-tracing-dashboard/gateway/adapter-types.ts
@/Users/ebbi/Work/ai-dashboard-projects/agents-tracing-dashboard/stores/gateway
</context>

<interfaces>
<!-- Key interfaces from existing codebase that define overview capabilities -->

From gateway/adapter-types.ts (dashboard display types):
```typescript
// Overview component data structures
export interface ChannelInfo {
  id: string;
  name: string;
  // ...
}

export interface SessionInfo {
  id: string;
  agentId: string;
  status: string;
  // ...
}

export interface SkillInfo {
  name: string;
  description: string;
  // ...
}
```

Pattern: These interfaces represent the data contracts for current overview components. The preserved capabilities document should map each capability to its data source (Gateway WebSocket vs file-based).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit existing OpenClaw overview components</name>
  <files>app/(shell)/dashboard/*</files>
  <read_first>
    1. app/(shell)/dashboard/page.tsx (main dashboard page)
    2. app/(shell)/layout.tsx (shell layout with navigation)
    3. gateway/adapter-types.ts (display type definitions)
    4. stores/gateway/* (Zustand stores for overview state)
    5. .planning/REQUIREMENTS.md (OPEN-01 requirement for preserved overview)
  </read_first>
  <action>
1. **Read and analyze dashboard components**:
   - List all components in app/(shell)/dashboard/
   - Identify each component's purpose and data dependencies
   - Note which components use Gateway WebSocket data (stores/gateway/*)
   - Note which components can work with static/session data

2. **Extract capability categories**:
   - Agent status capabilities (live agent state, connection health)
   - Session list capabilities (historical sessions, search, filtering)
   - Activity stream capabilities (real-time activity log, event streaming)
   - KPI/metrics capabilities (token usage, cost tracking, performance stats)
   - Skills/Cron capabilities (skill inventory, scheduled tasks)
   - Workspace/Office capabilities (2D layout, agent positions)

3. **Map capabilities to data sources**:
   - Gateway WebSocket: Agent live status, real-time activity stream, Gateway connection state
   - File/session data: Sessions list, KPI metrics, skill inventory, cron jobs
   - Hybrid: Session replay can start from Gateway live session, then switch to file-based history

4. **Create inventory table** (for use in Task 2):
   - Columns: Capability, Current Implementation, Data Source, Preserved Strategy
   - Row example: "Agent live status" | "AgentInfoCard component" | "Gateway WebSocket" | "Preserved as Gateway-exclusive, no changes in Phase 1"

5. **Identify components at risk**:
   - Components that mix Gateway and file data (may need refactoring in Phase 4)
   - Components that assume Gateway always exists (need to handle Gateway-disconnected state)
   - Components with hardcoded OVAO references (need brownfield rename per D-09)

Output: Create a scratchpad or comment block listing all discovered capabilities with their data source mappings. This will be structured into the final document in Task 2.
  </action>
  <verify>
    <automated>grep -r "AgentInfo\|SessionInfo\|Activity\|KPI\|Skill\|Cron" app/\(shell\)/dashboard/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - At least 10 distinct capabilities identified
    - Each capability is mapped to a data source (Gateway WebSocket / file-based / hybrid)
    - Components using Gateway data are distinguished from components using session data
    - At least 3 components identified as Gateway-exclusive
    - grep -c "Agent" app/(shell)/dashboard/*.tsx returns >= 1 (or equivalent search)
    - grep -c "Session" app/(shell)/dashboard/*.tsx returns >= 1
  </acceptance_criteria>
  <done>
    Complete inventory of OpenClaw overview capabilities with data source mappings. Ready to structure into formal documentation.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create preserved capabilities documentation</name>
  <files>docs/preserved-capabilities.md</files>
  <read_first>
    1. docs/preserved-capabilities.md (file being created)
    2. .planning/REQUIREMENTS.md (OPEN-01, OPEN-02, OPEN-03 requirements)
    3. .planning/phases/01-trace-contract-brownfield-reset/01-PATTERNS.md (documentation structure pattern)
    4. Task 1 output (capability inventory)
  </read_first>
  <action>
Create docs/preserved-capabilities.md with the following structure:

1. **Frontmatter**:
   ```markdown
   # Preserved Capabilities - OpenClaw Overview

   **Last updated:** 2026-05-06
   **Phase:** 1 - Trace Contract & Brownfield Reset
   **Purpose:** Audit trail of existing OpenClaw overview capabilities to ensure preservation during brownfield reset
   ```

2. **Overview Section**:
   - Explain what this document tracks (all current OpenClaw overview capabilities)
   - Explain preservation strategy (Gateway-exclusive vs file-replaceable)
   - Link to OPEN-01, OPEN-02, OPEN-03 requirements

3. **Gateway-Exclusive Capabilities** (per D-12, D-13):
   - **Agent Live Status**: Real-time agent state from Gateway WebSocket
   - **Gateway Connection Health**: Connection status, reconnection state, error state
   - **Real-time Activity Stream**: Live event streaming from Gateway (non-historical)
   - **Active Session Monitor**: Currently running sessions with live updates

   For each capability:
   - What it does
   - Which component implements it
   - Why it's Gateway-exclusive (requires real-time WebSocket data)
   - Preservation strategy: "Preserved but isolated — no changes in Phase 1, may be re-evaluated with Gateway in future phases"

4. **File-Replaceable Capabilities** (per D-12):
   - **Sessions List**: Historical session browsing, search, filtering
   - **KPI/Metrics Dashboard**: Token usage, cost tracking, session statistics
   - **Skills Inventory**: List of available skills, skill metadata
   - **Cron Jobs**: Scheduled tasks, job history, status
   - **Activity History**: Past activity events, searchable logs

   For each capability:
   - What it does
   - Which component implements it
   - Why it's file-replaceable (can work with parsed session data from ingest service)
   - Migration target: "Will be served by ingest API in Phase 2-4"

5. **Dependency Mapping Table**:
   ```markdown
   | Capability | Data Source | Current Component | Preservation Strategy |
   |------------|-------------|-------------------|----------------------|
   | Agent live status | Gateway WebSocket | AgentInfoCard | Gateway-exclusive, preserved |
   | Sessions list | File | SessionList | Replaceable, Phase 2-4 |
   | Real-time activity | Gateway WebSocket | ActivityStream | Gateway-exclusive, preserved |
   | KPI metrics | File | KPICard | Replaceable, Phase 2-4 |
   ```
   - Include at least 10 capabilities in the table
   - Separate Gateway-exclusive and file-replaceable capabilities

6. **Phase 4 Migration Notes**:
   - Components that mix Gateway and file data (need refactoring)
   - Components that assume Gateway always exists (need to handle Gateway-disconnected state per OPEN-03)
   - Components that need dual-status support (ingest + gateway status per D-14)

7. **References Section**:
   - Link to app/(shell)/dashboard/ components
   - Link to gateway/adapter-types.ts
   - Link to stores/gateway/
   - Link to REQUIREMENTS.md OPEN-01, OPEN-02, OPEN-03

Follow documentation structure pattern from .planning/phases/01-trace-contract-brownfield-reset/01-PATTERNS.md § "docs/preserved-capabilities.md (documentation, N/A)".
  </action>
  <verify>
    <automated>grep -c "## " docs/preserved-capabilities.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/preserved-capabilities.md exists
    - Contains "## Gateway-Exclusive Capabilities" section
    - Contains "## File-Replaceable Capabilities" section
    - Contains capability mapping table with at least 10 rows
    - Contains "## Phase 4 Migration Notes" section
    - grep -c "Gateway-exclusive" docs/preserved-capabilities.md returns >= 3
    - grep -c "File-replaceable" docs/preserved-capabilities.md returns >= 3
    - wc -l docs/preserved-capabilities.md returns >= 80 (substantive documentation)
  </acceptance_criteria>
  <done>
    Preserved capabilities document exists with complete inventory of OpenClaw overview capabilities, categorized by data source, and includes Phase 4 migration guidance.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation only — no runtime data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | N/A |

**Note**: Plan 03 is pure documentation work. No security threats apply.
</threat_model>

<verification>
## Overall Phase Checks

### Documentation Completeness
- [ ] All current OpenClaw overview capabilities are documented
- [ ] Each capability is categorized (Gateway-exclusive vs file-replaceable)
- [ ] Capability mapping table has at least 10 rows
- [ ] Phase 4 migration notes identify components at risk

### Traceability
- [ ] Document references OPEN-01, OPEN-02, OPEN-03 requirements
- [ ] Document links to current component implementations
- [ ] Document explains preservation strategy for each capability
</verification>

<success_criteria>
## Measurable Completion

1. **Document Created**: docs/preserved-capabilities.md exists with >= 80 lines
2. **Capability Inventory**: At least 10 distinct capabilities documented
3. **Categorization Complete**: Gateway-exclusive and file-replaceable sections are distinct
4. **Mapping Table**: Table includes columns for Capability, Data Source, Current Component, Preservation Strategy
5. **Migration Guidance**: Phase 4 Migration Notes section identifies components needing refactoring
6. **Requirement Traceability**: Document references OPEN-01, OPEN-02, OPEN-03 requirements
7. **Component Links**: Document links to app/(shell)/dashboard/ components and stores/gateway/
</success_criteria>

<output>
After completion, create `.planning/phases/01-trace-contract-brownfield-reset/01-03-SUMMARY.md` with:
- Total capabilities documented
- Breakdown: X Gateway-exclusive, Y file-replaceable
- Components identified as at-risk for Phase 4 migration
- Any ambiguities discovered (capabilities that could be either Gateway or file-based)
- Next steps for Plan 04 (documentation brownfield reset)
</output>
