---
phase: 09-batch-2-session-replay-and-codex-subagent-relationship-fixes
plan: 04
title: Codex patch category and subagent event anchoring
type: execute
wave: 1
depends_on: []
files_modified:
  - ingest/parser/codex.ts
  - tests/unit/ingest/codex-parser.test.ts
autonomous: true
requirements:
  - SRC-03
  - SRC-04
  - TURN-03
  - REPLAY-03
requirements_addressed: [SRC-03, SRC-04, TURN-03, REPLAY-03]
tags:
  - ingest
  - codex-parser
  - apply-patch
  - subagent-link
must_haves:
  truths:
    - "Codex `apply_patch`, `patch`, and file edit-like custom tools infer category `Edit`."
    - "Codex patch tool calls still pair with their custom/function output events by call id."
    - "Codex `collab_agent_spawn_end` with a valid `new_thread_id` creates a canonical `TraceSubagentLink` anchored to the spawning tool message when call id is known."
  artifacts:
    - path: "ingest/parser/codex.ts"
      provides: "Codex category inference and subagent link anchoring"
    - path: "tests/unit/ingest/codex-parser.test.ts"
      provides: "parser regression for apply_patch and collab_agent_spawn_end"
  key_links:
    - from: "ingest/parser/codex.ts"
      to: "types/trace.ts"
      via: "TraceToolCall.category and TraceSubagentLink.messageOrdinal"
      pattern: "category: inferToolCategory"
---

## Goal

修复 B2-04 的 Codex parser 分类缺口, 并补强 B2-05 的 subagent event anchoring。UI formatter 负责显示 patch, parser 负责把 edit-like Codex tools 归类为 `Edit` 并保持 call id/result pairing 不退化。

<execution_context>
@/Users/ebbi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ebbi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-batch-2-session-replay-and-codex-subagent-relationship-fixes/09-CONTEXT.md
@.planning/phases/09-batch-2-session-replay-and-codex-subagent-relationship-fixes/09-RESEARCH.md
@.planning/phases/09-batch-2-session-replay-and-codex-subagent-relationship-fixes/09-VALIDATION.md
@.planning/phases/09-batch-2-session-replay-and-codex-subagent-relationship-fixes/09-PATTERNS.md
@.planning/phases/03-claude-codex-parsers-turn-assembly/03-05-SUMMARY.md
@AGENTS.md
</context>

<tasks>
<task type="auto" tdd="true">
  <id>09-04-01</id>
  <title>Add Codex parser regressions for patch tools and spawn-end links</title>
  <name>Task 1: Add Codex parser regressions for patch tools and spawn-end links</name>
  <wave>1</wave>
  <files>tests/unit/ingest/codex-parser.test.ts</files>
  <read_first>
    - `tests/unit/ingest/codex-parser.test.ts`: `writeFixture` pattern, custom tool call tests, spawn-agent tests.
    - `ingest/parser/codex.ts`: `custom_tool_call`, output pairing, `collab_agent_spawn_end`, `inferToolCategory`.
    - `types/trace.ts`: `TraceToolCall` and `TraceSubagentLink.messageOrdinal`.
    - `.planning/bugs-by-user/2026-05-10-bugs-found-by-user-batch-2-research.md`: real Codex `collab_agent_spawn_end` shape.
  </read_first>
  <behavior>
    - Test 1: `custom_tool_call` named `apply_patch` with patch text produces one `TraceToolCall` with `category === 'Edit'`.
    - Test 2: the same tool pairs `custom_tool_call_output` by `call_id` and becomes `status === 'success'` when output status is `completed`.
    - Test 3: `patch` and `file_edit` style names also infer `Edit`.
    - Test 4: `collab_agent_spawn_end` with string `new_thread_id`, `sender_thread_id`, and matching `call_id` creates `TraceSubagentLink` with `subagentSessionId` and `messageOrdinal` equal to the spawning tool call ordinal.
    - Test 5: `collab_agent_spawn_end` with `new_thread_id: null` is ignored and does not create a broken subagent link.
  </behavior>
  <action>
    Extend `tests/unit/ingest/codex-parser.test.ts` using inline JSONL fixtures. Include a `session_meta`, a `response_item` or `event_msg` custom tool call with `call_id`, and the corresponding output event. Keep assertions on existing `TraceToolCall.resultEvents` behavior so TURN-03 result pairing is protected. Add subagent event tests using real fields `call_id`, `sender_thread_id`, `new_thread_id`, `new_agent_nickname`, and `status`, but only require canonical fields already present in `TraceSubagentLink`.
  </action>
  <acceptance_criteria>
    - Tests fail before parser changes because `apply_patch` is `Other` and spawn-end links lack `messageOrdinal`.
    - Tests do not require new DB schema columns or type expansion.
    - Tests keep malformed/null child events from producing links.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test:run tests/unit/ingest/codex-parser.test.ts</automated>
  </verify>
  <done>Codex parser expectations for patch tools and spawn-end links are pinned.</done>
</task>

<task type="auto" tdd="true">
  <id>09-04-02</id>
  <title>Implement Codex patch category inference and link anchoring</title>
  <name>Task 2: Implement Codex patch category inference and link anchoring</name>
  <wave>1</wave>
  <files>ingest/parser/codex.ts</files>
  <read_first>
    - `ingest/parser/codex.ts`: `toolCallMap`, `function_call`, `custom_tool_call`, output pairing, `collab_agent_spawn_end`, `inferToolCategory`.
    - `types/trace.ts`: existing `TraceSubagentLink.messageOrdinal` field.
    - `.planning/phases/09-batch-2-session-replay-and-codex-subagent-relationship-fixes/09-CONTEXT.md`: Codex `apply_patch` category and subagent decisions.
  </read_first>
  <action>
    Update `inferToolCategory(name)` so edit-like Codex tools return `Edit`: exact lowercase `apply_patch`, exact `patch`, names containing `apply_patch`, names containing `file_edit`, and names containing `patch` should be classified before the generic `Other` fallback. Preserve existing Bash, Read, Grep, Task, and Agent behavior.
    
    Add a lightweight `Map<string, number>` near the existing `toolCallMap` to remember `call_id -> messageOrdinal` for `function_call` and `custom_tool_call`. When handling `collab_agent_spawn_end`, create a `TraceSubagentLink` only when `typeof ev.new_thread_id === 'string' && ev.new_thread_id.length > 0`. If `ev.call_id` exists in the ordinal map, set `messageOrdinal` on the link. Do not add database writes in the parser. Do not use `hide_single_turn` or any UI filtering as a parser substitute.
  </action>
  <acceptance_criteria>
    - `inferToolCategory('apply_patch')` behavior is covered by parser output tests as `Edit`.
    - Custom tool output pairing still pushes result events to the same tool call.
    - `collab_agent_spawn_end` ignores null/missing child thread ids.
    - No schema migration or ORM push is added.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test:run tests/unit/ingest/codex-parser.test.ts tests/unit/ingest/turn-activity-regression.test.ts</automated>
    <automated>pnpm typecheck:ingest</automated>
  </verify>
  <done>Codex patch tools are categorized as Edit and spawn-end subagent links are anchored without breaking tool pairing.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Codex JSONL -> parser | Local log records are untrusted and may be malformed or incomplete. |
| Parser activities -> DB sync | Parser categories and links feed persisted activities and replay UI. |
| call_id relationship -> UI anchoring | call ids may be absent or duplicated; parser must degrade safely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-12 | Tampering | `inferToolCategory` | mitigate | Use explicit name checks only; do not parse or execute patch content. |
| T-09-13 | Denial of Service | malformed JSONL events | mitigate | Preserve parser error-tolerant behavior and ignore invalid `new_thread_id`. |
| T-09-14 | Repudiation | tool/result pairing | mitigate | Tests assert output events still pair by `call_id` after category changes. |
| T-09-15 | Information Disclosure | subagent event metadata | accept | Only existing local trace ids are emitted; no new file content or path reads are introduced. |
</threat_model>

## Verification

Run:

```bash
pnpm test:run tests/unit/ingest/codex-parser.test.ts tests/unit/ingest/turn-activity-regression.test.ts
pnpm typecheck:ingest
```

## Acceptance Criteria

- Codex patch-like tool calls are `Edit`.
- Tool outputs still pair correctly.
- Valid `collab_agent_spawn_end` creates a usable subagent link.
- Null child thread ids are ignored.

<output>
After completion, create `.planning/phases/09-batch-2-session-replay-and-codex-subagent-relationship-fixes/09-04-SUMMARY.md`.
</output>
